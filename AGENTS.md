# AGENTS.md

## 프로젝트

네이버지도 기반 식당 리뷰 서비스. 네이버지도와 카카오맵의 리뷰 신뢰 문제를 개선하고, 지도 위 업체 정보와 영수증 인증 리뷰를 하나의 업체 상세 흐름에 통합한다.

## 현재 단계

네이버커넥트 AI Agent Challenge 4주 과정 중 초기 설계 단계다. 지금은 핵심 기능 화면, 디자인 시스템, 개발 환경, Agent 지침을 정리한다. 본격 구현은 2주차부터 진행한다.

## 참고 문서

- 기획서: @plan.md
- 작업 분해: @checklist.md
- 디자인 시스템: @docs/design-system.md
- 개발 환경 결정: @docs/dev-setup.md
- 디자인 Skill: @docs/skills/trusted-place-design/SKILL.md
- 코드 검증 Skill: @docs/skills/code-verification/SKILL.md
- AI Agent Workflow: @docs/ai-workflow.md

## 핵심 사용자 가치

- 처음 방문한 동네에서도 믿을 수 있는 식당을 빠르게 고른다.
- 모든 집계 리뷰는 영수증 OCR 인증을 통과해야 한다.
- 좋아요를 많이 받은 리뷰는 평점에 더 반영하되, 작성 후 90일이 지나면 부스트를 제거한다.
- 업체를 누르면 네이버지도처럼 식당 정보, 대표 메뉴, 리뷰, 리뷰 작성 흐름을 확인한다.

## 기술 스택 기준

- 현재 저장소: Create React App 기반 React 프로젝트
- 목표 구조: React + TypeScript 프론트엔드, Express API 서버, Supabase(Postgres) 데이터베이스
- 지도: 네이버지도 API를 우선 검토한다. 카카오맵은 비교/확장 후보로 둔다.
- OCR: CLOVA OCR 연동을 우선 검토한다.
- 외부 UI 라이브러리는 합의 전까지 추가하지 않는다.

## 개발 원칙

- 사용자가 요청하지 않은 대규모 프레임워크 전환을 하지 않는다.
- 현재 CRA 프로젝트를 Next.js로 바꾸지 않는다. 전환이 필요하면 사유와 마이그레이션 계획을 먼저 문서화한다.
- 인증 리뷰와 가중 별점은 도메인 핵심이므로 임의로 단순화하지 않는다.
- 더미 데이터는 프로토타입 표시용으로만 사용하고, 실제 로직처럼 오해될 이름을 붙이지 않는다.
- 지도/업체 정보/리뷰 화면은 모바일 우선으로 설계한다.

## 컨벤션

- 컴포넌트: PascalCase
- 함수/변수: camelCase
- 상수: UPPER_SNAKE_CASE
- 커밋: `feat`, `fix`, `refactor`, `docs`, `style`, `test`
- UI 문구는 기본적으로 한국어를 사용한다.

## 도메인 규칙

- 리뷰 작성에는 영수증 이미지가 필요하다.
- OCR 인증은 상호명과 결제일을 검증한다.
- 결제일은 리뷰 작성일 기준 30일 이내만 인정한다.
- 이미지 해시와 승인번호로 영수증 중복 사용을 막는다.
- 가중치 공식은 `1 + log2(1 + 좋아요 수)`를 기본으로 한다.
- 좋아요 가중치와 상위 노출 부스트는 리뷰 작성 후 90일 동안만 유지한다.
- 긍정 리뷰는 4~5점, 아쉬운 리뷰는 1~3점으로 탭을 나눈다.

## 하지 말 것

- 방문 인증 없는 리뷰를 집계 대상에 포함하지 않는다.
- 예약 기능을 핵심 서비스처럼 다루지 않는다. 필요하면 추후 부가 기능으로만 검토한다.
- 디자인 시스템 없이 화면마다 색상, 반경, 여백을 새로 만들지 않는다.
- 카드 안에 또 다른 카드가 중첩되는 레이아웃을 만들지 않는다.
- 별도 합의 없이 `any` 타입, 무분별한 전역 상태, 과한 애니메이션을 도입하지 않는다.
