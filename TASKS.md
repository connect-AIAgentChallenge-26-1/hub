# 개발 Task 백로그

> 워크플로우: 기능별 GitHub 이슈 등록 → 설계 → 개발 → **dev 브랜치 대상 PR** → 리뷰·승인 후 머지 → 이슈 close
> 우선순위: **P0** 반드시 (MVP) · **P1** 있으면 좋음 · **P2** 여유 시

## ✅ 완료 (1~2주차)

- [x] 회원가입/로그인 — 이메일 + 구글 OAuth (캘린더 권한 통합)
- [x] ERD 5개 테이블 스키마 + Alembic 마이그레이션
- [x] 캘린더 3종 연동 — 구글 / 에브리타임 / 애플(CalDAV), 실계정 검증 완료
- [x] 기획서·디자인 시안·발표자료 (docs/)

## 🔜 3주차 — 밥약 모임 + 공강 계산 (핵심)

| 우선순위 | Task | 내용 | 완료 기준 |
|---|---|---|---|
| P0 | **Feat-1. 모임 생성 + 내 모임 목록** | `meetup_sessions` 활용, `POST /meetups` · `GET /meetups`, 모임 목록/생성 화면 | 모임을 만들고 목록에서 확인 가능 |
| P0 | **Feat-2. 친구 초대 + 수락/거절** | `session_participants`에 `invite_status` 추가(마이그레이션), 이메일 검색 초대 API, 수락/거절 API, 초대 UI | 두 계정으로 초대→수락 흐름 동작 |
| P0 | **Feat-3. 공통 공강 시간 계산** | `GET /meetups/{id}/available-times` — 수락한 참여자 전원의 일정 합집합→활동시간(09~22시) 여집합, 최소 60분, 점심/저녁 필터, 후보 리스트 화면 | 참여자 2명 이상에서 공강 후보가 정확히 계산됨 (수업 완료 기준과 동일) |
| P1 | **Feat-4. 시간 확정** | `POST /meetups/{id}/confirm-time`, 모임 상태 matching→time_fixed | 선택한 슬롯이 모임에 저장·표시됨 |

## 🗓 4주차 — 맛집 추천 + 마무리

| 우선순위 | Task | 내용 |
|---|---|---|
| P0 | **Feat-5. 맛집 추천** | 네이버 지역검색 + 카카오 로컬 병합, `GET /restaurants`, 추천 카드 화면 |
| P1 | **Feat-5b. AI 선별·추천 이유** | 후보 목록을 LLM에 넘겨 3~4곳 선별 + 한 줄 이유 (Gemini 무료 티어로 시작, 교체 가능한 구조) |
| P0 | **Feat-6. 장소 확정 + 확정 화면** | `POST /meetups/{id}/confirm-place`, 확정 요약 화면 |
| P1 | **UI 다듬기** | 디자인 시안(docs/디자인시안_밥약웹.html) 톤 적용 — 디자인 Skill 재사용 |
| P2 | **배포** | 여유 시 |

## 공통 규칙

- 커밋은 작게, 컨벤션: `feat / fix / refactor / docs`
- PR 제외: IDE 설정, 빌드 산출물, 대용량 이미지/PDF, `.env`(시크릿)
- **PR 전 보안 점검** 수행 (API 키 노출 여부 등)
