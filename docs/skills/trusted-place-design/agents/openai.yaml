interface:
  display_name: "Trusted Place Design"
  short_description: "네이버지도형 인증 리뷰 UI 디자인 규칙"
  default_prompt: "Use $trusted-place-design to design a Naver Map-like verified restaurant review screen."

policy:
  allow_implicit_invocation: true
