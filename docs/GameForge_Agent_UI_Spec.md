# GameForge Agent — UI/UX 설계 문서

> 기존 [GameForge Agent 기획서](./GameForge_Agent.md)의 "13. React Dashboard" 항목을 구체화한 문서.
> 실제 프로토타입(`gameforge-agent-prototype.html`)에서 검증된 화면 흐름과 인터랙션을 기준으로 작성했다.

---

## 1. 전체 화면 흐름

```
Repository 연결
      ↓ (저장소 확인 → Branch/프리셋 활성화)
분석 범위 선택 (빠름 / 기본 / 상세)
      ↓
분석 리포트 열람 (Markdown)
      ↓ Approve
Agent 워크스페이스 (9단계 순차 진행)
```

---

## 2. Repository 연결 화면

> **v2 변경**: URL 직접 입력 + PAT 방식에서 **GitHub OAuth 로그인** 방식으로 전환.

### 흐름
- 로그인 전: **"GitHub로 로그인"** 버튼만 활성화, 저장소/브랜치/프리셋 영역은 모두 잠김
- 로그인 성공: GitHub 아바타 + 계정명 표시, 로그인한 계정이 접근 가능한 **저장소 목록**이 드롭다운(검색 가능)으로 채워짐
- 저장소 선택: 이미 로그인 단계에서 접근 권한이 검증된 목록이라 별도의 "저장소 확인" 버튼이 필요 없음. 선택 즉시 해당 저장소의 **Branch 목록**을 조회해 Branch select를 채운다
- Branch 선택: 분석 범위 프리셋 섹션이 활성화된다
- **"연결 및 분석 시작"** 버튼은 저장소+Branch가 모두 선택된 이후에만 활성화

### 핵심 규칙: 단계적 잠금(Progressive Lock) 패턴
로그인 → 저장소 선택 → Branch 선택 순으로 다음 단계가 열리는 구조는 유지한다. 다만 잠금이 풀리는 트리거가 "확인 버튼 클릭"에서 "로그인 완료 / 항목 선택"으로 바뀐다.

| 단계 | 잠긴 상태 | 풀리는 조건 |
|---|---|---|
| 저장소 선택 | 로그인 전 비활성화 | 로그인 성공 |
| Branch 선택 | 저장소 미선택 시 비활성화 | 저장소 선택 |
| 분석 프리셋 | Branch 미선택 시 비활성화 | Branch 선택 |
| 연결 및 분석 시작 | 위 항목 미완료 시 비활성화 | 저장소+Branch 모두 선택 |

### 분석 범위 프리셋
개별 항목 체크박스 대신, 3단계 프리셋으로 묶어 선택 부담을 줄인다.

| 프리셋 | 설명 | 소요 시간 |
|---|---|---|
| 빠름 | 핵심 의존성 구조만 스캔 | 약 30초 |
| 기본 (권장) | 의존성 + 중복 코드 탐지 | 약 2분 |
| 상세 | Prefab 구조 + 리팩토링 대상까지 전수 분석 | 약 5분+ |

---

## 3. 분석 리포트 화면

분석이 끝나면 바로 Markdown 리포트(`00_Analysis_Report.md`)를 열람한다.

- 상단에 핵심 지표를 통계 카드로 요약: 클래스 의존성 수 / 중복 코드 블록 수 / 리팩토링 대상 수
- 하단에 리포트 원문을 코드 블록 형태로 미리보기
- **원문 수정** 가능 (아래 5번 항목의 편집 패턴과 동일)
- **Approve하고 Requirements Agent로** 버튼 → 9단계 워크플로우 시작

---

## 4. Agent 워크스페이스

### 4.1 레이아웃
좌측 사이드바(고정) + 우측 메인 패널의 2단 구성.

**좌측 사이드바**
- 연결된 Repository / Branch 정보
- 9단계 전체 리스트, 각 단계마다:
  - 단계 번호 (2자리, 모노스페이스)
  - 단계 이름
  - 진행률 %

**단계 상태 3종**
| 상태 | 표시 색상 | 클릭 가능 여부 |
|---|---|---|
| 완료 (done) | teal | 가능 — 완료된 문서를 읽기 전용으로 재열람 |
| 진행 중 (active) | amber | 가능 — 현재 작업 중인 화면으로 이동 |
| 대기 (pending) | 회색(mute) | **불가** — 이전 단계 미완료 시 잠김, 툴팁 안내 |

> 워크플로우 순서를 건너뛸 수 없다는 원 기획서의 원칙("각 단계는 사용자의 승인 후 다음 단계로 진행")을 UI 레벨에서 강제한다.

### 4.2 9단계 목록 (변경됨)

기존 계획에서 **"테스트" 단계를 제거**하고, 8번 단계명을 **"리팩토링 및 코드 리뷰"**로 확장했다. (v8에서 다시 **"코드 리뷰"**로 재정의 — 7단계 산출물만을 리뷰하는 단계로 범위가 좁혀졌다, 4.4a·4.6절 참고)

| # | 단계 |
|---|---|
| 1 | 요구사항 분석 |
| 2 | 게임 기획 |
| 3 | 게임 시스템 설계 |
| 4 | 클래스 설계 (UML) |
| 5 | 프로젝트 구조 설계 |
| 6 | ScriptableObject 설계 |
| 7 | 코드 생성 |
| 8 | ~~리팩토링~~ → 리팩토링 및 코드 리뷰 → **코드 리뷰** (v8) |
| 9 | ~~테스트~~ (제거됨) → **문서화** |

### 4.3 Agent와의 상호작용 — 채팅형 Q&A (변경됨)

> 이 절은 **1~6단계**에 적용된다. **7단계(코드 생성)와 9단계(문서화)는 채팅 없이 자동 생성되며 4.4a절을 따른다.**

**변경 전**: 질문마다 선택지 버튼(객관식)을 제공하는 Form 방식.
**변경 후**: 채팅 인터페이스로 자유 응답.

- Agent 질문과 사용자 답변이 말풍선으로 순서대로 쌓임 (Agent는 왼쪽, 답변은 오른쪽)
- 하단 텍스트 입력창 + 전송 버튼 (Enter 전송 지원)
- 상단에 `AGENT 이름 · 질문 N / 전체` 형태로 진행 상황 표시
- 답변 전송 시 "입력 중…" 상태를 짧게 보여준 뒤 다음 질문이 이어짐
- 준비된 질문을 모두 마치면 Agent가 "지금까지 내용으로 문서를 정리했다"는 안내와 함께 자연스럽게 아래 Markdown 미리보기·승인 단계로 연결
- **"지금까지 내용으로 문서 만들기"** 버튼을 채팅 입력창 근처에 항상 노출 — 사용자가 질문을 끝까지 기다리지 않고 그 시점까지의 대화만으로 문서 생성을 직접 트리거할 수 있다. 답변이 거의 없는 상태(예: 1개 미만)에서 누르면 "정말 지금 만들까요?" 확인을 한 번 거친다.

이 방식은 정해진 선택지에 없는 답변(예: 세부 수치, 조건부 답변)도 자유롭게 받을 수 있다는 장점이 있다.

### 4.4a 자동 생성 단계 — 7단계(코드 생성), 9단계(문서화)

> 채팅이 없다. Step에 진입하는 즉시 Agent가 결과 초안을 만들고, 사용자는 검토·수정만 한다.

- **7단계**: 진입 즉시 이전 단계 설계 문서 3개(클래스 설계·프로젝트 구조·ScriptableObject 설계) 기반으로 코드를 자동 생성. 이때 **기존 프로젝트의 네임스페이스 컨벤션을 자동 감지해 그대로 따르도록** 지시한다. **[v9] 설계 문서가 언급한 기존 클래스가 저장소에 실제로 있으면, 그 파일의 현재 내용을 함께 읽어와 컨텍스트로 제공** — 기존 코드를 이어서 확장하는 방식으로 생성. 생성 완료 후 결과는 4.5의 커밋 리뷰 화면으로 바로 연결된다 (별도 문서 미리보기 없음).
- **9단계**: 진입 즉시 1~8단계 전체 산출물을 종합해 **PR 설명처럼 읽기 좋은 최종 문서 초안**을 자동 생성. 사용자는 4.4의 Markdown 편집 UI(연필 아이콘 → 텍스트 영역 → 저장/취소)로 수정만 하며, 채팅으로 다시 대화해 고치는 경로는 없다.
- 두 단계 모두 로딩 상태("코드를 생성하는 중…" / "문서를 정리하는 중…")를 보여준다.
- 두 단계 모두 **Approve 시 별도 확인 절차 없이 즉시 실제 GitHub 커밋으로 이어진다.**

### 4.4 Markdown 문서 편집 및 승인

각 Agent 단계가 끝나면 해당 단계의 Markdown 문서를 미리보기로 보여준다.

- 기본은 읽기 전용 미리보기
- **"✎ 수정"** 클릭 → 원문을 직접 편집할 수 있는 텍스트 영역으로 전환 (편집 중임을 나타내는 강조 테두리)
- **저장** → 수정 내용을 반영하고 미리보기로 복귀 / **취소** → 변경 사항 폐기
- 다른 단계로 이동하면 저장하지 않은 편집 내용은 자동으로 폐기됨
- 최종적으로 **Approve** 버튼을 눌러야 다음 Agent로 진행 (원 기획서의 승인 프로세스와 동일). **1~6단계는 Approve 시 로컬 상태 전환 + 실제 `docs/` 커밋, 9단계는 Approve 시 즉시 실제 커밋까지 이어짐.**
- 이 편집 패턴은 문서 하나짜리 산출물(1~6단계, 9단계)에 공통 적용된다. 다만 1~6단계는 채팅(4.3)을 거쳐 문서가 만들어지고, 9단계는 채팅 없이 자동 생성(4.4a)된다는 차이가 있다. 코드가 여러 파일로 생성되는 7~8단계는 아래 4.5의 별도 화면을 거친다.

### 4.5 커밋 리뷰(Diff) 화면 — 코드 생성 / 리팩토링 단계 전용

**배경**: 7단계(코드 생성)와 8단계(코드 리뷰)는 한 번에 여러 파일이 새로 생기거나 수정된다. 4.4의 "문서 1개 Approve = 커밋 1개" 패턴을 그대로 쓰면 관련 없는 변경들이 한 커밋에 뭉쳐 리뷰와 되돌리기가 어려워진다. 그래서 이 두 단계는 **"Approve = 파일 단위로 쪼개진 여러 개의 원자적 커밋"**으로 처리한다.

> **[v8] 8단계는 7단계 산출물만 본다.** 8단계 Code Review Agent(구 Refactoring Agent)는 최초 저장소 분석 리포트가 아니라 **7단계가 방금 만든 코드**를 입력으로 받는다 — 프로젝트 전체를 리팩토링하는 게 아니라, 방금 생성된 코드에 대한 리뷰·피드백 단계다.

> **diff는 Agent가 만들지 않는다.** Agent는 파일의 새 전체 내용을 만들고, 화면에 보이는 Diff는 **서버가 (기존 파일 내용 vs 새 내용)을 직접 계산**한 것이다. LLM이 만든 diff 텍스트는 실제 파일과 어긋나 커밋이 깨질 위험이 있어 이 방식을 쓴다.

**화면 구성**
- Markdown 미리보기 자리 대신, **변경 파일 리스트**가 표시된다. 각 항목은:
  - 파일 경로 (모노스페이스)
  - 변경 유형 뱃지 — 신규(teal) / 수정(amber) / 삭제(red)
  - 개별 체크박스 (기본 전체 선택, **단 문법 오류가 있는 파일은 기본 체크 해제**)
  - Agent가 생성한 커밋 메시지 1줄 (예: `refactor: extract IMovable interface`) — 편집 가능
  - **[v6] 문법 오류 뱃지**: 커밋 직전 Roslyn Syntax 검증에서 오류가 발견된 파일은 파일 경로 옆에 red 경고 아이콘 + "문법 오류" 뱃지 표시, 클릭하면 에러 메시지 확인 가능. 강제로 체크해서 그대로 커밋하는 것은 막지 않는다(사용자 최종 판단 존중) — 단 기본값이 안전하게 꺼져 있을 뿐
- 파일 클릭 시 우측/하단에 **Diff 미리보기** 확장 (`git add -p` 느낌으로, 추가된 줄은 teal, 삭제된 줄은 red 배경 — 서버가 계산한 diff)
- 하단 액션: **선택 항목 커밋 (N개)** — Approve 버튼이 "파일 그룹 수만큼의 커밋을 순차 생성"하는 동작으로 바뀐다. 체크 해제한 파일은 이번 승인에서 제외되고 다음 라운드로 남는다.

**사이드바 진행률과의 관계**: 이 화면에서 전체 파일 중 일부만 Approve해도 되므로, 7~8단계의 진행률 %는 "승인 완료된 파일 수 / 전체 제안된 파일 수"로 계산한다 (다른 단계의 체크리스트 기반 계산과 동일한 원리).

---

## 5. 비주얼 디자인 시스템

Unity 에디터 감성의 다크 인스펙터 패널 톤을 전 화면에 일관되게 적용했다.

- **배경/패널**: 다크 그레이 계열 (`--bg-page`, `--panel`, `--panel-2`, `--panel-3`)
- **폰트**: 본문은 Inter, 단계 번호·경로·상태 등 기술적인 라벨은 JetBrains Mono
- **상태 색상**: teal(완료·성공) / amber(진행 중·주의) / red(위험·오류) — 의미별로 색을 고정하고 임의로 다른 용도에 재사용하지 않음
- 상세 컴포넌트 규칙(버튼, 카드, 뱃지, 코드 블록, 프리셋 선택기 등)은 별도 스킬 `gameforge-ui-style`로 문서화하여 이후 화면 제작 시 재사용

---

## 6. 변경 이력 (Changelog)

| 버전 | 변경 내용 |
|---|---|
| v0 | 기존 기획서 13번 항목(React Dashboard) 기반 초기 화면 설계 |
| v1 | Repository 연결에 "저장소 확인" 검증 게이트 추가 (Branch 목록 선(先)조회 문제 해결) |
| v1 | 16단계 중 "테스트" 삭제, 8번 "리팩토링 및 코드 리뷰"로 확장 |
| v1 | 사이드바 완료/진행중 단계 클릭 시 해당 단계 재열람 가능하도록 변경 |
| v1 | Markdown 문서에 원문 직접 편집(저장/취소) 기능 추가 |
| v1 | Agent 질문 방식을 객관식 선택지 → 자유 응답 채팅형으로 변경 |
| v1 | 7~8단계(코드 생성/리팩토링) 전용 **커밋 리뷰(Diff) 화면** 추가 — "Approve = 커밋 1개"에서 "Approve = 파일 단위 원자적 커밋 N개"로 모델 변경 |
| v2 | Repository 연결 방식을 **URL 직접 입력 + PAT**에서 **GitHub OAuth 로그인**으로 전환 — 로그인 후 저장소/Branch를 드롭다운으로 선택 |
| v4 | 커밋 리뷰 화면의 Diff를 Agent가 아니라 **서버가 직접 계산**하도록 변경 (LLM diff가 실제 파일과 어긋나는 위험 제거) |
| v5 | **7단계(코드 생성)·9단계(문서화)를 채팅 없는 자동 생성 방식으로 전환** (4.4a 신설) — 진입 즉시 결과 초안 생성, 사용자는 검토·수정만, Approve 시 즉시 실커밋. 1~6단계 채팅에는 "지금까지 내용으로 문서 만들기" 수동 트리거 버튼 추가 |
| v6 | 커밋 리뷰 화면에 **커밋 직전 Syntax 검증** 추가 — 문법 오류 있는 파일은 경고 뱃지 표시 + 기본 체크 해제 (8단계가 7단계 산출물을 자동 재검증하지 않는다는 갭을 최소 비용으로 보완) |
| v8 | **8단계를 "리팩토링 및 코드 리뷰"에서 "코드 리뷰"로 재정의** — 입력을 최초 저장소 분석 리포트에서 7단계 산출물 자체로 변경, v6이 임시로 메운 갭을 구조적으로 해소 |
| v9 | 7단계가 **저장소의 관련 기존 C# 스크립트를 읽어 컨텍스트에 포함**하도록 변경 — 설계 문서가 언급한 기존 클래스를 찾아 실제 내용을 함께 제공, 빈 상태가 아니라 기존 코드 위에서 이어서 생성 |
