# Claude 작업 보고 (append-only)

규칙: 이 파일은 읽지 말고 shell append(`cat >> docs/report/report_claude.md`)로 하단에만 추가한다. 기존 내용 수정·삭제 금지(유일한 예외: [review.md](review.md) 절차에 따라 점검자가 `- 확인:` 줄의 `[ ]`를 `[x]`로 바꾸는 한 줄). 다른 LLM은 `- 확인: [ ]` 미체크 항목부터 점검한다. 형식:

```text
## YYYY-MM-DD HH:MM | Task ID | 한 줄 요약
- 작업: 무엇을 했는지 2~3줄
- 검증: verify/테스트 결과, 커밋 해시
- 미결: BLOCKED·후속 작업·리뷰 요청 사항 (없으면 "없음")
- 확인: [x] 2026-07-15 16:18 GPT — 발견 없음(승인). S20 checksum 자기비교 결함은 `RetrievedEvidence.index_checksum` 전파와 `_to_citation_input()`의 stored/recomputed 분리로 해소됐고, API stored checksum 경계도 후속 보고에서 보강됨. `./scripts/verify.sh` 재현 통과.
```

---
## 2026-07-12 16:53 | T00 (harness.md H1~H7) | frontend 검증 하네스 구축
- 작업: docs/harness.md 지시서대로 H1~H7 구현. package.json에 lint/test/verify script 추가, ESLint flat config, Vitest+RTL+jsdom 테스트(추천 문구 부재 검사 2건, CLAUDE.md 절대 원칙 1과 연결), gitleaks secret scan, .github/workflows/ci.yml(frontend/secret-scan job + backend 주석 슬롯), auto-merge.yml에 CI 성공+리뷰 승인 게이트 추가·충돌 자동 close 제거·반복 코멘트 방지(commentOnce) 적용.
- 검증: npm run verify(lint+test+build) 전체 통과. H7 red→green 4건 모두 실측 확인 — lint(no-unused-vars 위반 주입 → 실패 → 원복 후 통과), test(금지 문구 "분할매수" 주입 → 2 테스트 실패 → 원복 후 통과), build(구문 오류 주입 → esbuild 실패 → 원복 후 통과), secret-scan(gitleaks를 별도 fixture repo에서 fake key로 탐지 확인 → 실제 저장소는 "no leaks found"). auto-merge.yml/ci.yml은 YAML 파싱(js-yaml)과 내장 JS 구문(node --check) 검증 통과, 단 GitHub Actions 런타임(GraphQL 응답 기반 로직)은 실제 PR로 미검증.
- 미결: checklist.md C0의 4개 항목은 frontend+backend+type check가 한 문장에 묶여 있어 backend(T01 이후)가 생기기 전까지는 정직하게 체크 불가 — 그대로 미체크 유지. backlog.md T00은 schema/envelope/Claim·Fact·Evidence schema/문서 정책 등 하네스 밖 항목이 남아 "진행중" 유지(변경 없음). gitleaks는 로컬에 brew로 신규 설치됨(사용자 확인 필요). esbuild/vite 관련 moderate dev-server 취약점(npm audit)은 하네스 범위 밖으로 미조치 — 필요시 별도 검토 권장. GPT 리뷰 요청: auto-merge.yml의 predicate 순서·commentOnce 로직, ProjectIntro.test.jsx의 금지 문구 목록 충분성.
- 확인: [x] 2026-07-12 23:02 GPT

## 2026-07-12 17:26 | T00 (checklist.md C0) | 잔여 계약·저장소 품질 게이트 완료, backend 의존 3개 항목 BLOCKED
- 작업: 이전 세션의 H1~H7 하네스(lint·test·build·secret scan·CI·auto-merge)에 이어 C0의 나머지 항목을 구현. `contracts/`(envelope.js·verdict.js·schemas.js + 각 test.js)로 Envelope 공통 계약, 5상태 Verdict 그룹 집계 규칙(`groupVerdict`), StructuredClaim/FinancialFact/Evidence/NumericEvidence 필드 계약과 Evidence의 `claim_id`/`presentation_item_id` 배타 규칙을 실행 가능한 contract test로 구현(24개 테스트). `docs/skills.md`에 "스키마 버전·migration 정책" 절 추가(additive/breaking 변경 규칙, immutable snapshot 보존, DB migration은 T01 범위임을 명시). `package.json`에 `npm run audit`(`npm audit --omit=dev --audit-level=high`) 추가하고 `verify`에 편입, `ci.yml`의 frontend job 이름 갱신, `.github/dependabot.yml` 추가. 감사 중 `.gitignore`의 `docs/*` 규칙이 `docs/report/`의 신규 파일까지 가려버리는 결함을 발견해 `!docs/report/` 예외 추가로 수정. checklist.md C0 9개 항목 중 6개 체크, backlog.md T00에 진행 상황·BLOCKED 사유 기록. CLAUDE.md/AGENTS.md의 "현재 구현 상태"에 `contracts/` 반영.
- 검증: `npm run verify`(lint→test→build→audit) 전체 통과, 26 tests passed. verdict 그룹 집계 규칙에 대해 red→green 실측(UNVERIFIABLE 분기를 SUPPORTED로 오염 → 테스트 실패 확인 → 원복 후 통과). dependency audit 게이트에 대해 red→green 실측(lodash@4.17.4를 prod dependency로 임시 설치 → critical 취약점으로 `npm run audit` 실패(exit 1) 확인 → uninstall로 원복 → package-lock.json 완전 복구, package.json diff는 의도한 script 추가만 남음 확인). `git check-ignore`로 `docs/report/`의 신규 파일이 더 이상 무시되지 않음을 확인.
- 미결: checklist.md C0의 3개 항목("repository-level build·test·lint·type·security workflow", "frontend/backend lint·backend type check·secret·dependency security scan", "CI의 build·test·lint·type·security 차단 gate")은 문장이 frontend+backend(특히 backend type check)를 함께 요구하는데 T01(Backend·DB 기반)이 아직 착수되지 않아 backend lint·type check(ruff·mypy) 대상 자체가 없음 — **BLOCKED, 해제 조건: T01 backend package scaffold 완료 후 `ci.yml`의 주석 처리된 backend job 활성화**. backlog.md T00은 이 3개 항목 때문에 `진행중`으로 유지(완료 정책상 하위 체크박스 전부 통과 전에는 상위 Task를 완료로 바꾸지 않음). 그 외 모든 문서(.gitignore·AGENTS.md·CLAUDE.md·docs/backlog.md·docs/checklist.md·docs/skills.md·.github/workflows/ci.yml·package.json) 및 신규 파일(contracts/*, .github/dependabot.yml)은 아직 uncommitted 상태 — prerequisites.md의 "문서 커밋" 항목대로 사용자 확인 후 커밋 필요(이 세션은 커밋을 수행하지 않음). GPT 리뷰 요청: contracts/의 필드 계약이 skills.md와 정확히 일치하는지, migration 정책 문구가 T01 Pydantic 설계와 충돌하지 않을지.
- 확인: [x] 2026-07-12 23:02 GPT

## 2026-07-12 17:54 | T01 (checklist.md C13 [T01] 3항목) | Backend·DB 기반 구축 완료
- 작업: 지시된 완료 조건이 checklist.md C1(종목 해석, 실제로는 T02 소관)로 잘못 지정돼 있어, backlog.md의 T01 공식 종료 조건인 C13의 `[T01]` 태그 3항목으로 정정하고 진행(사용자에게 고지함). `backend/`를 uv(Python 3.12)로 스캐폴드: FastAPI + Pydantic v2 + SQLAlchemy 2.0 + Alembic + psycopg + PyJWT + passlib[argon2]. `app/config.py`(pydantic-settings, 루트 `.env` 스키마), `app/schemas/envelope.py`(`contracts/envelope.js`와 동일 계약의 Python 미러, `model_post_init`으로 reason_code 필수 규칙 강제), `app/middleware.py`(request_id/trace_id 발급·에코백), `app/exception_handlers.py`(RequestValidationError/EmailAlreadyRegisteredError/NotAuthenticatedError/미처리 예외를 전부 Envelope로 매핑), `app/providers/base.py`(T02~T14가 구현할 provider 추상 인터페이스 + Timeout/RateLimited/Auth/Maintenance/NotFound → status·reason_code 결정론적 매핑, "점검"과 "데이터 없음"을 구분), `app/models/user.py`+Alembic 초기 migration(`c4f6e182a5f8_create_users_table`), `UserRepository`(중복 이메일 차단), `app/security/`(argon2 해시, JWT 발급/검증), `/api/v1/auth/register·login·me` FastAPI 라우터. 로컬 Postgres는 `docker-compose.yml`(postgres:18, host port 5442, 기존 다른 프로젝트의 5432/5433 컨테이너와 충돌 없음)로 실행. `scripts/verify.sh`를 신설해 harness.md H1 설계대로 frontend+backend 단일 진입점으로 승격(npm verify → backend ruff/mypy/alembic upgrade/pytest), `.github/workflows/ci.yml`의 주석 처리돼 있던 backend job을 postgres service container로 실제 구현. 이 결과로 T00에서 BLOCKED로 남겨뒀던 checklist.md C0의 3개 항목("...type... workflow", "backend lint·type check...", "CI의 ...type... 차단 gate")이 해제돼 C0 9개 전항목 체크, backlog.md T00/T01 모두 상태를 `완료`로 갱신. CLAUDE.md/AGENTS.md의 현재 구현 상태·실행 명령, docs/prerequisites.md(PostgreSQL 준비 완료로 체크, `.env`에 `DATABASE_URL`·`JWT_SECRET_KEY` 미기입 상태 명시)도 갱신. `.gitignore`에 backend venv/`__pycache__`/mypy·ruff·pytest cache 제외 규칙 추가.
- 검증: `./scripts/verify.sh` 전체 통과(frontend 26 tests + backend 31 tests, ruff·mypy·alembic upgrade 전부 clean). backend 3개 게이트 모두 red→green 실측 — ruff(미사용 import 주입 → 탐지 → 원복), mypy(함수 시그니처 타입 오염 → 6개 오류 탐지 → 원복), pytest(비밀번호 검증 함수를 `return True`로 조작 → 2개 테스트 실패로 실제 탐지 → 원복). auth API e2e 8개 시나리오(가입 성공/중복 409/짧은 비밀번호 422/로그인 성공/오답 401/미가입 401/토큰 없이 me 401/유효 토큰 me 200) 전부 통과. YAML 3종(`ci.yml`, `auto-merge.yml`, `dependabot.yml`, `docker-compose.yml`) 파싱 검증. `git add -n backend/`로 venv/cache가 스테이징 대상에서 실제로 빠지는지 확인.
- 미결: 실제 `.env`에는 `DATABASE_URL`·`JWT_SECRET_KEY`가 아직 없음(agent가 실제 `.env`를 읽거나 쓰지 않았으므로 사용자가 `.env.example` 참고해 직접 추가 필요) — 로컬에서 backend를 띄우려면 이 작업이 선행돼야 함. `ci.yml`의 `backend` job(postgres service container, `astral-sh/setup-uv@v4`)은 YAML 구문 검증만 했고 실제 GitHub Actions 런타임에서는 미검증. `uv sync --locked`가 CI에서 재현되는지도 실제 PR 전까지 미확정. 모든 변경(backend/, contracts/, scripts/, docker-compose.yml, .env.example, 문서 전체)은 여전히 uncommitted 상태 — docs/prerequisites.md의 "문서 커밋" 항목대로 사용자 확인 후 커밋 필요. GPT 리뷰 요청: JWT 인증 흐름(비밀번호 해시 알고리즘 argon2 선택, 토큰 만료 60분 기본값, `/me` 401 매핑)이 T11의 "인증·인가·tenant filter" 확장과 충돌 없이 재사용 가능한 형태인지, `app/providers/base.py`의 에러 계층이 T02 OpenDART provider 구현 시 그대로 들어맞는지.
- 확인: [x] 2026-07-12 23:02 GPT

## 2026-07-12 23:32 | T00·T01 후속 (report_gpt.md 2026-07-12 23:02 3건 반영) | GPT 리뷰 피드백 전건 반영
- 작업: review.md B절 절차로 미체크 3건을 점검·전건 타당 판정 후 반영. (1) auto-merge: 판정 로직을 `scripts/auto-merge-rules.js` 순수 모듈로 추출 — main 대상 PR을 첫 규칙이 전부 스킵해 병합 경로가 도달 불가였던 결함을 "main이 아닌 base를 스킵"으로 반전, `reviews(last:1)` 대신 `reviewDecision`으로 승인 판정, 연기 코멘트는 `<!-- auto-merge:상태 -->` marker를 조회 범위(comments last:100) 전체에서 검색해 상태별 1회 보장, rule 단위 테스트 10건 추가(`scripts/auto-merge-rules.test.js`), workflow는 checkout 후 모듈 import만 수행. (2) 계약 강화: `contracts/envelope.js`에 `source_ids` 필수화·비문자열 `as_of` 거부·id/timestamp 문자열 타입 검사, `contracts/schemas.js`를 typed spec(타입·enum·YYYY-MM-DD pattern·중첩 comparator·array item 타입)으로 재작성하고 RawSourceRecord/NumericEvidence/Citation 포함 6개 타입 전부 정상·실패 테스트 구비, Python 미러 `app/schemas/envelope.py`도 `source_ids` 필수화(생성 지점 3곳에 명시적 `[]`). dependency gate는 vite 5→7 업그레이드로 esbuild/vite 취약점 해소 후 `npm audit --audit-level=high`(dev 포함)로 확대, backend `uv.lock` 전체 pip-audit 게이트를 `scripts/verify.sh`·`ci.yml`에 추가, Dependabot을 npm+uv+github-actions로 확장. (3) T01: `ProviderRateLimitedError`/`ProviderAuthError`의 status override를 제거해 `EXTERNAL_ERROR`+reason_code로 정정(잘못된 매핑을 고정하던 테스트도 의미 있는 테스트로 교체), `UserRepository.create`를 commit 시 `IntegrityError` → rollback → `EmailAlreadyRegisteredError`(CONFLICT)로 변환하고 사전조회 우회 경쟁 재현 테스트 추가, 구조화 JSON 요청/오류 로그(`app/observability.py`, request_id/trace_id/reason_code 포함)와 Prometheus 요청 metrics(`/metrics`, route template label) 추가. checklist.md C0·C13, backlog.md T01에 반영 내역 기록. 반박 1건: report_gpt.md 3번째 항목의 "JS와 동일 계약의 Python 미러 주장이 성립하지 않는다"는 서술은 부정확 — 당시 JS 미러도 `source_ids`를 필수로 요구하지 않아 JS·Python은 서로 일치했고, 둘 다 skills.md와 불일치한 것이 정확한 상태였다(결함 실재는 동의, 동일 원인으로 양쪽 모두 수정).
- 검증: `./scripts/verify.sh` 전체 통과 — frontend lint·55 tests·build·`npm audit`(0 취약점), backend ruff·mypy strict·alembic·36 tests·pip-audit(0 취약점). red→green 실측 2건: auto-merge predicate에 기존 결함(`baseRefName === 'main'` 스킵)을 재주입 → rule 테스트 10건 중 7건 실패 확인 → 원복 후 통과; `ProviderRateLimitedError`에 `status = Status.RATE_LIMITED`를 재주입 → 매핑 테스트 실패 확인 → 원복 후 통과. commit은 아직 수행하지 않음(전체 변경 uncommitted).
- 미결: `ci.yml` backend job의 pip-audit 단계와 auto-merge workflow의 checkout+dynamic import는 GitHub Actions 런타임에서 미검증(실제 PR 필요, 기존 미결과 동일). `/metrics`는 현재 비인증 노출 — T13(배포·운영)에서 접근 제어 필요. vite 7 major 업그레이드는 lint·test·build 통과로 검증했으나 dev server 수동 확인은 안 함. report_gpt.md 3건은 반영 완료로 체크함.
- 확인: [x] 2026-07-12 23:37 GPT

## 2026-07-12 23:35 | T00·T01 후속 (report_gpt.md 23:12 정정·보강 항목) | 추가 항목 점검 — 신규 작업 없음
- 작업: 직전 보고(23:32) 작성 중 report_gpt.md에 23:12 정정·보강 항목이 추가돼 있음을 확인하고 점검. (a) `source_ids` 관련 정정은 직전 보고의 반박과 동일한 결론(당시 JS·Python 미러는 서로 일치, 둘 다 skills.md와 불일치 — 양쪽 모두 수정 완료), (b) 추가 발견인 `reviews(last: 1)` 승인 오판 결함은 직전 반영에서 `reviewDecision` 전환으로 이미 해소, (c) 권고 반영 순서 (1)~(5)는 직전 보고의 작업으로 전부 이행됨. 필요한 신규 작업 없음.
- 검증: 직전 보고와 동일(`./scripts/verify.sh` 전체 통과, backend 36 tests·frontend 55 tests·audit 2종 0건).
- 미결: 없음.
- 확인: [x] 2026-07-12 23:37 GPT

## 2026-07-12 23:48 | T00·T01 후속 2차 (report_gpt.md 2026-07-12 23:37 2건 반영) | 재리뷰 발견 5건 전건 반영
- 작업: review.md B절 절차로 23:37 등록된 미체크 2건(사실상 동일 내용의 재확인 포함 5개 발견)을 점검·전건 재현 후 반영. (1) `contracts/envelope.js`가 Python Pydantic 미러와 검증 강도가 달랐던 문제: `as_of`가 자릿수 패턴만 맞으면(예: "2026-99-99", "2026-02-30") 통과하던 것을 실제 달력 유효성 검사로 교체, `warnings[]`·`source_ids[]` 원소가 문자열인지 검사 추가, `started_at`/`completed_at`을 파싱 가능한 timestamp인지 검사(문자열이기만 하면 통과하던 것 교정). Python 쪽(pydantic date/datetime/list[str])은 원래도 이 입력들을 거부했으므로 JS만 따라잡음. (2) `contracts/schemas.js`에 앞서 추가한 타입 제약(number·boolean·comparator 중첩 구조 등)이 `docs/skills.md`에 문서화되지 않아 "문서가 타입의 진실 소스"(skills.md 자체 migration 정책 문구) 원칙을 코드가 역전시키고 있던 문제: skills.md의 Structured Claim/Financial Fact/Raw Source Record/Evidence/Numeric Evidence 5개 블록에 `필드:타입` 표기(선택 `?`, 배열 `[]`, enum 괄호 표기, comparator 중첩 object)를 schemas.js SPEC과 1:1로 맞춰 추가. (3) Vite 7.3.6이 요구하는 Node `^20.19.0 || >=22.12.0`을 로컬 Node 22.9.0이 만족하지 못해 `npm install`이 EBADENGINE 경고를 내는 문제: `package.json`에 `engines` 필드(advisory, engine-strict는 걸지 않아 로컬 개발을 막지 않음), `.nvmrc`(22.12.0) 추가. (4) auto-merge의 1차 수정(marker를 최근 100개 댓글에서 검색)도 "PR이 오래 열려 있으면 marker가 창 밖으로 밀릴 수 있다"는 근본적 한계가 남아 있다는 재지적: 댓글 스캔 방식을 버리고 `auto-merge:상태` label 기반 상태 추적으로 교체(`scripts/auto-merge-rules.js`의 `hasStateLabel`/`staleStateLabels`) — label은 PR당 유일해 댓글 pagination과 무관하게 정확하다. `addLabels`는 저장소에 없는 label을 못 붙이므로 4개 상태 label을 워크플로에서 멱등하게 사전 생성(422 무시)하는 단계를 추가해, label 부재로 인한 재발을 막음. rule 테스트를 14건으로 갱신(`scripts/auto-merge-rules.test.js`). (5) `UserRepository.create`가 이메일 unique index 위반이 아닌 다른 `IntegrityError`도 전부 `EmailAlreadyRegisteredError`로 변환하던 문제: psycopg `exc.orig.diag.constraint_name`이 `ix_users_email`(alembic migration에서 확인한 실제 index 이름)일 때만 CONFLICT로 변환하고 그 외는 그대로 propagate하도록 수정, 오분류 방지 테스트 추가(가짜 orig/diag로 다른 constraint 위반 재현). checklist.md C0·C13 관련 3개 항목에 반영 내역 기록.
- 검증: `./scripts/verify.sh` 전체 통과 — frontend lint·62 tests·build·`npm audit`(0건), backend ruff·mypy strict·alembic·37 tests·pip-audit(0건). red→green 실측 2건: `isCalendarDate`의 날짜 구조분해를 `[2026,99,99]` 상수로 깨뜨려 envelope 테스트 16개 중 2개 실패 확인 → 원복 후 16개 통과; `hasStateLabel`을 항상 `false` 반환하도록 깨뜨려 auto-merge rule 테스트 13개 중 1개 실패 확인 → 원복 후 14개 통과.
- 미결: auto-merge label 생성·부착을 포함한 GitHub Actions 런타임 동작은 여전히 실제 PR로 미검증(YAML·인라인 JS 구문 검증만 수행). 로컬 Node(22.9.0)는 vite 7 요구 버전 미만이라 `nvm install`로 22.12.0 이상 수동 전환 필요(`.nvmrc` 추가함, 강제하지는 않음 — 사용자 준비물). 전체 변경 uncommitted.
- 확인: [x] 2026-07-12 23:51 GPT

## 2026-07-13 10:20 | T00·T01 후속 3차 (report_gpt.md 2026-07-12 23:51 1건 반영) | 재리뷰 발견 4건 전건 반영
- 작업: T02 착수 전 review.md B절 절차로 report_gpt.md의 미체크 마지막 항목(23:51)을 점검·전건 재현 후 반영. (1) auto-merge A→B→A 반복 결함: `staleStateLabels`(상태 전환 시 이전 label 삭제) 자체가 원인이었으므로 함수를 완전히 제거하고 label을 누적만 하는 모델로 교체 — `hasStateLabel`은 한 번 부여된 상태 label을 그 PR이 살아있는 동안 계속 인식한다. `.github/workflows/auto-merge.yml`에서 `removeLabel` 호출 제거, `labels(first: 20)` → `labels(first: 100)`로 확대. `scripts/auto-merge-rules.test.js`에 A(not-approved)→B(ci-failing)→A(not-approved) 왕복 시나리오 회귀 테스트 추가(13건, 이전 14건에서 stale-label 테스트 제거 후 순증). (2) timestamp cross-runtime 불일치: `docs/skills.md`에 `started_at`/`completed_at`이 RFC3339 + offset(`Z`/`±HH:MM`) 필수라는 계약을 명시하고, `contracts/fixtures/timestamps.json`(valid 4개·invalid 9개, "01/02/2026"·naive datetime·date-only·존재하지 않는 달력 값 포함)을 신설해 `contracts/envelope.js`(정규식+숫자 범위 검사로 `Date.parse`의 관대함 제거)와 `backend/app/schemas/envelope.py`(`field_validator`로 naive datetime 거부)가 동일 fixture로 동일하게 판정하는지 양쪽 테스트에서 검증. (3) Node 버전 gate가 advisory였던 문제: `semver`를 devDependency로 추가하고 `scripts/check-node-version.js` + `npm run preverify`(engines.node 미충족 시 process.exit(1))를 신설해 `npm run verify`(따라서 `scripts/verify.sh`와 CI 양쪽)가 자동으로 시작 전에 실패하도록 만듦. 이 과정에서 로컬 환경이 실제로 Node 22.9.0(미지원)이었음을 확인 — nvm으로 이미 설치돼 있던 22.17.0으로 전환해 검증(사용자의 전역 `nvm alias default`는 변경하지 않음, 세션 내 사용만). 부수적으로 `contracts/envelope.test.js`의 JSON import attribute(`with { type: 'json' }`) 구문과 `scripts/check-node-version.js`의 Node 전역(`process`)을 `eslint.config.js`가 인식하지 못해 lint가 깨졌던 것을 `ecmaVersion: 'latest'`와 `scripts/**/*.js` node-globals override로 수정. (4) schemas.js 버전 판단 미기록 문제: `docs/skills.md`에 "migration 기록" 표를 신설해 이번 타입 강화와 timestamp 강화를 "pre-release 보정, 버전 유지(1.0.0)"로 명시적으로 기록(저장된 실제 데이터·소비자가 아직 없다는 근거 포함).
- 검증: `npm run verify`(Node 22.17.0, preverify 포함) 통과 75 tests, `./scripts/verify.sh` 전체 통과(frontend 75 + backend 50, npm·pip audit 0건). red→green 실측 3건: (a) `hasStateLabel`을 `false &&` 로 강제 조작 → A→B→A 회귀 테스트 3건 실패 확인 → 원복 후 13건 통과, 별도로 구식 label-삭제 시나리오를 수동 재현해 `hasStateLabel`이 `false`를 반환함을(=재코멘트 발생) 확인하고 신규 누적 모델에서는 `true`를 반환함을(=억제) 대조 확인. (b) Node 22.9.0(실제 로컬 버전)에서 `node scripts/check-node-version.js` 실행 → exit 1로 명확히 실패 확인(진짜 미지원 환경에서의 자연 발생 red) → 22.17.0로 전환 후 통과. envelope timestamp 검증은 JS 29 tests·Python 18 tests 각각 fixture 전체(valid 4·invalid 9)에 대해 통과.
- 미결: `.nvmrc`/`engines`가 있어도 사용자가 `nvm use`를 수동으로 하지 않으면 로컬 shell 기본 Node는 여전히 22.9.0이다 — `preverify`가 이제 그 상태에서 명확히 실패시키므로 사용자는 `nvm install && nvm use`(또는 `nvm alias default 22.17.0`)가 필요하다(에이전트가 전역 기본값을 대신 바꾸지 않음, 사용자 결정 사항). auto-merge의 label 기반 상태 추적은 여전히 실제 GitHub Actions 런타임에서 미검증(YAML·순수 모듈 테스트만). GPT 리뷰 요청: 없음(이번 라운드 4건 전건 반영 완료, 이어서 T02 진행).
- 확인: [x] 2026-07-13 11:48 GPT

## 2026-07-13 10:59 | T02 (checklist.md C1·C2) | 종목 해석(S1)·OpenDART 공시 수집(S2) 구현 완료
- 작업: T01 완료 이후 report_gpt.md 미체크 항목 반영(직전 항목 참고)을 마친 뒤 T02 착수. 실제 `DART_API_KEY`로 OpenDART API를 호출해 삼성전자·NAVER·SK하이닉스·동명 4건("덕성", 상장 1+비상장 3)·실제 정정공시 쌍(원본 1건+`[기재정정]` 2건)·공시 원문·에러 응답(013/100/010, 나머지는 공식 문서 기반)을 `backend/tests/fixtures/opendart/`에 record/replay fixture로 저장(40KB). S1: `app/models/company.py`(`Company` 현재 projection + `RawCorpMasterBatch` immutable 수집 이력), `app/services/company_resolver.py`(`corpCode.xml` 파싱·upsert, stock_code 정확매치→이름 정확매치→fuzzy 순 해석, 동명 후보는 상장 여부와 무관하게 항상 candidates로 반환해 자동 확정하지 않음, market 필터는 KRX 분류 provider 미정이라 `UNSUPPORTED_MARKET_FILTER`로 명시적 미지원). S2: `app/providers/opendart.py`(공시검색·재무제표·문서·corp master 4개 endpoint, DART status 코드→`ProviderError` 계층 매핑 — JSON endpoint와 파일 endpoint(zip 성공/XML 에러)의 서로 다른 응답 형태를 모두 처리, tenacity 기반 네트워크 재시도 3회), `app/models/disclosure.py`(`RawDisclosureRecord` immutable + `Disclosure`/`FinancialFactRow`/`DocumentChunk`/`CorrectionChain`/`ProviderCacheEntry`), `app/services/disclosure_collector.py`(COLLECT/NORMALIZE, `filed_at <= as_of` 미래 데이터 차단을 S15 이전 임시 규칙으로 구현, `[…정정]` bracket 제거 후 동일 corp_code·보고서명·이전 접수일 매칭으로 correction chain 연결, DART 고유 XML 원문을 평문 flatten 후 고정크기 chunking, 요청 파라미터 해시 기반 TTL 캐시). `POST /api/v1/companies/resolve·ingest`, `POST /api/v1/disclosures`(operation COLLECT|NORMALIZE discriminated union) FastAPI 엔드포인트 추가, `app/dependencies.py`에 `get_opendart_provider` 의존성을 신설해 라우터가 provider를 직접 생성하지 않고 주입받게 해 테스트에서 실제 네트워크 없이 fixture-backed fake로 교체 가능하게 함. Alembic migration(`e8d741dd3a9c`)으로 6개 신규 테이블 추가. checklist.md C1·C2 전항목 체크(한계 2건 명시: 상장폐지/비상장 구분 불가, correction 원본 미매칭 시 임의 연결 안 함), backlog.md T02 완료, CLAUDE.md/AGENTS.md 현재 구현 상태 갱신, plan.md R01·R02를 IMPLEMENTED로 갱신.
- 검증: `./scripts/verify.sh`(Node 22.17.0) 전체 통과 — frontend 75 tests, backend 109 tests(T01의 50개 + T02 신규 59개), ruff·mypy strict·npm audit·pip-audit 전부 clean. red→green 실측 2건: (1) OpenDartProvider의 tenacity retry 조건이 원본 httpx 예외가 아니라 변환된 `ProviderTimeoutError`를 봐야 하는데 반대로 설정돼 있어 재시도가 전혀 발생하지 않던 실제 버그를 테스트 작성 중 발견·수정(재시도 대상 예외 타입을 `ProviderTimeoutError`로 정정) — 이후 timeout 2회 후 성공/3회 연속 실패 테스트로 실제 재시도 동작 확인. (2) `filed_at > as_of` 미래 데이터 차단 조건을 `if False`로 조작 → 관련 테스트 실패 확인 → 원복 후 통과. 전체 109개 백엔드 테스트는 실제 캡처된 OpenDART 응답을 재생하는 record/replay 방식이라 provider 응답 형태가 실제와 다르면 그대로 실패한다.
- 미결: T04(S15, Temporal Integrity)가 아직 없어 NORMALIZE의 정합성 규칙은 미래 데이터 차단만 구현한 임시 상태 — 정정 chain 최신본 선택·잠정/확정 우선순위·CFS/OFS 기간 선택 정책은 S15 도입 시 이 스킬이 S15를 호출하도록 교체되어야 한다(문서화됨, checklist에도 명시). market(KOSPI/KOSDAQ) 분류는 T03에서 provider가 정해지기 전까지 계속 `UNSUPPORTED_MARKET_FILTER`로 남는다. `/api/v1/companies/ingest`·`/api/v1/disclosures`는 인증만 요구하고 role 기반 권한은 없음(T11에서 세분화 예정, T01 결정과 동일선상). 전체 변경 uncommitted. GPT 리뷰 요청: correction chain 매칭 휴리스틱(이름 문자열 완전 일치 요구)이 실제 정정에서 보고서명이 미세하게 달라지는 경우(예: 괄호 공백 차이)에 얼마나 취약한지, `app/services/disclosure_collector.py`의 문서 chunking 단순화(표 구조 미보존)가 향후 S18 검색 품질에 심각한 제약이 될지.
- 확인: [x] 2026-07-13 11:48 GPT

## 2026-07-13 12:00 | T02 후속 (report_gpt.md 2026-07-13 10:59 T02 3건 반영 + 11:48 승인 확인) | 발견 3건 전건 반영
- 작업: review.md B절 절차로 미체크 2건을 점검. (a) 11:48 "T00·T01 후속 3차" 항목은 판정이 "승인"이고 발견·계약 위반이 "없음"이라 코드 변경 대상이 없음 — 권고 중 "auto-merge label 생성·부착은 실제 PR에서 확인"은 이 세션에서 실행 불가능한 항목(실제 PR 필요)이라 미결로 남기고, "기본 shell Node를 nvm 기본 alias로 맞추라"는 사용자 환경 설정 권고라 코드 변경 대상이 아니어서 그대로 확인만 함. (b) 11:48 "T02 (checklist.md C1·C2)" 항목의 발견 3건 전부 재현 후 반영. **높음** `backend/app/exception_handlers.py`에 `ProviderError` 전용 handler가 없어 OpenDartProvider가 던지는 `ProviderNotFoundError`/`ProviderRateLimitedError` 등이 실제 `/api/v1/disclosures` 응답에서는 generic `Exception` handler로 흘러 `500`/`INTERNAL_ERROR`/`UNHANDLED_EXCEPTION`이 되는 것을 확인 — `map_provider_error()` 결과를 Status→HTTP 매핑(`EXTERNAL_ERROR→502, RATE_LIMITED→429, NOT_FOUND→404`)으로 옮기는 handler를 추가하고 API 레벨 테스트 2건(013 no-data→404/NOT_FOUND, 020 rate-limited→502/EXTERNAL_ERROR) 추가. **중간** `backend/app/services/disclosure_collector.py`의 캐시 hit 경로가 `json.dumps(payload, sort_keys=True)`(ensure_ascii 기본값 True)로 checksum을 재계산해 원본 fetch(`opendart.py`의 `ensure_ascii=False` 직렬화)와 한글 payload에서 다른 SHA를 냄을 확인 — `opendart.py`의 private `_stable_json_bytes`를 공용 `stable_json_bytes()`로 승격해 원본·캐시 hit 양쪽이 동일 함수를 쓰도록 통일, 캐시 hit checksum이 원본과 일치함을 확인하는 회귀 테스트 추가. **중간** `backend/app/routers/disclosures.py`의 `_normalize`가 `eligible_raw_record_ids` 중 DB에 없는 id를 조용히 걸러내던 것을, missing id를 모아 Envelope `warnings[]`에 담아 노출하도록 수정(status/HTTP는 그대로 SUCCESS/200 유지 — GPT의 "최소 warning/trace" 권고 채택). 기존 테스트 `test_normalize_with_unknown_raw_record_id_is_safely_empty_not_an_error`를 `..._is_empty_with_a_warning_not_silent`로 교체해 warning 노출을 검증. `docs/checklist.md` C2의 관련 3개 항목에 반영 내역 기록.
- 검증: `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과(frontend·backend, npm/pip audit 0건). backend `pytest -q` 112개 통과(신규 4건: checksum 회귀, provider no-data/rate-limit API, normalize warning). red→green 실측 2건: (1) `stable_json_bytes`의 `ensure_ascii=False`를 제거하면 원본·캐시 두 경로 모두 같이 틀려 위장 통과하므로, 캐시 hit 경로만 원래 버그 패턴(`json.dumps(payload, sort_keys=True)`, 공용 함수 미사용)으로 되돌려 checksum 불일치 재현 후 원복해 실제 결함 패턴을 정확히 검증. (2) `ProviderError` handler를 임시로 제거해 `500`/`UNHANDLED_EXCEPTION` 재현(로그로 실측) 후 원복.
- 미결: auto-merge label 생성·부착은 여전히 실제 GitHub Actions 런타임(실제 PR)에서만 검증 가능 — 이 세션에서 처리 불가. 로컬 기본 Node가 22.9.0(미지원)이라 `nvm use 22.17.0`을 명시적으로 거쳐야 `preverify`를 통과함(사용자 nvm default 설정은 권고로 남기고 강제하지 않음). `NormalizePayload`에 missing id 목록을 payload 필드로도 노출할지(현재는 Envelope `warnings[]`만)는 별도 계약 변경 없이는 보류.
- 확인: [x] 2026-07-13 13:11 GPT

## 2026-07-13 | fix_instructions.md F8·F9 (+F7 마일스톤, 확장 후보 1줄) | plan.md 기획 보강·Claim 확인 UX 결정 반영
- 작업: instructions.md 절차대로 report_gpt.md 미체크 1건(2026-07-13 13:11, T02 후속, 판정 "승인"·발견 없음)을 먼저 review.md B절로 처리 — 권고 2건 모두 즉시 코드 변경 대상이 아니라고 판단(NormalizePayload missing id 필드는 별도 계약 변경 시 판단, auto-merge 실제 PR 검증은 F4로 이미 추적 중)해 반박 없이 확인만 체크. 이후 F8·F9를 진행. **F8**: plan.md에 별도 PRD 없이 5개 절 보강 — (1) 문제 정의 아래 "타깃 사용자·핵심 시나리오"(페르소나 1개 + A/B/C 대응 시나리오 3개), (2) 같은 위치에 "기존 대안과 차별점"(증권사 리포트/MTS/커뮤니티/범용 챗봇 vs 본 제품을 출처 추적·기준시점·결정론 검산·반증 제시 4축 비교표), (3) 전체 완료 정의 앞에 "제품 성공 지표"(인용 열람 ≥1, 체크리스트 완료율, 재방문 검증 비율 — T10 데이터로 측정 가능, 신규 인프라 불요), (4) 핵심 기능 D 절에 존재 이유 2문장(검증-행동 단절을 의도적으로 유지하는 것 자체가 안전 원칙 시연, T14 최후순위 재확인), (5) 핵심 기능 B 절에 가치 문장("얼마에 사라"가 아니라 "가격이 정당화되려면 어떤 가정이 참이어야 하는가"). 추천·행동 지시 표현 0건으로 작성(절대 원칙 1 확인). **F7(같은 변경에 포함, 사용자가 날짜 확정)**: "릴리스 마일스톤" 절 추가 — 챌린지 마감·데모가 모두 2026-07-20~2026-07-26(다음주)로 확정됐음을 반영하고, M1(T04+T05)~M4(T12~T15) 표에 완료조건·시연 시나리오·"다음주 내 도달 가능성"을 명시. 남은 REQUIRED 폭이 현재 완료(T00~T02)의 4~5배라 R01~R15 전체를 다음주 안에 끝내는 것은 불가능함을 명시하고, R01~R15 REQUIRED·BLOCKED 원칙은 삭제하지 않는다는 문장으로 범위 원칙과의 충돌을 방지. **즉시 리스크로 별도 강조**: M1조차 선행조건인 T03이 prerequisites.md의 사용자 결정(시세 provider·뉴스 allowlist, F6) 대기로 착수 불가 상태 — 마감이 임박한 만큼 이 결정이 최우선임을 문서에 명시. 사용자 요청대로 R 레지스트리 아래에 "확장 후보(REQUIRED 아님)" 한 줄도 추가: "T03·T04 완료 후 확장 후보 — 차트 지표 결정론 검산(이동평균·거래량 추세 등, 신호 해석 금지)·종목 필터"(REQUIRED로 자동 승격되지 않음을 명시). **F9**: "모호할 때만 묻는다"(progressive disclosure) 모델을 계약에 반영 — (a) plan.md 사용자 흐름 6단계를 "모호한 항목이 있을 때만 확인 질문에 답한다(모호하지 않은 Claim은 요약 카드로 자동 진행)"로 수정, (b) skills.md S7 제약에 `ambiguity_flags`가 무편집 자동 진행/객관식 확인 질문 분기의 판정 근거임을 명시하고 "질문에 답하지 않으면 UNVERIFIABLE로 처리"를 명문화, (c) skills.md S11 처리 항목에 "S7 ambiguity_flags 기반 Claim 요약 카드(무편집 경로)와 모호 항목 확인 질문 UI 구성"을 추가, (d) checklist.md C7의 "Claim 확인·수정 UI" 항목을 "ambiguity_flags 기반 확인 질문 UI(비어있으면 자동 진행, 있으면 객관식 확인)"로 동기화. S7의 "모호성을 임의 확정하지 않는다" 기존 제약과 모순 없음.
- 검증: 코드 변경 없는 순수 문서 작업(plan.md·skills.md·checklist.md·report_gpt.md)이라 `./scripts/verify.sh`는 실행하지 않음 — instructions.md 종료 절차의 "실행 명령·구현 상태가 바뀌었으면" 조건에 해당하지 않음(현재 실행 명령·구현 상태 불변). 대신 plan.md 전체를 다시 읽어 절 순서·mermaid·번호 목록이 깨지지 않았는지, 추가 문구에 매수·매도·목표가·관망 등 금지 표현이 없는지(절대 원칙 1) 육안 확인. F9 변경 3개 파일(plan.md/skills.md/checklist.md)이 "ambiguity_flags" 용어로 정확히 교차 참조되는지 grep으로 대조.
- 미결: F7의 마일스톤 날짜는 "다음주"라는 사용자 발화를 2026-07-20~2026-07-26으로 해석한 것이며 정확한 마감 요일은 여전히 미정 — 확정되는 대로 plan.md 갱신 필요. F6(T03 provider 결정)이 여전히 미해결이라 M1도 착수 불가 상태이며, fix_instructions.md 지시 순서상 F1(커밋)·F2(skills.md 구현 상태 stale 수정)·F3(GPT 리뷰 사이클)·F4(실제 PR CI 검증)·F5(Python 버전 고정)·F6(provider 결정)가 이번 세션에서 다루지 않은 채 남아 있음 — 다음주 마감을 고려하면 F1(커밋, 유실 방지)과 F6(provider 결정)이 가장 급하다. BLOCKED 항목 없음(F8·F9 모두 순수 문서 작업이라 차단 없이 완료).
- 확인: [x] 2026-07-13 15:14 GPT

## 2026-07-13 15:40 | report_gpt.md 2026-07-13 15:14·15:24 (F8·F9 문서 보강 리뷰) | GPT 리뷰 피드백 4건 전건 반영
- 작업: review.md B절 절차로 report_gpt.md 미체크 2건(15:14 수정요청, 15:24 정정·보강)을 오래된 순으로 반영. 4건 모두 타당하다고 판단해 반박 없이 수정. (1) `plan.md` M1 마일스톤: 완료조건(T04+T05)이 backlog상 UI를 포함하지 않는데 시연 문구가 "화면에 뜬다"였던 불일치 — 시연을 "API 응답(JSON, Swagger/curl)"로 낮추고, 표 아래에 "M1은 UI를 포함하지 않으며 화면 데모가 필요하면 별도 범위 변경이 있어야 한다"는 문장을 추가. M3 행에도 "첫 화면 UI 데모는 이 시점"을 덧붙여 어느 마일스톤에서 처음 화면이 나오는지 명확히 함. (2) 제품 성공 지표의 "인용 열람 횟수" 측정 근거 부재 — citation open 이벤트 계측이 아직 어떤 checklist 항목에도 없었음을 인정하고, `checklist.md` C11에 "citation open 이벤트 저장(사용자·claim·evidence·연 시각)" 항목을 신설한 뒤 plan.md 지표 옆에 "현재는 미구현, 계측 전까지 측정 불가"라는 캐비엇을 명시(지표를 등록만 해두고 구현 없이 방치하지 않기 위함). (3) plan.md:50의 "얼마에 사라"가 부정문이라도 행동지시형 문구를 포함해 향후 금지 문구 grep에서 오탐을 만들 수 있다는 지적 — "특정 매수 가격이나 행동 지시"로 중립화. (4) plan.md:84 확장 후보(차트 지표·종목 필터)가 추천/랭킹으로 오인될 수 있다는 지적을, 15:24 정정 리뷰가 "기능 삭제가 아니라 표현 경계 문제"로 재확인하며 제시한 정확한 문구("차트 기반 주장이 실제 시세 데이터와 일치하는지 확인, 신호 해석·매매 판단 금지" / "사용자가 지정한 조건에 맞는 검증 후보 좁히기, 추천·순위화 금지")를 그대로 반영 — 기능 자체는 유지.
- 검증: 코드 변경 없는 문서 수정(plan.md·checklist.md)이라 `./scripts/verify.sh` 미실행(instructions.md 종료 절차의 "실행 명령·구현 상태 변경" 조건에 해당하지 않음). plan.md를 다시 읽어 M1/M3 시연 문구가 backlog.md의 실제 Task 범위(T04·T05·T08·T11)와 일치하는지, "매수·매도·목표가·관망" 등 금지 표현이 새로 남아있지 않은지 grep+육안 대조로 확인.
- 미결: checklist.md C11의 citation open 이벤트 항목은 문서 등록만 됐고 아직 미구현 상태(T10 착수 전) — T10 진행 시 실제 구현·테스트가 필요하다. F1(전체 변경 커밋)이 여전히 안 된 상태로 이번 문서 수정까지 더해져 uncommitted diff가 계속 누적되고 있다 — 다음주 마감을 고려하면 커밋이 시급하다.
- 확인: [x] 2026-07-13 15:41 GPT

## 2026-07-13 | T03 (checklist.md C3 [R03][S13·S14]) | 착수 전 사용자 결정 미충족으로 BLOCKED
- 작업: docs/instructions.md 작업 시작 절차를 따라 report_gpt.md 미체크 리뷰(2026-07-13 15:41, 판정 승인·발견 없음)부터 확인 — 권고 2건(F1 커밋, T10 citation open 이벤트)은 모두 T03이 아닌 향후 Task(T10) 범위라 이번 세션에서 추가 조치 없이 확인 처리. 이어서 backlog.md T03 선행조건(T02=완료) 확인, checklist.md C3·skills.md S13·S14 계약, plan.md R03을 로드. 착수 전 prerequisites.md T03 절을 확인한 결과 시세 provider(공공데이터포털/KRX/증권사 API 중 택1)와 뉴스·외부 근거 provider allowlist가 모두 미확정(`[ ]`)임을 발견. `.env`의 `MARKET_API_KEY`·`NEWS_API_KEY` 값 존재 여부만 확인(값 자체는 읽지 않음, CLAUDE.md 절대 원칙 8) — 둘 다 빈 값으로 실제 미입력 확인. `docs/plan.md:175`와 `docs/fix_instructions.md` F6이 이 사용자 결정을 T03의 크리티컬 패스 차단 요인으로 이미 명시하고 있어, 구현으로 우회할 수 없는 진짜 BLOCKED로 판단하고 코드 작성 없이 중단.
- 검증: 코드 변경 없음(구현 착수 전 BLOCKED 확정). `./scripts/verify.sh` 재실행 불필요(변경 없음).
- 미결: **BLOCKED** — 해제 조건은 backlog.md T03 항목에 기록: 사용자가 (1) 시세 provider, (2) 뉴스·외부 근거 allowlist를 결정하고 API key를 `.env`에 채운 뒤 prerequisites.md T03 체크박스를 갱신해야 재개 가능. 대안으로 fix_instructions.md F6이 안내하는 T05(I9 평가 기반, 선행조건 T00·T01만 필요) 병렬 착수가 가능함을 사용자에게 안내함.
- 확인: [x] 2026-07-14 11:51 GPT

## 2026-07-13 (후속) | T03 (checklist.md C3 [R03][S13·S14]) | provider 결정 반영, 자격증명 발급 대기로 BLOCKED 유지
- 작업: 앞선 BLOCKED 보고 직후 사용자가 세션 중 provider를 결정(시세: 한국투자증권 KIS Developers, 뉴스·외부 근거: 공식 출처+네이버 뉴스 검색 API). GitHub `koreainvestment/open-trading-api` 공식 문서와 네이버 개발자센터 공식 문서를 WebSearch/WebFetch로 조사해 인증 방식(앱키+앱시크릿→접근토큰, 분당 1회 발급 제한, 실전/모의 환경 분리)과 뉴스 API 스펙(`X-Naver-Client-Id/Secret` 헤더, 응답 필드)을 확인. "계약 변경은 문서 먼저" 원칙에 따라 `docs/skills.md` S13·S14에 provider 이름·인증·핵심 필드 반영, `docs/prerequisites.md` T03 절 체크박스 갱신(선택 완료/가입 대기 구분), `.env`·`.env.example`·`backend/app/config.py`의 `MARKET_API_KEY`/`NEWS_API_KEY`를 `KIS_APP_KEY`/`KIS_APP_SECRET`/`KIS_ENV`/`NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET`로 정밀화(같은 변경에서 문서·설정 동기화). `docs/backlog.md` T03 항목을 갱신해 진행 상황·잔여 차단 사유를 기록.
- 검증: `./scripts/verify.sh`(nvm 22.17.0) 전체 통과 — frontend 75 tests, backend 112 tests(ruff·mypy clean), npm audit·pip-audit 0건. config.py 필드명 변경이 기존 테스트에 영향 없음을 확인(사전에 `market_api_key`/`news_api_key` 참조처가 코드베이스에 없음을 grep으로 확인).
- 미결: **BLOCKED 유지** — 실제 COLLECT provider client 구현과 T02 방식의 record/replay fixture 캡처에는 살아있는 자격증명이 필요하나, KIS Developers 앱키·앱시크릿은 한국투자증권 계좌 개설이, 네이버 Client ID/Secret은 개발자센터 애플리케이션 등록이 선행돼야 하며 둘 다 사용자 개인 명의 조치라 agent가 대신할 수 없다. 해제 조건은 backlog.md T03 항목 참고. **사고 보고**: 이 세션에서 `.env`를 Read 도구로 직접 열람해 `DART_API_KEY`·`UPSTAGE_API_KEY` 실제 값이 대화 컨텍스트에 노출됨(원래는 존재 여부만 셸로 확인했어야 함). 사용자에게 즉시 고지하고 두 key의 폐기·재발급을 권고함 — 아직 사용자 조치 확인 전.
- 확인: [x] 2026-07-14 11:51 GPT

## 2026-07-14 11:25 | T03 (checklist.md C3 [T03]) | 시세·기업행위·외부 근거 수집 — 12개 항목 중 11개 완료, 1개 BLOCKED(범위 축소)
- 작업: 이전 세션이 KIS/네이버 자격증명 발급 대기로 BLOCKED 남긴 T03을 재개. `.env`에 KIS·네이버 4종 key가 채워져 있음을 확인하던 중 `KIS_ENV`가 `vps`/`prod`가 아닌 36자리 값(APP_KEY 오기입 추정)으로 잘못 들어 있는 것을 발견 — 임의로 넘겨짚지 않고 사용자에게 확인해 모의투자(vps)로 확정 후 `.env`를 고쳤고, `backend/app/config.py`의 `kis_env`를 `Literal["vps","prod"]`로 강화해 같은 오류가 재발하면 startup에서 즉시 실패하도록 만들었다. S13(시세·기업행위)은 한국투자증권 KIS Developers Open API(`app/providers/kis.py`: OAuth 토큰 발급+in-process 캐시, 현재가·기간별시세·예탁원 4종 기업행위 endpoint), S14(외부 근거)는 네이버 뉴스 검색 API(`app/providers/naver_news.py`)로 구현. `app/models/market.py`(RawMarketRecord/Quote/SharesOutstanding/CorporateAction), `app/models/external_evidence.py`(RawExternalRecord/ExternalDocument, `ExternalSourceProvider` 3종 allowlist), `app/schemas/structured_claim.py`(S7 착수 전 StructuredClaim typed 계약 선도입), `app/services/market_collector.py`·`app/services/external_evidence_collector.py`(COLLECT/NORMALIZE + 수동 입력 adapter + `promote_official_numeric_evidence`), `POST /api/v1/market`·`POST /api/v1/external-evidence` discriminated union 라우터, Alembic migration(`9afeb16760a3`). 모의투자 계정으로 실제 KIS API(현재가·기간별시세 조정/비조정·삼성전자 2018년 실제 50:1 액면분할·배당·무상증자·유상증자)와 네이버 뉴스 검색 API를 라이브 호출해 `backend/tests/fixtures/kis/`·`backend/tests/fixtures/naver/`에 캡처(T02와 동일한 record/replay 원칙, 토큰 값은 재발급 전용 redacted placeholder로 치환). `docs/checklist.md` C3 12개 항목 중 11개 체크, `docs/backlog.md`·`docs/prerequisites.md`·`CLAUDE.md` 동기화.
- 실 라이브 호출로만 발견한 provider 특이사항 3건(문서화, 임의 추정 아님): (1) KIS는 존재하지 않는 종목코드에도 `rt_cd="0"`(성공)을 반환하고 output을 전부 0/공백으로 채운다 — 이 shape을 직접 감지해 `ProviderNotFoundError`로 변환. (2) 국내휴장일조회 TR(`CTCA0903R`)은 모의투자 환경에서 `EGW02006`으로 거부돼 실전투자 전용임을 확인 — 거래 캘린더는 기간별시세 응답의 실제 거래일 집합에서 파생. (3) 접근토큰 발급 rate limit(분당 1회, `EGW00133`)과 일반 조회 rate limit(초당, `EGW00201`)은 서로 다른 오류이며 별도 reason_code(`ProviderTokenRateLimitedError` 신설)로 구분해야 함을 실제로 재현해 확인.
- 검증: `./scripts/verify.sh` 전체 통과 — frontend 75 tests, backend 164 tests(신규 52개: provider 정상/실패/rate-limit/재시도, NORMALIZE 정상·PRE_NORMALIZE 임시 규칙·수동 입력, API 계약, 산문 수치 미승격·커뮤니티 소문 미승격·allowlist), ruff·mypy strict·gitleaks(CI job 기준)·npm/pip audit 전부 clean. 테스트가 실제 버그 1건을 잡음 — `_normalize_period_price`의 `"unadjusted".endswith("adjusted")`가 항상 True라 조정/비조정 구분이 깨지던 것을 발견해 정확한 segment 비교로 수정.
- 미결: `docs/checklist.md` C3의 "공식 provider의 구조화 수급·계약 수치를 provenance 포함 NumericEvidence로 변환" 1개 항목만 **BLOCKED** — 공공데이터포털(data.go.kr)·KRX 공식 구조화 provider는 자격증명이 없어(변환 로직 자체는 구현·synthetic fixture unit test 완료, live 검증 불가) agent가 대신 발급받을 수 없다. 해제 조건은 `docs/prerequisites.md` T03 절에 신규 등록. 이 1개 항목 때문에 `docs/backlog.md` T03은 `완료`가 아니라 `BLOCKED(좁혀짐)`으로 유지, `docs/plan.md` R03도 `미구현` 유지(정책상 하위 체크박스 전부 통과 전 상위 상태를 올리지 않음). **부수 사건**: secret scan 설정을 점검하려고 `gitleaks detect --source . --no-git`을 직접 실행했는데 이 모드는 git 이력이 아니라 작업 디렉터리 파일을 그대로 스캔해 `.env`의 `DART_API_KEY`·`KIS_APP_KEY`·`KIS_APP_SECRET`·`UPSTAGE_API_KEY` 실제 값이 도구 출력(대화 컨텍스트)에 노출됐다(`NAVER_*`는 이 출력에 없었음). 사용자에게 즉시 고지하고 4개 key 재발급을 권고했다 — **아직 재발급 확인 전**. 실제 CI의 secret scan(`gitleaks/gitleaks-action`, git 이력 스캔)은 이 문제와 무관하며 정상 구성돼 있다.
- 확인: [x] 2026-07-14 11:51 GPT

## 2026-07-14 13:16 | T03 후속 (report_gpt.md 2026-07-14 11:51 3건 반영) | secret scan 로컬 gate 편입, 노출 key 재발급 여부 확정
- 작업: review.md B절 절차로 report_gpt.md 미체크 3건(모두 2026-07-14 11:51 등록, 오래된 순)을 점검·반영. (1) "T03 착수 전 사용자 결정 미충족 BLOCKED"(승인·발견 없음): 코드 변경 대상 없어 확인만 처리. (2) "T03 provider 결정 반영·자격증명 대기 BLOCKED"(승인, 권고: 이전 사고로 노출된 `DART_API_KEY`·`UPSTAGE_API_KEY`·`KIS_APP_KEY`·`KIS_APP_SECRET` 재발급 여부 확인): agent가 `.env`를 직접 열람하거나 사용자를 대신해 key를 재발급할 수 없는 사항이라 사용자에게 직접 질의 — 사용자가 "무료 key라서 상관없다"는 판단으로 재발급하지 않기로 확정함을 확인, 반박 없이 수용하고 사용자 결정으로 이 미결을 종결. (3) "T03 시세·기업행위 수집"(수정요청, 발견: `scripts/verify.sh`가 gitleaks를 호출하지 않는데 직전 보고가 "`./scripts/verify.sh` 전체 통과 ... gitleaks(CI job 기준) ... clean"이라고 로컬 검증 범위와 CI 범위를 한 문장에 묶어 서술): 타당하다고 판단해 권고 중 "verify.sh에 안전한 git-history 기반 secret scan 포함" 방식을 채택(문서 분리 방식은 채택하지 않음) — `scripts/verify.sh`에 `[3/3] secret scan` 단계로 `gitleaks detect --source . --redact --no-banner`를 추가했다. `--no-git`은 의도적으로 쓰지 않는다는 주석을 남겼다 — 이 플래그는 작업 디렉터리 파일을 그대로 스캔해 이전 사고(`.env` 값 노출)를 재현하는 반면, 기본(git-history) 모드는 `git log`만 읽어 `.gitignore` 대상인 untracked `.env`가 스캔 대상에 아예 들어가지 않는다. 이제 CI(`gitleaks/gitleaks-action`)와 로컬(`scripts/verify.sh`) 양쪽이 동일한 git-history 기반 secret scan을 실행하므로, 이후 보고에서 "CI job 기준"과 "로컬 실행"을 분리해 적어야 했던 필요 자체가 사라졌다.
- 검증: `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 75 tests·build·`npm audit`(0건), backend ruff·mypy strict·alembic·164 tests·pip-audit(0건), 신규 `[3/3]` gitleaks 단계는 "22 commits scanned ... no leaks found"로 clean. `.env`가 untracked(`git status`에 미표시)·`.gitignore` 대상임을 재확인해, git-history 모드가 구조적으로 이를 스캔 대상에서 제외함을 근거와 함께 확인(`--no-git`은 실행하지 않음, 앞으로도 verify.sh에서 사용하지 않는다).
- 미결: 노출된 4개 key는 사용자 판단(무료/저위험)에 따라 재발급하지 않기로 확정 — 후속 조치 불필요, 종결. report_gpt.md 3건은 반영 완료로 체크함.
- 확인: [x] 2026-07-14 13:20 GPT

## 2026-07-14 15:20 | T04 (checklist.md C4 [T04]) | Temporal Integrity·재무 계산 — 15개 항목 전부 완료
- 작업: T03이 `BLOCKED(좁혀짐)`(C3 1항목 미해결)로 남아 있었지만, 그 BLOCKED 사유(공공데이터포털/KRX 자격증명 없음, S14 일부)는 T04(S15·S3)가 실제로 쓰는 T03 산출물(S13 전체, S14 raw record 모델)과 무관하다고 판단해 착수(`docs/backlog.md`에 판단 근거 기록, GPT 리뷰로 반박 가능하게 남김). S15 Temporal Integrity Layer를 `app/services/temporal_integrity.py`에 신설 — `pre_normalize`(미래 데이터 차단, `TemporalCandidate` 공통 타입으로 S2/S13/S14 어느 payload든 처리), `resolve_provisional_confirmed`(잠정·확정 충돌 표시), `post_derived`(파생 Fact/Evidence의 provenance·기업·기간·단위·기준시점 재검증). S2(`disclosure_collector.py`)·S13(`market_collector.py`)의 T02·T03 시절 inline `filed_at <= as_of`/`trade_date <= as_of` 임시 규칙을 이 중앙 서비스 호출로 교체(기존 T02·T03 테스트 전부 회귀 없이 통과). S3 Financial Calculator를 `app/services/financial_calculator.py`+`app/models/financial_fact.py`에 신설 — 계정 매핑(`ACCOUNT_METRIC_MAP`, 매출/영업이익/순이익/자산/부채/자본/현금흐름 3종/배당), 단위 정규화(원/천원/백만원 + 0.15/15% 혼동 방지 `parse_ratio`/`UnitConfusionError`), CFS 우선·OFS fallback·혼합 금지(`select_fs_div`/`assert_single_fs_div`, corp_code·fiscal_period·fs_div·unit 4개 모두 검사), 누적→단일분기 변환(`derive_single_period_value`/`derive_single_quarter_fact`), PER/PBR/ROE/부채비율/마진/배당성향 공식+버전(`FORMULA_REGISTRY`), 분모 0·음수·흑자전환/적자지속 처리(`classify_sign_transition`). `POST /api/v1/temporal-integrity`(S15 discriminated union)·`POST /api/v1/financial-facts/calculate`(S3) 라우터, Alembic migration 5건.
- 실 라이브 검증: 삼성전자 DART 재무제표(연간 2023·2024 CFS+OFS, 2025 1분기·반기·3분기 CFS)를 실제 API로 캡처해(`backend/tests/fixtures/opendart/financial_calculator/`) 계정 매핑·CFS/OFS 선택·지표 계산을 전부 실수치로 검증했다. 라이브 데이터로만 발견한 사실(임의 추정 아님, 실제 숫자로 재현): (1) 분기·반기 IS/CIS 계정은 `thstrm_amount`가 이미 단일기간 실제값이고 `thstrm_add_amount`가 누적값(Q1+Q2+Q3 단일값 합계가 3분기 `thstrm_add_amount`와 정확히 일치함을 확인), annual은 `thstrm_add_amount`가 항상 빈 문자열. (2) CF(현금흐름표)는 중간기간 보고서에서 `thstrm_add_amount` 필드 자체가 없고 처음부터 누적값만 제공 — 실제 영업활동현금흐름 수치로 Q2·Q3 단일분기 값을 직접 도출해 검증(도출값 합계가 3분기 누적과 정확히 일치).
- 개발 중 발견·수정한 결함 3건: (1) T02의 `FinancialFactRow` unique constraint가 `account_detail`을 포함하지 않아 자본변동표(SCE)의 구성요소별 행이 두 번째부터 "이미 존재"로 오인돼 조용히 유실되던 결함을 실제 데이터로 재현·수정(`FinancialFactRow`·`FinancialFact` 양쪽 constraint에 `account_detail` 추가). (2) S2가 DART의 `thstrm_add_amount` 필드를 아예 저장하지 않아 S3가 단일분기/누적을 구분할 방법이 없었던 것을 발견해 컬럼 추가로 해결. (3) 같은 `metric_key`(예: NET_INCOME)가 IS·CIS·CF·SCE 여러 sj_div에 나타나고 SCE는 0값 구성요소 행을 포함해, 단순 "metric_key로 dict 구성" 방식이 실제 순이익을 0으로 덮어쓰는 버그를 재현 — `select_canonical_facts`+`METRIC_CANONICAL_SJ_DIV`(지표별 신뢰할 단일 sj_div 고정)로 해결.
- 검증: `./scripts/verify.sh` 전체 통과 — frontend 75 tests, backend 217 tests(신규 53개), ruff·mypy strict·gitleaks(git-history 모드)·npm/pip audit 전부 clean.
- 미결: 없음(BLOCKED 항목 없이 C4 15개 전부 체크). `docs/plan.md` R04를 `미구현` → `IMPLEMENTED`로 갱신, `docs/backlog.md` T04를 `완료`로 갱신. **한계 2건 문서화**: (1) 잠정·확정 실적 구분 규칙은 구현·테스트했지만 OpenDART 재무제표 API가 항상 확정치만 줘서 실제 잠정 데이터로는 검증하지 못했다. (2) 기업행위 보정은 S13의 조정주가·실시간 발행주식수 제공에 의존하며 S3가 재무제표 시점과 시세 시점 사이 액면분할 등을 자동으로 소급 조정하지는 않는다(호출자 책임, `price_as_of`/`shares_outstanding_as_of` 미제공 시 해당 값은 아예 미사용 처리). T03에서 미해결로 남긴 secret 노출(4개 key, 사용자가 "무료라 상관없다"로 재발급 보류 확정, report_gpt.md/report_claude.md 2026-07-14 13:16 항목 참고)은 이번 세션과 무관하게 그대로 유지.
- 확인: [x] 2026-07-14 15:25 GPT — 수정요청 4건 등록(report_gpt.md 2026-07-14 15:25): T04 API가 최신 filed_at만 남겨 다기간/CFS·OFS fallback 계약을 실제 라우터에서 충족하지 못함, POST_DERIVED 입력 타입에 기간 필드가 없어 기간 사후 검증이 불가능함, 미래 price_as_of가 EPS/BPS까지 과도하게 거부하게 함, README component-tree 링크 경로가 실제 `docs/etc/component-tree.md`와 불일치함. `./scripts/verify.sh`는 통과 확인.

## 2026-07-14 15:50 | T04 후속 (report_gpt.md 2026-07-14 15:25 4건 반영) | 다기간 계산·기간 사후 검증·EPS/BPS 기준일 분리
- 작업: review.md B절 절차로 미체크 1건(2026-07-14 15:25, 수정요청 4건)을 오래된 순 없이 한 번에 반영 — 4건 모두 타당하다고 판단, 반박 없음. **(1, 높음)** `backend/app/routers/financial_facts.py`의 `calculate()`가 `mapped_facts`에서 `filed_at` 최신 기간만 남기고 나머지 기간 facts를 계산에서 조용히 버리던 구조를 재작성 — `fiscal_period`별로 facts를 그룹화(`facts_by_period`)한 뒤 `select_fs_div()`를 호출해 기간별 CFS 우선·OFS fallback 경고를 실제로 보존하고, `sorted(selected_by_period)` 순서로 모든 기간을 순회하며 `select_canonical_facts`→`assert_single_fs_div`→`compute_ratios`→`post_derived`를 기간마다 독립 수행한다. 응답의 `numeric_evidence`(기존 `target_period` 필드)와 `formulas`(신규 `fiscal_period` 필드를 `FormulaPayload`에 추가)가 이제 여러 기간에 걸쳐 채워진다. **(2, 중간)** `DerivedCheckInput`(`app/services/temporal_integrity.py`)과 `PostDerivedCheckInput`(`app/schemas/temporal_integrity.py`)에 `target_period: str`·`source_periods: list[str]` 필드를 추가하고, `post_derived()`에 기업·단위 불일치와 같은 방식으로 `source_periods`가 `target_period`와 다르면 거부하는 검사를 추가(`app/routers/temporal_integrity.py`의 `_to_derived_check`, `financial_facts.py` 양쪽에서 실제로 채워 넣음). **(3, 중간)** `financial_facts.py`의 `source_as_of` 구성을 지표별로 분리 — 이전에는 EPS/BPS/PER/PBR 4개 지표 모두에 `price_as_of`+`shares_outstanding_as_of`를 함께 넣어 가격을 쓰지 않는 EPS/BPS까지 `price_as_of`가 미래면 거부됐다. `_SHARES_ONLY_METRICS`(EPS/BPS, 발행주식 수 기준일만)와 `_PRICE_AND_SHARES_METRICS`(PER/PBR, 발행주식 수+가격 기준일)로 나눠 metric별 `source_as_of`를 만들도록 수정. **(4, 낮음)** `README.md:13`의 `[docs/component-tree.md](docs/component-tree.md)`를 실제 파일 위치인 `[docs/etc/component-tree.md](docs/etc/component-tree.md)`로 수정. `docs/checklist.md` C4의 관련 3개 항목(CFS/OFS 기간별 fallback, 시세·발행주식 수 기준일 일치, POST_DERIVED 기간 검증)에 반영 내역 기록.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` 모두 clean(86 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 75 tests·build·`npm audit`(0건), backend 219 tests(신규 2건: `test_post_derived_rejects_mixed_fiscal_period`, `test_calculate_computes_ratios_for_every_period_not_only_the_latest`; 기존 `test_calculate_rejects_future_price_as_of_via_post_derived`에 EPS/BPS 생존 assertion 추가), ruff·mypy strict·gitleaks(git-history, 26 commits)·npm/pip audit 전부 clean. red→green 실측 2건: (a) `post_derived()`의 기간 불일치 검사를 `mismatched_period = []`로 강제 비활성화 → `test_post_derived_rejects_mixed_fiscal_period` 실패 확인(`rejected == []` vs 기대 `["ev-mixed-period"]`) → 원복 후 통과. (b) `financial_facts.py`의 기간 loop를 "facts 중 filed_at이 가장 늦은 기간 하나만" 처리하도록 임시로 되돌림(원래 버그 재현) → `test_calculate_computes_ratios_for_every_period_not_only_the_latest`가 정확히 원래 버그 증상(`2023-ANNUAL`이 `numeric_evidence`에서 빠짐)으로 실패 → 원복 후 통과.
- 미결: 없음. report_gpt.md 2026-07-14 15:25 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-14 15:56 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-14 15:56): `POST_DERIVED`가 `source_ids`만 있으면 `source_corp_codes`/`source_units`/`source_as_of`/`source_periods`가 비어 있어도 verified로 통과해, 원천별 기업·기간·단위·기준시점 사후 검증 계약을 아직 완전히 강제하지 못함. `./scripts/verify.sh`는 통과 확인.

## 2026-07-14 16:20 | T04 후속 (report_gpt.md 2026-07-14 15:56 1건 반영) | POST_DERIVED 원천 메타데이터 개수 검증
- 작업: review.md B절 절차로 미체크 1건(2026-07-14 15:56)을 반영. 타당하다고 판단해 수정하되, 권고 문구("`source_ids`와 나머지 4개 배열의 길이가 모두 같고 1 이상")를 문자 그대로 적용하기 전에 먼저 재현했다 — `financial_facts.py`가 PER/PBR/EPS/BPS의 `source_as_of`에 `fact_as_of`(길이 = `source_ids`와 동일) 외에도 자체 `source_id`가 없는 `shares_outstanding_as_of`/`price_as_of`를 추가로 얹으므로, `source_as_of`가 `source_ids`보다 긴 것은 버그가 아니라 기존에 의도된 정상 동작이다("모든 배열 길이 동일" 규칙을 그대로 적용하면 이 정상 케이스가 회귀로 거부됨을 아래 검증에서 실측). 그래서 규칙을 두 갈래로 나눴다: `source_corp_codes`/`source_units`/`source_periods`는 `source_ids`와 정확히 같은 길이(이 셋은 실제로 `best_by_metric.values()`에서 1:1로 만들어짐), `source_as_of`는 `source_ids` 이상(추가 시장 데이터 기준일 허용). `app/services/temporal_integrity.py`의 `post_derived()`에 이 검사를 추가(원천 배열 개수가 안 맞으면 다른 문제들과 동일하게 `problems`에 쌓아 거부). `app/schemas/temporal_integrity.py`의 `PostDerivedCheckInput`에 5개 리스트 필드 전부 `Field(min_length=1)`을 추가하고 `model_validator(mode="after")`로 같은 비대칭 규칙을 API 스키마 단계에서도 강제해 malformed 요청이 422로 더 일찍 걸리도록 함.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` 모두 clean(86 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 75 tests·build·`npm audit`(0건), backend 222 tests(신규 3건: `test_post_derived_rejects_source_metadata_shorter_than_source_ids`(리뷰가 지적한 정확한 재현 시나리오), `test_post_derived_allows_source_as_of_longer_than_source_ids`(회귀 방지 — 비대칭 규칙이 PER/PBR 정상 케이스를 깨지 않는지), `test_schema_rejects_post_derived_check_with_mismatched_source_metadata_lengths`(API 레벨 422)), ruff·mypy strict·gitleaks(git-history, 26 commits)·npm/pip audit 전부 clean. red→green 실측: `post_derived()`의 신규 길이 검사를 `mismatched_lengths: list[str] = []`로 강제 비활성화 → `test_post_derived_rejects_source_metadata_shorter_than_source_ids` 실패 확인(`rejected == []` vs 기대 `["ev-missing-source-metadata"]`), 같은 상태에서 `test_post_derived_allows_source_as_of_longer_than_source_ids`는 (검사가 없으니 더 관대해질 뿐이라) 그대로 통과함도 함께 확인 → 원복 후 두 테스트 모두 통과. 기존 `test_calculate_returns_facts_and_ratios_from_real_annual_data`(PER/PBR `post_derived_rejected == []` 기대)가 이번 변경 후에도 계속 통과함을 확인해 비대칭 규칙이 회귀를 만들지 않았음을 검증.
- 미결: 없음. report_gpt.md 2026-07-14 15:56 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-14 16:33 GPT — 발견 없음(승인). `POST_DERIVED` 원천 메타데이터 개수 검증이 서비스·API 스키마 양쪽에 반영됐고, 전체 `./scripts/verify.sh` 통과 확인.

## 2026-07-14 18:10 | T05 (checklist.md C12-A [T05]) | I9 골든 평가 하네스 구현, BLOCKED 1항목(LLM fixture)으로 범위 축소
- 작업: docs/instructions.md 절차대로 시작 — report_gpt.md 미체크 항목 1건(2026-07-14 16:33, 발견 없음/승인) 확인·체크, backlog.md T05 선행조건(T00·T01) 완료 확인, checklist.md C12-A·docs/skills.md·docs/prerequisites.md 로드(T05 전용 외부 자격증명 없음 확인), `./scripts/verify.sh` baseline green 확인 후 착수. `docs/skills.md`에 "I9 골든 평가 하네스 계약" 절을 문서 먼저 원칙으로 추가(golden dataset 14개 카테고리·필수 커버리지 태그 7종·verdict 5상태, threshold registry 구조·변경 승인 규칙, scorer 계약, 평가 리포트·regression diff, harness smoke test 3종)한 뒤 `eval/`을 구현했다: `eval/schema.js`(dataset/registry validator), `eval/golden-v1.json`(30개 case — claim extraction, verdict 5상태(그룹 PARTIALLY_SUPPORTED 포함), 단위 혼동·CFS/OFS·누적분기, 정정공시·미래데이터 차단, provider 장애 분류, 인용 exact/fuzzy/none, 환각, injection, 추천 금지, schema 위반, retrieval recall/precision, counter retrieval, insufficient/unverifiable, 상충 근거), `eval/thresholds-v1.json`(checklist C12-A 명시값 그대로 등록 + retrieval·latency·cost·실패율은 초기 placeholder로 명시하고 change_log로 근거 기록), `eval/scorers.js`(14개 카테고리 순수 함수 scorer + aggregate, `contracts/schemas.js`의 validateShape/validateEvidence와 `contracts/verdict.js`의 groupVerdict를 재사용해 로직 중복 없음), `eval/report.js`(리포트 schema + 이전 리포트 대비 regression diff), `eval/run.js`(CLI 러너 — threshold 누락/dataset·registry schema 위반/scorer 예외/fixture checksum drift/dataset-registry 버전 불일치를 exit code 2로 구분해 차단, 정상 metric 실패는 exit 1, `eval/reports/latest.json`+`history/`에 저장, `.gitignore`에 `eval/reports/` 추가), `eval/fixtures-manifest.json`(OpenDART·KIS·네이버는 T02~T04에서 이미 캡처한 `backend/tests/fixtures/`를 재캡처 없이 sha256 checksum으로 고정 참조, LLM은 BLOCKED로 명시). `scripts/verify.sh`를 4단계로 확장(frontend→backend→I9 eval→secret scan)하고 `.github/workflows/ci.yml`에 신규 `eval` job 추가, `docs/prerequisites.md`의 branch protection 항목에 `eval` job을 required로 추가.
- 검증: `eval/*.test.js`(vitest, 70개) — 14개 scorer 전부 완전(reference_prediction)/불완전(의도적으로 틀린 predicted) synthetic 쌍으로 통과/차단 확인(단위 혼동, 환각 span 주입, injection marker 누출, 상충 근거 누락 등), dataset/registry schema validator의 정상·위반 case, report의 threshold 평가(min/max/equals/per_environment)·regression diff(개선/악화 방향 구분), harness smoke test(`MISSING_THRESHOLD`/`INVALID_DATASET`/`INVALID_THRESHOLD_REGISTRY`/`SCORER_ERROR`/`FIXTURE_CHECKSUM_MISMATCH`/`DATASET_THRESHOLD_MISMATCH`)가 실제 HarnessError로 이어짐을 임시 사본(실 파일 미변경)으로 자동 검증. 추가로 `scripts/verify.sh`가 실제로 실행하는 `node eval/run.js` 게이트에 대해 threshold 제거·필수 태그 제거·잘못된 operation 주입 3가지를 수동으로 실측해 매번 exit code 2로 차단함을 확인하고 `diff`로 원본 파일이 정확히 원복됐음을 확인(docs/harness.md H7과 동일한 red→green 증거). `./scripts/verify.sh` 전체(frontend 145 tests + backend 222 tests + I9 eval 16개 metric 전부 PASS + npm/pip audit 0건 + gitleaks 26 commits clean) green. checklist.md C12-A 9개 항목 중 8개 체크, backlog.md T05에 반영 내역 기록.
- 미결: **BLOCKED(범위 축소)** — C12-A "OpenDART·시세·외부 근거·LLM용 immutable record/replay fixture와 checksum" 중 LLM(Solar) fixture만 남았다. S7(Structured Claim, T06)이 아직 구현되지 않아 Solar를 호출하는 코드 경로 자체가 없고, `UPSTAGE_API_KEY`도 미발급(`docs/prerequisites.md` T06·T07 절 미체크)이라 agent가 대신 캡처할 수 없다. 해제 조건: 사용자가 UPSTAGE_API_KEY를 발급해 `.env`에 채우고 `docs/prerequisites.md` T06·T07 절을 갱신하며, T06에서 S7이 Solar를 실제로 호출하는 지점이 생기면 그 세션에서 T02~T04와 동일한 record/replay 방식으로 `eval/fixtures-manifest.json`의 `llm_solar` 항목을 채운다. 이 판단에 따라 backlog.md T05 상태는 "완료"가 아니라 "BLOCKED(좁혀짐)"으로 기록했다(T03 선례와 동일 패턴) — 나머지 8개 항목은 이미 완료 상태라 T06(선행조건 T04·T05) 착수는 가능하다고 판단했다(T04 세션의 선례와 동일 논리, backlog.md에 근거 기록). retrieval/counter-retrieval/insufficient·unverifiable/conflict/latency/cost/실패율 threshold 7개는 S18~S20·S7·T11 파이프라인이 아직 없어 실측 없는 초기 placeholder임을 명시했다 — T06·T07·T11·T12에서 실측이 쌓이면 change_log를 통해 갱신해야 한다. 커밋은 아직 수행하지 않음(전체 변경 uncommitted, 다른 uncommitted 세션 변경(T02~T04)과 함께 남아 있음). GPT 리뷰 요청: (1) eval/golden-v1.json의 numerical_consistency 카테고리가 `app/services/financial_calculator.py`(S3)의 실제 단위·CFS/OFS·누적분기 로직을 재사용하지 않고 `eval/scorers.js` 안에 작은 순수 helper(단위 배수표·select_fs_div·derive_single_quarter)로 독립 구현한 것이 "로직 중복"에 해당하는지, 아니면 eval 계층의 독립적 회귀 감시로서 의도된 설계인지. (2) retrieval·latency·cost 등 placeholder threshold 수치(0.70/0.50/0.90, latency dev 5000ms 등)가 임의 추정처럼 보이지 않도록 문서화가 충분한지.
- 확인: [x] 2026-07-14 18:23 GPT — 수정요청 2건 등록(report_gpt.md 2026-07-14 18:23): threshold registry 변경 승인 규칙을 스키마가 강제한다는 문서 주장과 실제 validator 동작 불일치, hallucination/injection/recommendation scorer가 금지 상태 자체가 아니라 expected와의 일치만 봐서 잘못된 golden label이 실패 0건 게이트를 우회할 수 있음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-14 18:40 | T05 후속 (report_gpt.md 2026-07-14 18:23 2건 반영) | 금지 metric scorer 불변식화·threshold 변경 승인 diff 검증
- 작업: review.md B절 절차로 미체크 1건(2026-07-14 18:23, 수정요청 2건)을 반영. 2건 모두 타당하다고 판단, 반박 없음. **(1, 중간)** `eval/scorers.js`의 `scoreHallucination`/`scoreInjectionDefense`/`scoreRecommendationBan`이 실제 상태를 `expected.hallucinated`/`expected.leaked`/`expected.banned_phrase_count`(case별 gold label)와 비교하던 것을, docs/skills.md:529가 정의하는 불변식("0건/false가 아니면 실패")에 직접 비교하도록 재작성 — `hallucinated===false`/`leaked===false`/`banned_phrase_count===0`만 통과시킨다. `expected.*`가 이 불변식과 다르면(dataset이 위반을 정상으로 기록) scorer가 조용히 넘기지 않고 던지도록 해(`scoreVerdictAccuracy`의 dataset 내부 일관성 검사와 같은 패턴) 잘못된 gold label 자체도 harness가 즉시 드러내게 했다. 리뷰가 제시한 정확한 재현(recommendation_ban, `expected.banned_phrase_count=1` + `output_text: "현재는 목표가를 제시합니다."`)을 그대로 회귀 테스트 3건(hallucination·injection_defense·recommendation_ban 각 1건)으로 추가. **(2, 중간)** `eval/schema.js`에 `validateThresholdChangeApproval(previous, next)`를 신설 — 두 registry 스냅샷을 비교해 값이 바뀐 metric마다 (a) `thresholds_version` bump와 (b) 그 metric을 정확히 지목하는 새 `change_log` 항목이 있는지 검증한다(`validateThresholdRegistry()`는 단일 파일 shape만 보므로 이전값 대비 diff는 이 함수가 따로 맡는다). `eval/run.js`에 `loadPreviousRegistryFromGit()`을 추가해 `git show HEAD:eval/thresholds-v1.json`으로 마지막 커밋 registry를 읽어 CLI(`node eval/run.js`) 실행 시 자동 적용하고, 위반 시 `THRESHOLD_CHANGE_NOT_APPROVED`(exit 2)로 막는다. git/커밋이 없거나 파일이 새로 생긴 경우(현재 `eval/`은 전부 uncommitted)는 비교 대상이 없으므로 조용히 건너뛴다(docs/harness.md 설계 원칙 3 — 인프라 상태로 하네스를 죽이지 않음). `docs/skills.md`의 "변경 승인 규칙"과 `docs/checklist.md` C12-A 관련 2개 항목의 "스키마가 강제" 과장 서술을 실제 구현(단일 파일 shape=`validateThresholdRegistry`, 이전값 대비 diff=`validateThresholdChangeApproval`+git, review.md 교차 점검=사람이 수행)에 맞춰 정정.
- 검증: `npx eslint eval/` clean. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 154 tests(신규 9건: 금지 metric 회귀 3건, `validateThresholdChangeApproval` 단위 테스트 5건, `loadPreviousRegistryFromGit` no-op 1건)·build·`npm audit`(0건), backend 222 tests, I9 eval 16개 metric 전부 PASS, gitleaks(26 commits) clean. red→green 실측 2건: (a) `scoreRecommendationBan`을 원래 gold-비교 로직으로 되돌리고 리뷰의 정확한 재현(`expected.banned_phrase_count=1` + 실제 금지 문구 포함 `output_text`)을 넣은 신규 테스트를 실행 → "함수가 던질 것으로 기대했으나 조용히 `ok:true`를 반환"하며 실패 확인(원래 버그 재현) → 원복 후 통과. (b) `validateThresholdChangeApproval`의 metric-diff 계산을 빈 배열로 강제 → "threshold 값을 낮췄지만 버전·change_log 그대로"인 재현 케이스가 `valid:true`로 잘못 통과함을 확인 → 원복 후 올바르게 `valid:false` 반환.
- 미결: 없음. report_gpt.md 2026-07-14 18:23 항목은 반영 완료로 체크함. `eval/`이 여전히 uncommitted라 `loadPreviousRegistryFromGit()`의 git-diff 경로는 실제 커밋 이후에야 유의미하게 작동한다(현재는 항상 스킵) — 커밋 후 실제 위반 케이스로 한 번 더 실측 확인을 권장.
- 확인: [x] 2026-07-14 18:37 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-14 18:37): `validateThresholdChangeApproval()`가 새 change_log의 `metric_or_field`만 확인하고 `before`/`after`가 실제 이전·이후 threshold 값과 일치하는지 확인하지 않아 감사 로그가 틀려도 통과함. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-14 18:52 | T05 후속 (report_gpt.md 2026-07-14 18:37 1건 반영) | threshold change_log before/after 정확성 검증
- 작업: review.md B절 절차로 미체크 1건(2026-07-14 18:37)을 반영. 타당하다고 판단해 수정. `eval/schema.js`에 `extractBound(entry)`를 추가 — metric 항목에서 `description`을 제외하고 선언된 bound(`min`/`max`/`equals`/`per_environment` 중 하나)만 뽑아낸다. `validateThresholdChangeApproval()`이 changed metric마다 "`metric_or_field`가 일치하는 새 change_log 항목이 있는가"만 보던 것을, 그 항목들 중 `before`가 `extractBound(prevMetrics[metric])`과, `after`가 `extractBound(nextMetrics[metric])`과 `JSON.stringify` 기준으로 정확히 일치하는 것이 하나라도 있는지까지 검증하도록 강화 — 이름만 맞고 값이 조작된 change_log 항목은 이제 `metric "..." has a change_log entry naming it, but none record the actual before/after bound` 에러로 거부된다. 리뷰의 정확한 재현(`metric_or_field="extraction_precision"`, 실제 bound는 0.8→0.85인데 `before=999, after=-1`로 기록)을 그대로 회귀 테스트로 추가했고, `per_environment`(환경별 map) metric이 정상적으로 전체 map을 `before`/`after`에 기록했을 때 통과하는 케이스도 추가해 canonical diff가 scalar와 map 양쪽에서 동작함을 확인. 리뷰가 덤으로 지적한 `validateThresholdRegistry()`의 JSDoc 과장("version bump + change_log entry are both schema-enforced")도 "단일 파일 shape만 검사, diff 성격의 (1)·(2)는 `validateThresholdChangeApproval()`이 따로 맡는다"로 정정. `docs/skills.md`의 "변경 승인 규칙"과 `docs/checklist.md` C12-A 관련 항목에도 before/after 정확성 요건을 포함하도록 갱신.
- 검증: `npx eslint eval/` clean. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 156 tests(신규 2건: before/after 조작 거부 재현, per_environment 정상 변경 통과)·build·`npm audit`(0건), backend 222 tests, I9 eval 16개 metric 전부 PASS, gitleaks(26 commits) clean. red→green 실측: `validateThresholdChangeApproval()`의 `accurate` 판정을 `true`로 강제 고정 → 리뷰의 정확한 재현 테스트(before=999/after=-1 조작)가 `valid:false`를 기대했으나 `valid:true`로 실패함을 확인(원래 버그 재현) → 원복 후 올바르게 `valid:false`·`"actual before/after bound"` 에러 반환.
- 미결: 없음. report_gpt.md 2026-07-14 18:37 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-14 18:44 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-14 18:44): `extractBound()`는 threshold metric entry가 단일 bound라고 가정하지만 `validateThresholdRegistry()`가 `min`/`max`/`equals`/`per_environment` 중 둘 이상 선언된 ambiguous entry를 허용해 실제 평가 bound와 changelog 검증 bound가 달라질 수 있음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-14 19:02 | T05 후속 (report_gpt.md 2026-07-14 18:44 1건 반영) | threshold metric entry 단일 bound 강제·extractBound 우선순위 정렬
- 작업: review.md B절 절차로 미체크 1건(2026-07-14 18:44)을 반영. 타당하다고 판단해 수정, 반박 없음. `eval/schema.js`의 `validateThresholdRegistry()`가 각 metric 항목에서 `min`/`max`/`equals`/`per_environment` 중 몇 개가 선언됐는지(`boundKinds = ['min','max','equals','per_environment'].filter((k) => k in entry)`) 세어, 정확히 1개가 아니면(`0`개 또는 `2`개 이상) 거부하도록 강화했다 — 기존에는 "최소 1개 이상"만 확인해 `{min: 0.8, max: 0.1}`처럼 서로 다른 kind를 동시에 선언한 ambiguous entry도 통과시켰다. `extractBound()`의 필드 확인 순서를 `eval/report.js`의 `resolveThreshold()`가 실제 평가에 쓰는 순서(`per_environment → min → max → equals`)와 동일하게 재정렬했다(기존은 `min → max → equals → per_environment`). 두 함수를 완전히 하나로 합치지는 않았다 — `resolveThreshold(entry, environment)`는 `per_environment`를 특정 환경의 스칼라로 resolve해서 반환하지만(실제 pass/fail 평가용), `extractBound(entry)`는 change_log 감사 비교를 위해 `per_environment` 전체 map을 그대로 반환해야 하므로 반환 타입 자체가 다르다 — 대신 두 함수 정의 옆에 서로를 참조하는 주석을 남겨 앞으로 한쪽 우선순위만 바뀌는 drift를 코드 리뷰에서 바로 보이게 했다. `docs/skills.md`의 "Threshold Registry" 절과 `docs/checklist.md` C12-A 관련 항목에 "정확히 하나의 bound kind만 허용" 규칙을 문서화.
- 검증: `npx eslint eval/` clean. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 157 tests(신규 1건: `{min, max}` 동시 선언 거부 재현)·build·`npm audit`(0건), backend 222 tests, I9 eval 16개 metric 전부 PASS, gitleaks(26 commits) clean. red→green 실측: `validateThresholdRegistry()`의 신규 "정확히 하나" 검사를 `if (false)`로 강제 비활성화 → 리뷰의 정확한 재현 테스트(`{min: 0.8, max: 0.1}`)가 `valid:false`를 기대했으나 `valid:true`로 실패함을 확인(원래 버그 재현) → 원복 후 올바르게 `valid:false`·`"exactly one of"` 에러 반환. 실제 `eval/thresholds-v1.json`의 모든 metric은 이미 단일 bound만 쓰므로 이번 강화로 인한 회귀는 없음(전체 verify green으로 재확인).
- 미결: 없음. report_gpt.md 2026-07-14 18:44 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-14 18:53 GPT — 발견 없음(승인). `validateThresholdRegistry()`의 단일 bound 강제와 `extractBound()`/`resolveThreshold()` 우선순위 정렬이 반영됐고, `{min,max}` ambiguous registry와 changelog before/after 조작 재현 모두 차단됨. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 통과 확인.

## 2026-07-14 23:53 | T06 (checklist.md C7·C8 [T06]) | S7·S16·S17·S23 구현, Solar 라이브 호출만 미검증
- 작업: docs/instructions.md 절차대로 시작 — report_gpt.md 미체크 항목 1건(2026-07-14 18:53, 발견 없음/승인) 확인·체크, backlog.md T06 선행조건 확인. 형식적 선행조건 T05가 `완료`가 아니라 `BLOCKED(좁혀짐)`임을 확인했으나, 남은 BLOCKED 항목(I9 LLM fixture)은 T06이 처음 만드는 Solar 호출 경로에 대한 fixture라 T06의 선행조건이 될 수 없다고 판단(순환 의존 아님, backlog.md에 근거 기록)하고 착수 — plan.md R07(선행조건 R01뿐)·R08(R04·R07)도 이 판단과 일치함을 확인. `./scripts/verify.sh` baseline green 확인 후 시작.
  C8(S16·S17, LLM 의존 없음)부터 완전 구현: `app/services/verdict_aggregator.py`(`contracts/verdict.js` Python 미러, 그룹 5상태 집계), `app/services/evidence_planner.py`(S17, comparator.op 기준 필수 근거 3종 registry+충족률), `app/services/deterministic_verifier.py`(S16, `THRESHOLD`/`INCREASE`/`DECREASE`/`MULTIPLE`/`RATIO`/`CONTINUITY` 6종 comparator, 부호 전환 `RATIO_UNDEFINED_SIGN_CHANGE`, 근거 모호 시 `AmbiguousEvidenceError`, financial/market/flow/valuation/peer 5개 도메인 어댑터 + `integrity_status` 기반 POST_DERIVED 신뢰 경계, `Calculation`+`AtomicVerdictResult`로 계산식·사용값·출처·reason_code 노출). `StructuredClaim`/`Comparator`를 `claim_type`(6종)·`comparator.op`(6종)·`comparison_operator`(5종) `Literal` enum + `extra="forbid"`로 강화하고 JS `contracts/schemas.js`에 `strict` allowlist 모드를 신설해 양쪽 동일 계약으로 맞춤(`docs/skills.md` "Structured Claim" 절 갱신 + migration 기록에 pre-release 보정으로 기록, 문서 먼저 원칙).
  C7(S7·S23)은 2026-07-14 WebSearch로 Upstage Solar 공식 문서(console.upstage.ai/api/chat, console.upstage.ai/api/docs/for-agents/raw)를 조사해 실제 엔드포인트(`POST https://api.upstage.ai/v1/chat/completions`)·인증(`Authorization: Bearer`)·모델명(`solar-pro3`)·Structured Outputs(`response_format={type:json_schema,...}`)·오류 status(400/401/403/429/5xx)를 확인한 뒤(임의 추정 아님) `app/providers/solar.py`(OpenDartProvider와 동일 tenacity retry·에러 매핑 패턴)를 구현. `app/services/llm_security_gateway.py`(S23 — 지시/데이터 delimiter 분리 + 그 delimiter 문자열 자체를 원문이 흉내낼 때 이스케이프하는 방어 추가, secret redaction, injection 패턴 trace, `run_structured()`가 provider 호출→schema 검증까지 단일 진입점), `app/services/structured_claim_extractor.py`(S7 — 원문 span·숫자 grounding으로 환각 후보 드롭, `corp_code`/`stock_code`/`as_of`는 검증이 아니라 애초에 LLM 출력을 신뢰하지 않고 S1 `resolved_company`·요청 `as_of`로 강제 덮어씀). F9 progressive disclosure는 `app/services/claim_disclosure.py`(Python)와 `contracts/disclosure.js`(JS 미러) 양쪽에 동일 계약으로 구현하고 `src/components/ClaimDisclosure.jsx`(React, ambiguity_flags 비면 요약 카드 자동 진행·있으면 객관식 확인 질문, 미답변 시 확인 필요 안내) + 테스트로 마무리. `POST /api/v1/claims/extract`·`POST /api/v1/claims/verify`(`app/routers/claims.py`) 라우터와 `ExtractionFailedError`/`AmbiguousEvidenceError` 전용 exception handler(422 VALIDATION_ERROR) 추가.
  부수 수정 2건: (1) `src/test/setup.js`에 `afterEach(cleanup)`이 없어 vitest `globals:false`에서 한 파일 내 여러 테스트의 render() 출력이 DOM에 누적되던 결함을 새 컴포넌트 테스트 작성 중 발견 — 전체 프론트엔드 테스트에 영향 있는 수정이라 전체 스위트 재실행으로 회귀 없음 확인. (2) `test_external_evidence_api.py`/`test_external_evidence_collector.py`의 claim fixture가 구 comparator 형식(`{"op": ">", ...}`, comparison_operator 없음)이라 새 enum 강화로 깨진 것을 발견·수정.
- 검증: `./scripts/verify.sh` 전체(frontend 178 tests + backend 324 tests + I9 eval 16개 metric 전부 PASS + npm/pip audit 0건 + gitleaks 30 commits clean) green. checklist.md C7 9개·C8 11개 항목 전부 체크(신규 backend pytest 다수: verdict_aggregator 8, evidence_planner 11, deterministic_verifier 25, llm_security_gateway 13, solar_provider 12, structured_claim_extractor 8, claim_disclosure 7, structured_claim_schema 7, claims_api e2e 10 등), backlog.md T06 `완료`로 갱신, plan.md R07·R08을 `IMPLEMENTED`로 갱신(C7·C8 전체 통과 확인 후). checklist.md C8의 "배수 >= 2, 실제 1.38배 → REFUTED" 요구를 단위 테스트와 API e2e 테스트 양쪽에서 문구 그대로 재현.
- 미결: checklist.md C7의 9개 항목은 모두 코드+테스트로 완료했지만 `UPSTAGE_API_KEY` 미발급으로 실제 Solar 라이브 호출은 미검증이다(C7 문서 내 "한계" 절 참고) — 이를 BLOCKED가 아니라 "한계"로 분류한 것은 checklist.md C7 항목 1이 "provider mock"을 명시적으로 완료 조건에 포함하고 있어서다(T02·T03·T05가 BLOCKED로 분류한 항목들은 체크리스트 문구가 실 캡처를 직접 요구했다는 점에서 다름). 키가 발급되면 `backend/tests/fixtures/solar/`에 실 라이브 호출을 캡처해 회귀 테스트로 추가한다. backlog.md T06 설명의 "Claim 편집"은 C7·C8에 별도 체크박스가 없어 F9 확인 질문 UI로 충족했다고 판단했고, 자유 편집 텍스트박스형 전체 편집기는 T08·T11로 미뤘다. 커밋은 아직 수행하지 않음(전체 변경 uncommitted). GPT 리뷰 요청 3건: (1) C7 "완료" 판정의 BLOCKED vs 한계 분류가 타당한지, (2) T05 미완료 상태에서 T06 착수 판단(위 "작업" 문단)이 타당한지, (3) `deterministic_verifier.py`가 부호 전환 처리를 MULTIPLE뿐 아니라 INCREASE/DECREASE에도 동일 적용(`RATIO_UNDEFINED_SIGN_CHANGE`)한 것이 checklist.md C8 문구("부호 전환 배수")의 확대 해석인지.
- 확인: [x] 2026-07-15 10:07 GPT — 수정요청 2건 등록(report_gpt.md 2026-07-15 10:07): S7 숫자 grounding이 substring 기반이라 원문에 없는 `target_value=2`가 `2025년`의 `2`로 오인되어 통과함, checklist.md C8 섹션이 중복되어 하나는 전부 미체크로 남아 T06 완료 주장과 충돌함. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-15 10:20 | T06 후속 (report_gpt.md 2026-07-15 10:07 2건 반영) | 숫자 grounding 토큰화·checklist.md 중복 C8 제거
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 10:07, 수정요청 2건)을 반영. 2건 모두 타당하다고 판단, 반박 없음. **(1, 높음)** `backend/app/services/structured_claim_extractor.py`의 `_is_grounded_number()`가 `str(value)`/정수 문자열이 원문 어딘가에 부분 문자열로 있으면 통과시키던 것을, 정규식(`_NUMBER_TOKEN_PATTERN = r"\d+(?:,\d{3})*(?:\.\d+)?"`)으로 원문에서 자릿수 경계를 지킨 숫자 '토큰'을 전부 추출(`_extract_number_tokens`)한 뒤 그 집합에 정확히 속하는지로 바꿔 재작성 — "2025년"의 정규식 매치는 "2025" 전체이므로 더 이상 `target_value=2`의 부분 매치가 되지 않는다. `value==0`은 기존대로 예외(방향성만 있는 주장의 관례적 임계값) 유지. 리뷰의 정확한 재현(원문 "삼성전자 2025년 영업이익이 늘었다.", `target_value=2`인 조작된 claim)을 `test_target_value_matching_only_a_substring_of_a_year_is_not_grounded`로 그대로 추가. **(2, 중간)** `docs/checklist.md`의 C8 섹션이 두 번 존재하던 것(112-124행: 11개 항목 전부 체크+구현 근거, 126-138행: 같은 11개 항목이 전부 `[ ]`인 미편집 stub 잔재) 중 뒤의 stub 블록을 삭제 — `docs/backlog.md` T06 `완료`·`docs/plan.md` R07·R08 `IMPLEMENTED` 판단은 원래도 완료된 앞쪽 블록을 근거로 한 것이었으므로 추가 정정은 필요 없음을 확인. `docs/checklist.md` C7의 "원문에 없는 span·기업·숫자 생성 0건" 항목에 이번 결함·수정 내역을 기록.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(104 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 178 tests·build·`npm audit`(0건), backend 325 tests(신규 1건), I9 eval 16개 metric 전부 PASS, gitleaks(30 commits) clean. red→green 실측: `_is_grounded_number()`를 원래의 부분 문자열 검색 로직으로 되돌리고 신규 테스트를 실행 → 리뷰가 지적한 그대로 조작된 claim이 `result.claims == ()`를 기대했으나 실제로는 1건 통과함을 확인(원래 버그 재현, `AssertionError`로 실측) → 원복 후 올바르게 빈 튜플·`"target_value"` 경고 반환.
- 미결: 없음. report_gpt.md 2026-07-15 10:07 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 10:19 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-15 10:19): 숫자 grounding 토큰화가 `2025년` 부분 문자열 케이스는 막았지만 `2분기`/`Q2` 같은 기간 숫자를 `target_value=2`의 근거로 여전히 인정해 C7 숫자 환각 차단 계약을 완전히 보장하지 못함. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인. C8 중복 제거는 확인.

## 2026-07-15 10:35 | T06 후속 (report_gpt.md 2026-07-15 10:19 1건 반영) | 분기 designator 숫자 grounding 제외
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 10:19)을 반영. 타당하다고 판단해 수정. `backend/app/services/structured_claim_extractor.py`에 `_PERIOD_DESIGNATOR_PATTERN = re.compile(r"\d+분기|Q\d+")`를 추가하고, `_extract_number_tokens()`가 숫자 토큰을 정규식으로 뽑기 전에 이 패턴에 매치되는 구간 전체를 원문에서 먼저 지우도록(`_PERIOD_DESIGNATOR_PATTERN.sub(" ", source_text)`) 수정했다 — 직전 토큰화 수정(10:07 반영)은 "2025년"의 부분 문자열 매치는 막았지만, "2분기"의 "2"나 "Q2"의 "2"는 그 자체로 정규식 자릿수 경계를 지킨 유효한 독립 토큰이라 여전히 `target_value=2`와 정확히 일치해버렸다 — designator 패턴 전체를 토큰화 이전에 제거하면 그 안의 숫자가 애초에 토큰 후보 집합에 들어오지 않는다. 원문의 다른 위치에 있는 진짜 "2배" 같은 값은 이 패턴에 걸리지 않으므로 정상적으로 grounding된다(기존 `test_grounded_claim_is_accepted_with_corp_code_as_of_overwritten` 등 회귀 없음, 아래 검증 참고). 리뷰의 정확한 재현 2건(`"삼성전자 2025년 2분기 영업이익이 늘었다."`, `"삼성전자 2025년 Q2 영업이익이 늘었다."`, 둘 다 target_value=2)을 각각 `test_target_value_matching_only_a_quarter_designator_digit_is_not_grounded`·`test_target_value_matching_only_a_q_notation_quarter_digit_is_not_grounded`로 추가. `docs/checklist.md` C7 105행에 이번 결함·수정 내역을 기록.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(104 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 178 tests·build·`npm audit`(0건), backend 327 tests(신규 2건), I9 eval 16개 metric 전부 PASS, gitleaks(30 commits) clean. red→green 실측: `_extract_number_tokens()`의 designator 제거 단계를 무력화(`without_period_designators = source_text`)하고 신규 테스트 2건을 실행 → 리뷰가 지적한 그대로 두 재현 모두 조작된 claim이 통과함을 확인(`AssertionError`, 원래 버그 재현) → 원복 후 둘 다 올바르게 빈 튜플·`"target_value"` 경고 반환.
- 미결: `target_unit`/`comparator.op`별 허용 표기 문맥 분리(예: multiple은 "N배"/"Nx" 근처 숫자만 허용)는 이번 범위에 포함하지 않았다 — 리뷰가 실제로 재현한 두 반례(분기 designator)는 정확히 막지만, 더 정교한 문맥 기반 grounding은 후속 리뷰나 T07 이후 실 데이터 반례가 나오면 확장 대상으로 남겨둔다. report_gpt.md 2026-07-15 10:19 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 10:44 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-15 10:44): `2분기`/`Q2` designator 숫자는 차단됐지만 `target_unit`·`comparator.op` 문맥 없이 숫자 값만 비교해 `2조원`의 `2`가 `target_unit="multiple"`/`target_value=2`의 근거로 인정되는 반례가 남아 C7 숫자 환각 차단 계약을 완전히 보장하지 못함. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-15 10:50 | T06 후속 (report_gpt.md 2026-07-15 10:44 1건 반영) | MULTIPLE op 배수 표기 기반 grounding
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 10:44)을 반영. 타당하다고 판단해 수정. `backend/app/services/structured_claim_extractor.py`의 `_is_grounded_number()` 시그니처를 `target_value: float` 하나만 받던 것에서 `comparator: Comparator` 전체를 받도록 바꾸고, `comparator.op == "MULTIPLE"`일 때만 `_MULTIPLE_NOTATION_PATTERN = re.compile(r"\d+(?:,\d{3})*(?:\.\d+)?\s*(?:배|[xX×])")`에 매치되는 숫자만 근거로 인정하는 `_extract_multiple_notation_tokens()`를 새로 추가해 분기 처리했다 — "2조원"은 숫자 뒤에 배수 표기(배/x/×)가 없어 이 패턴에 안 걸리므로, 배수 없이 금액만 있는 문장은 더 이상 `op=MULTIPLE` claim의 근거가 되지 못한다. MULTIPLE이 아닌 나머지 5개 op(THRESHOLD/INCREASE/DECREASE/RATIO/CONTINUITY)는 기존 `_extract_number_tokens()`(전체 숫자 토큰 비교, 분기 designator 제외)를 그대로 쓴다 — `target_unit`이 자유 문자열이라 percent·금액 등 표기를 전부 안전하게 열거하기 어렵고, 리뷰가 실제로 재현한 것도 MULTIPLE 케이스뿐이라 범위를 그쪽으로 좁혔다(문서화된 미결, 아래 참고). 호출부 `extract()`의 `_is_grounded_number(claim.comparator.target_value, text)`를 `_is_grounded_number(claim.comparator, text)`로 갱신. 리뷰의 정확한 재현("삼성전자 영업이익은 2조원이었다.", op=MULTIPLE, target_value=2)을 `test_multiple_claim_is_not_grounded_by_a_same_number_in_a_currency_amount`로 추가하고, 배수 판별 범위가 "배" 하나로 좁아지지 않았는지 확인하는 `test_multiple_claim_is_grounded_by_x_notation_not_only_bae`("2x")도 회귀 방지용으로 함께 추가. `docs/checklist.md` C7 105행에 이번 수정과 남은 한계를 기록.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(104 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 178 tests·build·`npm audit`(0건), backend 329 tests(신규 2건), I9 eval 16개 metric 전부 PASS, gitleaks(30 commits) clean. red→green 실측: `_is_grounded_number()`의 MULTIPLE 분기를 제거해 모든 op이 다시 `_extract_number_tokens()`(단순 숫자 토큰 비교)만 쓰도록 되돌리고 신규 테스트 2건을 실행 → 리뷰가 지적한 그대로 "2조원" 재현이 조작된 claim을 통과시킴을 확인(`AssertionError`, 원래 버그 재현)했고 "2x" 회귀 테스트는 두 상태 모두에서 통과함도 함께 확인(비활성화는 더 관대해지는 방향이므로 당연) → 원복 후 두 테스트 모두 올바른 결과 반환.
- 미결: MULTIPLE 외의 op(RATIO의 "%", 금액 단위 등)는 여전히 단위 표기를 구분하지 않고 원문 숫자 토큰 전체와 비교한다 — 완전한 단위별 문맥 grounding은 후속 리뷰나 T07 이후 실 데이터 반례가 나오면 확장 대상. report_gpt.md 2026-07-15 10:44 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 11:21 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-15 11:21): MULTIPLE 배수 표기 패턴이 좌우 경계를 보지 않아 `S2X` 제품/코드명이나 `2X200` 식별자 내부의 `2X`도 `target_value=2`의 배수 근거로 인정되는 반례가 남아 C7 숫자 grounding 계약을 완전히 보장하지 못함. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-15 11:28 | T06 후속 (report_gpt.md 2026-07-15 11:21 1건 반영) | MULTIPLE 배수 표기 좌우 경계 추가
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 11:21)을 반영. 타당하다고 판단해 수정, 권고를 그대로 채택. `backend/app/services/structured_claim_extractor.py`의 `_MULTIPLE_NOTATION_PATTERN`을 `r"\d+(?:,\d{3})*(?:\.\d+)?\s*(?:배|[xX×])"`에서 `r"(?<![A-Za-z0-9])\d+(?:,\d{3})*(?:\.\d+)?\s*(?:배|[xX×](?![A-Za-z0-9]))"`로 바꿔 좌우 lookaround 경계를 추가했다 — 숫자 시작 직전에 영문자·숫자가 있으면(`(?<![A-Za-z0-9])`) 매치하지 않으므로 "S2X"의 "2"(직전이 "S")가 배제되고, `x`/`X`/`×` 바로 뒤에 영문자·숫자가 있으면(`(?![A-Za-z0-9])`) 매치하지 않으므로 "2X200"의 "X"(직후가 "2")가 배제된다. "배"는 뒤에 조사(예: "이상")가 붙어도 ASCII 영문/숫자가 아니라서 별도 lookahead 없이 그대로 통과하며, 권고가 언급한 "배 별도 처리"는 실제로는 불필요함을 확인했다. 권고의 최소 회귀 세트를 그대로 테스트로 추가: `test_multiple_claim_is_not_grounded_by_x_inside_a_product_name`("S2X" → drop), `test_multiple_claim_is_not_grounded_by_x_inside_an_identifier`("2X200" → drop), `test_multiple_claim_is_grounded_by_space_separated_and_multiplication_sign_notation`("2 x"·"2×" → accept, "2x"·"2배"·"2배 이상" accept는 기존 테스트가 이미 커버). `docs/checklist.md` C7 105행에 이번 수정 내역을 기록.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(104 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 178 tests·build·`npm audit`(0건), backend 332 tests(신규 3건), I9 eval 16개 metric 전부 PASS, gitleaks(30 commits) clean. red→green 실측: `_MULTIPLE_NOTATION_PATTERN`을 경계 없는 원래 정규식으로 되돌리고 신규 테스트 3건을 실행 → 리뷰가 지적한 그대로 "S2X"·"2X200" 재현 2건 모두 조작된 claim이 통과함을 확인(`AssertionError`, 원래 버그 재현)했고 "2 x"·"2×" accept 테스트는 두 상태 모두에서 통과함도 함께 확인(경계 제거는 더 관대해지는 방향이므로 당연) → 원복 후 3건 모두 올바른 결과 반환.
- 미결: MULTIPLE 외의 op(RATIO의 "%", 금액 단위 등)는 여전히 단위 표기를 구분하지 않고 원문 숫자 토큰 전체와 비교한다는 기존 미결은 그대로 남아 있다 — 이번 수정 범위 밖. report_gpt.md 2026-07-15 11:21 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 11:40 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-15 11:40): MULTIPLE 배수 표기 경계가 ASCII 영문·숫자만 보므로 `S-2X` 같은 하이픈 코드명과 `2배럴`처럼 `배`가 다른 단어의 일부인 경우도 `target_value=2` 배수 근거로 인정되는 반례가 남아 C7 숫자 grounding 계약을 완전히 보장하지 못함. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-15 12:53 | T06 후속 (report_gpt.md 2026-07-15 11:40 1건 반영) | MULTIPLE 배수 표기 suffix별 경계 분리(하이픈·한글 접미)
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 11:40)을 반영. 타당하다고 판단해 수정, 권고를 그대로 채택. `backend/app/services/structured_claim_extractor.py`의 `_MULTIPLE_NOTATION_PATTERN`을 x/X/×용과 배용 두 갈래(`|` alternation)로 분리했다. **x/X/× 갈래**: 경계 문자 집합을 `[A-Za-z0-9]`에서 `[A-Za-z0-9_-]`로 확장 — "S-2X"의 "2" 직전에 있는 하이픈이 이제 lookbehind에 걸려 매치 자체가 시작되지 않는다. **배 갈래**: 새로 lookahead `(?=[가이는은를을도만나로])|(?![가-힣])`를 추가 — "배" 바로 뒤에 흔한 조사 1음절(가/이/는/은/를/을/도/만/나/로) 중 하나가 오면 배수 표기로 인정하고, 그 외 한글 음절(예: "럴")이 바로 붙으면 인정하지 않는다("2배럴"의 "배"는 "배럴"이라는 별개 단어의 일부이므로 배제). 권고가 제시한 최소 회귀 세트를 그대로 테스트로 추가: `test_multiple_claim_is_not_grounded_by_x_inside_a_hyphenated_code_name`("S-2X" → drop), `test_multiple_claim_is_not_grounded_by_bae_inside_a_different_word`("2배럴" → drop), `test_multiple_claim_is_grounded_by_bae_with_an_attached_particle`("2배가" → accept, 조사가 붙어도 정상 배수 표기임을 확인). `docs/checklist.md` C7 105행에 이번 수정을 기록하고, 정규식 기반 grounding이 이번까지 5차 수정임을 알려진 구조적 한계로 명시했다.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(104 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 178 tests·build·`npm audit`(0건), backend 335 tests(신규 3건), I9 eval 16개 metric 전부 PASS, gitleaks(30 commits) clean. red→green 실측: `_MULTIPLE_NOTATION_PATTERN`을 직전(11:28) 버전(하이픈·배 접미 경계 없는 상태)으로 되돌리고 신규 테스트 3건을 실행 → 리뷰가 지적한 그대로 "S-2X"·"2배럴" 재현 2건 모두 조작된 claim이 통과함을 확인(`AssertionError`, 원래 버그 재현)했고 "2배가" accept 테스트는 두 상태 모두에서 통과함도 함께 확인(경계 완화는 더 관대해지는 방향이므로 당연) → 원복 후 3건 모두 올바른 결과 반환.
- 미결: 조사 화이트리스트(가/이/는/은/를/을/도/만/나/로)가 실제 한국어 조사 전체를 포괄하지 못할 수 있어, 목록 밖 조사가 "배" 뒤에 바로 붙는 정상 문장에서 오탐(정상 배수 표기 거부)이 생길 가능성이 남아 있다. MULTIPLE 외의 op(RATIO의 "%", 금액 단위 등) 단위별 문맥 grounding 미구현도 그대로 남아 있다 — 둘 다 후속 리뷰나 실 데이터 반례가 나오면 확장 대상. report_gpt.md 2026-07-15 11:40 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 13:05 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-15 13:05): `S-2X`/`2배럴` 반례는 막혔지만 `배` 뒤 조사 화이트리스트가 좁아 `2배보다`, `2배까지` 같은 정상 배수 표현을 드롭해 Structured Claim extraction recall이 떨어지는 반례가 남음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과 확인.

## 2026-07-15 13:12 | T06 후속 (report_gpt.md 2026-07-15 13:05 1건 반영) | 배 grounding을 조사 allowlist→명사 blocklist로 역전
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 13:05)을 반영. 타당하다고 판단해 수정, 권고의 블록리스트 방향을 채택. 직전(12:53)에 내가 넣은 "배" 조사 allowlist(`(?=[가이는은를을도만나로])|(?![가-힣])`)가 조사 종류를 좁게 열거해 "2배보다"·"2배까지"·"2배로써" 같은 정상 배수 표현을 잘못 드롭하던 것을 재현으로 확인 — 조사·어미는 열린 부류라 allowlist가 태생적으로 불완전하다는 리뷰 진단에 동의. `backend/app/services/structured_claim_extractor.py`의 `_MULTIPLE_NOTATION_PATTERN` 배 갈래를 `배(?![` + `_BAE_NON_MULTIPLIER_HEADS` + `])` 형태의 명사-head 부정 lookahead로 뒤집었다 — 모듈 상수 `_BAE_NON_MULTIPLIER_HEADS = "럴"`(char class로 쉽게 확장)에 등록된 음절이 "배" 바로 뒤에 오면 배수가 아니라고 보고(배럴/barrel 배제), 그 외 모든 문자(조사/어미/공백/문장부호/끝)는 배수로 허용한다. x/X/× 갈래는 11:40 수정 그대로 유지. 권고의 최소 회귀 세트를 반영: 기존 단일 "2배가" accept 테스트를 `test_multiple_claim_is_grounded_by_bae_with_various_particles`(parametrize `["보다","까지","로써","가"," 이상"]`)로 확장해 5개 표현 모두 accept 고정, "2배럴" drop은 기존 `test_multiple_claim_is_not_grounded_by_bae_inside_a_different_word`가 계속 커버. `docs/checklist.md` C7 105행에 이번 수정을 기록.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(104 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 178 tests·build·`npm audit`(0건), backend 339 tests(신규 4건: parametrize 5개 중 "가"·" 이상"은 기존 커버분과 사실상 동일하나 명시), I9 eval 16개 metric 전부 PASS, gitleaks(30 commits) clean. red→green 실측: 배 갈래를 직전(12:53) 조사 allowlist 버전으로 되돌리고 신규 parametrize 테스트를 실행 → "보다"·"까지" 케이스가 정확히 드롭되어 실패함을 확인(`assert 0 == 1`, 리뷰가 지적한 recall 저하 재현)했고 allowlist에 있던 "가"·"로"(로써)·공백은 통과함도 대조 확인 → 원복 후 5개 전부 통과.
- 미결: 배 명사 블록리스트(`_BAE_NON_MULTIPLIER_HEADS`)는 "배"로 시작하는 명사가 열린 집합이라 태생적으로 불완전하다 — 현재 "럴"(배럴)만 등록. 완전한 판별은 형태소 분리가 필요(후속 과제)하며, 이 숫자 grounding 정규식은 이번까지 6차 수정으로 구조적 반례 여지가 계속 있음을 알려진 한계로 남긴다. MULTIPLE 외 op(RATIO의 "%"·금액 단위) 단위별 문맥 grounding 미구현도 기존 미결 그대로. report_gpt.md 2026-07-15 13:05 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 13:19 GPT — 발견 없음(승인). `2배보다`·`2배까지`·`2배로써` 정상 배수 표현은 accept되고, `2배럴`·`S-2X` 방어는 유지됨을 재현했다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 통과 확인.

## 2026-07-15 15:38 | T07 (checklist.md C9·C10) | 기능 C RAG·반증·인용·오케스트레이션·결과 UI 완료
- 작업: 착수 전 report_gpt.md 미체크 1건(2026-07-15 13:19 승인, 발견 없음)을 review.md B절로 확인·반영. S18 근거 검색(evidence_retriever.py — dense 임베딩 코사인 + 순수 Python BM25 sparse → RRF hybrid, 종목 metadata filter 필수, rerank·dedup·score threshold, Recall@K/precision), 벡터 스토어(vector_store.py — Chroma 시맨틱 InProcessVectorStore, IndexedChunk chunk/embedding schema + checksum·version), 임베딩 provider(embedding_provider.py — 결정론 HashingEmbeddingProvider + Upstage SolarEmbeddingProvider httpx mock). S19 반증 검색(counter_evidence_retriever.py — 방향 반전 counter query, 결정론 lexical relation 규칙 + RELATION_RULE_VERSION, 상충 감지·노이즈 기록). S20 인용 무결성(citation_integrity.py — exact/offset/fuzzy·checksum·DART canonical URL, 인용 실패 시 확정 차단). S8 오케스트레이션(evidence_orchestrator.py — 수치 branch(S16) ∥ 문서 branch(S18→S19→S20) 병합, 예산·timeout·coverage 기반 최대 3회 재검색, provider 장애 EXTERNAL_ERROR 보존, 그룹 집계). S9 체크리스트(checklist_generator.py). Evidence Pydantic 미러(schemas/evidence.py, claim_id/presentation_item_id 배타). POST /api/v1/evidence/verify(routers/evidence.py). 기능 C 결과 UI(EvidenceResult.jsx — 5상태 verdict·계산식·상충·인용 원문 viewer·체크리스트·provider 장애 구분). S23는 T06 게이트 재사용 + "모든 LLM 호출 S23 통과" 정적 커버리지 테스트 추가.
- 아키텍처 결정(사용자 2026-07-15): CLAUDE.md 목표 스택 Chroma·LangGraph는 chromadb(~700MB, onnxruntime·tokenizers·kubernetes 등)·langgraph의 무게가 pip-audit 0-vuln·mypy strict 게이트와 solar.py(httpx로 공식 계약) 선례에 어긋나 위험 → 사용자에게 선택을 물어 "인터페이스+결정론 인메모리"로 결정. VectorStore/EmbeddingProvider/오케스트레이션을 인터페이스로 두고 Chroma 동일 시맨틱 in-process + 순수 Python state graph로 구현, Solar embedding·LangGraph·Chroma 영속 실바인딩은 T11/T13로 연기·문서화.
- 검증: ./scripts/verify.sh 전체(frontend 182 tests + backend 395 tests + I9 eval 16 metric 전부 PASS(retrieval_recall_at_5·counter_retrieval_recall·conflict_detection 포함) + npm/pip audit 0건 + gitleaks 31 commits clean) green. 신규 backend pytest 57개(retriever/counter/citation/evidence schema/orchestrator/checklist/s23 gate)·frontend 4개. red→green: 인용 실패 시 확정 verdict 하향(CITATION_UNVERIFIED)·provider 장애 EXTERNAL_ERROR 보존·문서 인젝션 판정 무개입을 각각 재현 테스트로 고정. 커밋 없음(uncommitted working tree).
- 미결: BLOCKED 없음(C9·C10 전부 완료). 한계(문서화, checklist.md C9 하단): UPSTAGE_API_KEY 미발급으로 dense 임베딩(HashingEmbeddingProvider 결정론 baseline)·S8 서술형 LLM 판정 라이브 미검증 — T06 C7 선례와 동일하게 BLOCKED 아닌 "한계"로 분류(주입 가능한 결정론 provider로 계약 검증, 체크리스트 문구가 실 provider 캡처를 직접 요구하지 않음). GPT 리뷰 요청: (1) 목표 스택 Chroma·LangGraph를 인터페이스+인메모리로 구현하고 T11/T13로 연기한 결정의 타당성. (2) dense 임베딩·서술형 판정 라이브 미검증을 C7과 동일 "한계"로 분류한 판단. (3) S8 결정론 경로가 서술형 Claim에 LLM 없이는 확정하지 않고 INSUFFICIENT_EVIDENCE를 내는 설계가 과보수인지.
- 확인: [x] 2026-07-15 15:44 GPT — 수정요청 1건 등록(report_gpt.md 2026-07-15 15:44): S20 checksum/provenance gate가 통합 경로에서 `quote=doc.text`, `source_text=doc.text`, `stored_checksum=recomputed_checksum`으로 구성되어 실제 원문 변조·checksum mismatch를 잡을 수 없음. Chroma·LangGraph 연기, live 미검증 한계 분류, 서술형 Claim 보수 판정 설계는 수용 가능하다고 판단. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과(frontend 182 tests, backend 396 tests, I9 eval PASS, audits 0건, gitleaks clean).

## 2026-07-15 16:10 | T07 후속 (report_gpt.md 2026-07-15 15:44 1건 반영) | S20 checksum 통합 경로 자기비교 결함 수정
- 작업: review.md B절 절차로 미체크 1건(2026-07-15 15:44)을 반영. 타당하다고 판단해 수정, 반박 없음. 근본 원인: `evidence_orchestrator._to_citation_input()`이 `stored_checksum`·`recomputed_checksum`을 둘 다 검색 결과의 현재 `doc.text`로 같은 시점에 재계산해 대입했고(`checksum = chunk_checksum(...); stored_checksum=checksum, recomputed_checksum=checksum`), `quote=doc.text`·`source_text=doc.text`도 같은 값이라 exact 매칭 역시 자기 자신과만 비교됐다 — 따라서 색인 이후 실제 원문 변조가 있어도 checksum_ok는 구조적으로 항상 True였다. 수정: (1) `evidence_retriever.py`의 `RetrievedEvidence`에 `index_checksum` 필드를 추가하고, `EvidenceRetriever`가 `index()` 시점에 계산한 checksum을 `self._checksums` dict로 보존했다가 `retrieve()`가 만드는 각 `RetrievedEvidence`에 그대로 실어 보낸다(색인 당시 스냅샷). (2) `_to_citation_input()`은 이제 `stored_checksum=ev.index_checksum`(색인 시점 값)과 `recomputed_checksum=chunk_checksum(doc.document_id, doc.chunk_index, doc.text)`(인용 검사 시점에 현재 quote 텍스트로 재계산한 값)를 서로 다른 시점의 값으로 비교한다. `quote`/`source_text`는 리뷰가 별도 지적하지 않았고 인용문=본문 그대로 노출하는 현재 설계(전체 chunk를 quote로 쓰는 UI) 자체는 문제가 아니라고 판단해 변경하지 않았다.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(122 source files). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 182 tests·build·`npm audit`(0건), backend 400 tests(신규 4건), I9 eval 16개 metric 전부 PASS, gitleaks(31 commits) clean. red→green 실측: `_to_citation_input()`을 원래의 자기비교 코드(`checksum = chunk_checksum(...); stored=recomputed=checksum`)로 되돌리고 신규 테스트 2건(`test_citation_input_detects_post_index_text_tampering`, `test_post_index_tampering_downgrades_confirmed_verdict_via_orchestrator`)을 실행 → 리뷰가 지적한 그대로 색인 이후 원문이 바뀐 근거도 `checksum_ok=True`로 통과해 orchestrator가 `SUPPORTED`를 그대로 확정함을 확인(`AssertionError: assert 'SUPPORTED' == 'INSUFFICIENT_EVIDENCE'`, 원래 결함 재현) → 원복 후 두 테스트 모두 `CITATION_UNVERIFIED`로 정확히 하향됨을 확인. `test_evidence_retriever.py::test_retrieved_evidence_carries_index_time_checksum`으로 `index_checksum` 전파도 별도 고정.
- 미결: 없음. `docs/checklist.md` C9(137행)에 수정 이력·신규 테스트명을 기록했다. report_gpt.md 2026-07-15 15:44 항목은 반영 완료로 체크함.
- 확인: [x] 2026-07-15 16:18 GPT — 발견 없음(승인). S20 checksum 자기비교 결함은 `RetrievedEvidence.index_checksum` 전파와 `_to_citation_input()`의 stored/recomputed 분리로 해소됨. `./scripts/verify.sh` 재현 통과.

## 2026-07-15 16:10 | T07 후속 보강 (S20 stored checksum API 경계) | 원본 checksum 입력 경로 추가
- 작업: 15:44 피드백 반영 후 추가 점검에서 `index_checksum`은 생겼지만 API 호출자가 S2/S14 원본 snapshot checksum을 넘기는 통로가 부족한 점을 보강했다. `EvidenceDocumentInput.stored_checksum`·`SourceDocument.stored_checksum`을 추가하고, `EvidenceRetriever.index()`가 이 값이 있으면 현재 text 재계산값 대신 stored checksum으로 보존하게 했다. 라우터도 request document의 `stored_checksum`을 `SourceDocument`로 전달한다.
- 검증: `cd backend && uv run ruff check . && uv run mypy .` clean(122 source files). 관련 타깃 테스트 `tests/test_evidence_retriever.py tests/test_evidence_orchestrator.py tests/test_evidence_api.py` 27 passed. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 전체 통과 — frontend 182 tests, backend 402 tests, I9 eval 16개 metric PASS, npm/pip audit 0건, gitleaks(31 commits) clean.
- 미결: 없음. `docs/checklist.md` C9에 API stored checksum 경계와 `test_retrieved_evidence_uses_stored_checksum_when_provided`·`test_stored_checksum_mismatch_downgrades_confirmed_verdict`를 추가 기록했다.
- 확인: [x] 2026-07-15 16:18 GPT — 발견 없음(승인). `EvidenceDocumentInput.stored_checksum`·`SourceDocument.stored_checksum`·라우터 전달·retriever 보존 경로가 실제 코드에 연결되어 있고 API 레벨 mismatch 하향 테스트가 있다. `./scripts/verify.sh` 재현 통과.
