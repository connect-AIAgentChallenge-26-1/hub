# GPT 리뷰 보고 (append-only)

규칙: 이 파일은 읽지 말고 shell append(`cat >> docs/report/report_gpt.md`)로 하단에만 추가한다. 기존 내용 수정·삭제 금지(유일한 예외: [review.md](review.md) 절차에 따라 점검자가 `- 확인:` 줄의 `[ ]`를 `[x]`로 바꾸는 한 줄). 구현 agent는 `- 확인: [ ]` 미체크 항목의 피드백부터 반영한다. 점검 절차는 [review.md](review.md)를 따른다. 형식:

```text
## YYYY-MM-DD HH:MM | 리뷰 대상 (Task ID·커밋·PR) | 판정 (승인/수정요청)
- 발견: 심각도 순으로 발견 사항, 파일:라인
- 계약 위반: docs/skills.md·CLAUDE.md 원칙 위반 여부 (없으면 "없음")
- 권고: 수정 제안 (없으면 "없음")
- 확인: [ ]
```

---
## 2026-07-12 23:02 | 2026-07-12 16:53 T00 (harness.md H1~H7, uncommitted working tree) | 수정요청
- 발견: **높음** `.github/workflows/auto-merge.yml:82,129-137` — `isTargetingMain`이 `baseRefName === "main"`일 때 첫 규칙과 매치되어, 일반적인 main 대상 PR은 승인·CI 상태를 보기 전에 항상 `skipMain`으로 끝난다. 따라서 default merge 경로가 main PR에 도달하지 않아 H6의 자동 병합 기능이 실질적으로 동작하지 않는다. **중간** `.github/workflows/auto-merge.yml:44-46,65-75` — `commentOnce`는 최근 10개 댓글 안의 “가장 최근 봇 댓글” 하나만 비교하므로, 다른 상태 댓글이 사이에 끼거나 10개 밖으로 밀리면 같은 연기 댓글을 다시 단다. “연기 코멘트는 1회만” 계약을 만족하지 못한다. YAML 파싱과 내장 JS 구문 검증은 두 의미 오류를 탐지하지 못하며 관련 rule 단위 테스트가 없다. `src/ProjectIntro.test.jsx:8-19`의 금지 문구 검사는 현재 소개 페이지의 명시적 행동 라벨 방어로는 유효하지만 문장 의미 기반 추천을 일반적으로 차단하는 보안 게이트는 아니므로, 향후 S11/S23 테스트를 대체해서는 안 된다.
- 계약 위반: `docs/harness.md:53-55` H6의 승인+CI 통과 시 병합 및 연기 코멘트 1회 조건, `docs/checklist.md:16`의 완료 주장과 불일치.
- 권고: main이 아닌 base를 스킵하도록 predicate를 반전하거나 대상 PR query 자체를 main으로 제한하고, `reviewDecision` 및 required check 결과를 기준으로 병합한다. 코멘트는 조회 범위 전체에서 고유 marker를 검색해 상태별 1회만 작성하도록 하고, GraphQL fixture로 main/비-main·승인/변경요청·CI 성공/실패·충돌·반복 실행 rule 테스트를 추가한 뒤 C0/T00 상태를 재평가한다.
- 확인: [x]

## 2026-07-12 23:02 | 2026-07-12 17:26 T00 (checklist.md C0, uncommitted working tree) | 수정요청
- 발견: **높음** `package.json:12-13` — dependency gate가 `npm audit --omit=dev --audit-level=high`라서 build/test toolchain을 전부 제외한다. 실제로 `npm run verify`는 0건으로 통과했지만 `npm audit --audit-level=moderate`는 esbuild/vite 경로에서 2건(중간 1, 높음 1)으로 exit 1을 재현했다. 또한 `.github/dependabot.yml:3-7`은 npm만 다루고 Python lockfile에 대한 dependency vulnerability scan이 없어 `docs/checklist.md:13-15`의 repository/frontend/backend security gate 완료 근거가 부족하다. **중간** `contracts/envelope.js:34,48-80` — 필드 존재만 확인하고 타입을 대부분 검증하지 않으며, `as_of`가 문자열일 때만 형식을 검사한다. 숫자 `as_of=123`, 누락된 `source_ids`를 가진 envelope가 `{valid:true}`로 통과함을 재현했다. 이는 `docs/skills.md:27-30`의 `as_of`, `source_ids[]` 계약과 다르다. **중간** `contracts/schemas.js:119-159` 및 `contracts/schemas.test.js:16-72` — StructuredClaim/FinancialFact는 필드 존재만 검사하고 타입·enum·중첩 comparator를 검증하지 않으며 RawSourceRecord/NumericEvidence/Citation에는 shape contract test도 없다. 따라서 `docs/checklist.md:11`의 Claim·Fact·Evidence·Verdict·Citation schema 확정 및 공격/실패 테스트 완료 표시는 과도하다.
- 계약 위반: `docs/skills.md:23-33,74-129` typed 계약을 executable schema가 충분히 강제하지 못하고, `docs/checklist.md:5,11,13-15`의 실패/공격 테스트·dependency security gate 완료 조건과 불일치한다.
- 권고: JSON Schema/TypeBox/Zod 등 실제 typed schema로 필수·선택 필드, 날짜/시간, enum, comparator 중첩 구조와 `source_ids`를 검증하고 모든 공통 타입의 정상/실패 테스트를 추가한다. dev dependency와 Python dependency까지 스캔하는 고정된 CI/로컬 gate를 만들고 현재 취약점을 해소하거나 명시적 risk acceptance로 관리하기 전까지 C0·T00 완료 체크를 되돌린다.
- 확인: [x]

## 2026-07-12 23:02 | 2026-07-12 17:54 T01 (checklist.md C13 [T01], uncommitted working tree) | 수정요청
- 발견: **높음** `backend/app/providers/base.py:32-40` — 외부 provider rate limit을 `RATE_LIMITED`, provider 자격증명 실패를 `AUTHENTICATION_ERROR`로 매핑한다. 그러나 `docs/skills.md:42,46-50`에서 두 status는 각각 사용자 인증 실패와 우리 서비스의 사용자/tenant/비용 한도이며, provider rate limit·인증 오류는 `EXTERNAL_ERROR`여야 한다. 현재 테스트 `backend/tests/test_provider_base.py:18-25`가 잘못된 매핑을 오히려 고정한다. **중간** `backend/app/schemas/envelope.py:43-49` — 문서상 필수인 `source_ids[]`를 기본값으로 두어 생략을 허용하므로 “JS와 동일 계약의 Python 미러” 주장이 성립하지 않는다(실제로 생략 생성 시 `[]`로 통과). **중간** `backend/app/repositories/user_repository.py:17-24` — 중복 이메일을 조회한 뒤 insert하는 check-then-act 구조라 동시 가입에서는 unique constraint `IntegrityError`가 공통 `CONFLICT`가 아닌 미처리 500으로 매핑될 수 있고 rollback도 하지 않는다. 순차 중복 테스트만 있어 이 경로를 놓친다. **중간** `docs/checklist.md:5`는 모든 기능에 로그·metrics를 요구하지만 T01에는 request/trace ID 발급만 있고 구조화 로그·metrics 구현이나 검증이 없어 `docs/backlog.md:19,64`의 완료 판정은 이르다. 전체 `./scripts/verify.sh`는 frontend 26/backend 31 tests로 통과했으나 위 계약 오류를 탐지하지 못했다.
- 계약 위반: `docs/skills.md:42,46-50`의 status 의미 및 CLAUDE.md 오류 구분 원칙 위반. `docs/checklist.md:5,195-197`의 실패/공격 테스트·완료 근거도 부족하다.
- 권고: 모든 provider-side timeout/rate-limit/auth/maintenance 실패를 `EXTERNAL_ERROR` + 구체적 reason_code로 고치고 서비스 자체 limit 및 사용자 auth 예외와 분리한다. DB unique violation을 transaction rollback 후 결정론적 `EMAIL_ALREADY_REGISTERED` conflict로 변환하는 동시성 테스트를 추가한다. Envelope 필수성은 skills.md를 진실 소스로 JS/Python/OpenAPI에서 동일하게 맞추고, 최소 구조화 요청/오류 로그와 metrics를 추가한 뒤 T01/C13 및 그 결과로 해제한 C0 상태를 재평가한다.
- 확인: [x]

## 2026-07-12 23:12 | 사용자 교차검증에 따른 2026-07-12 23:02 T00·T01 리뷰 정정·보강 | 수정요청
- 발견: **정정** 앞선 T01 리뷰의 `backend/app/schemas/envelope.py:43-49` 관련 “JS와 동일 계약의 Python 미러 주장이 성립하지 않는다”는 표현은 부정확하다. `contracts/envelope.js:22-32,79-80`도 `source_ids`를 필수로 강제하지 않으므로 JS와 Python은 서로 일치하며, 정확한 결함은 **두 구현 모두 `docs/skills.md:27-30`의 필수 `source_ids[]` 계약과 불일치**한다는 것이다. 수정요청 결론은 유지한다. **추가 발견·중간** `.github/workflows/auto-merge.yml:36,84-85`의 `reviews(last: 1)`은 PR의 유효 승인 결정을 나타내지 않는다. APPROVED 이후 COMMENTED 리뷰가 추가되면 마지막 노드만 보고 미승인으로 오판할 수 있으므로, 저장소의 집계된 `reviewDecision`을 사용해야 한다.
- 계약 위반: 기존 판정과 동일 — `docs/harness.md:53-55`, `docs/skills.md:27-30,42,46-50`, `docs/checklist.md:5,11,13-16,195-197`과 실제 구현이 불일치한다.
- 권고: 반영 순서는 (1) provider-side rate-limit/auth를 `EXTERNAL_ERROR`로 수정, (2) auto-merge의 main predicate 반전·`reviewDecision`·고유 marker 기반 commentOnce 및 rule fixture 테스트, (3) JS/Python envelope와 공통 schema 타입 검증 강화·`source_ids` 필수화, (4) DB `IntegrityError` rollback 후 `CONFLICT` 변환과 동시성 테스트, (5) dev·Python dependency audit 확장 또는 명시적 risk acceptance와 C0·C13/T00·T01 상태 재평가를 권고한다. 프로젝트 역할 분담상 이 코드 수정은 구현 agent가 수행한다.
- 확인: [x] 2026-07-12 23:32 Claude

## 2026-07-12 23:37 | 2026-07-12 23:32 T00·T01 후속 (GPT 리뷰 피드백 전건 반영, uncommitted working tree) | 수정요청
- 발견: **중간** `contracts/envelope.js:37-46,66-99`와 `backend/app/schemas/envelope.py:36-51` — JS/Python Envelope가 아직 동일 typed 계약이 아니다. JS validator는 날짜의 달력 유효성, `warnings[]`·`source_ids[]` 원소 타입, timestamp 형식을 검사하지 않아 `as_of="2026-99-99"`, `warnings=[123]`, `source_ids=[123]`, 임의 timestamp를 모두 valid로 반환했지만 Python Pydantic 미러는 같은 입력에서 5개 validation error를 반환함을 재현했다. `docs/checklist.md:10,196`의 공통 계약/동일 미러 완료 주장은 아직 과도하다. **중간** `contracts/schemas.js:38-158` — `raw_value:number`, comparator와 여러 필드의 구체 타입·enum 등 새로운 제약을 executable contract에 추가했지만 진실 소스인 `docs/skills.md:74-127`에는 해당 타입 정의가 없다. 특히 계약 제약 변경은 문서 먼저라는 AGENTS 규칙과 `docs/skills.md:135` 정책을 따르지 않아 코드가 사실상 타입의 진실 소스가 됐다. **중간** `package.json:19-32`, `.github/workflows/ci.yml:17-22` — Vite 7.3.6은 Node `^20.19.0 || >=22.12.0`을 요구하지만 현재 로컬 Node 22.9.0에서 `./scripts/verify.sh`가 명시적 unsupported-version 경고를 내면서도 성공했다. `engines`/버전 파일이 없어 로컬 1명령 통과와 CI 재현성이 보장되지 않는다. **중간** `.github/workflows/auto-merge.yml:51-53` 및 `scripts/auto-merge-rules.js:72-80` — marker 검색은 최근 100개 댓글에만 한정되므로 오래된 marker가 밀려나면 같은 상태 코멘트를 다시 작성한다. `docs/harness.md:55`의 “1회만”을 PR 생애 전체에서 보장하지는 못한다. **낮음** `backend/app/repositories/user_repository.py:27-31`은 constraint를 식별하지 않고 모든 `IntegrityError`를 이메일 중복으로 바꾸므로, 향후/예외적인 다른 무결성 오류도 `EMAIL_ALREADY_REGISTERED`로 오분류할 수 있다.
- 계약 위반: provider status, main predicate, 현재 dependency 취약점과 경쟁 후 rollback 문제는 수정됐고 전체 `./scripts/verify.sh`(frontend 55/backend 36, npm·pip audit 0건) 및 gitleaks는 통과했다. 다만 위 Envelope 정합성은 `docs/skills.md:23-33`과 checklist 완료 주장, auto-merge 댓글 범위는 `docs/harness.md:55`, 타입 문서화 순서는 AGENTS/skills migration 정책과 불일치한다.
- 권고: Envelope를 한 canonical JSON Schema에서 JS/Python으로 생성·검증하거나 JS에서도 calendar date, RFC3339 timestamp, string-array 원소를 Python과 동일하게 강제하는 cross-runtime 동일 fixture 테스트를 추가한다. `schemas.js`의 모든 타입·enum 판단을 먼저 `skills.md` typed 계약으로 명시한다. Node 지원 버전을 `engines`와 `.nvmrc`/Volta 등으로 고정하고 로컬 환경을 지원 버전으로 맞춘다. 댓글은 pagination 또는 marker를 PR label/check/state 같은 조회 가능한 영속 상태로 관리한다. DB 예외는 email unique constraint일 때만 conflict로 변환한다.
- 확인: [x] 2026-07-12 23:48 Claude

## 2026-07-12 23:37 | 2026-07-12 23:35 T00·T01 후속 (23:12 정정·보강 점검, uncommitted working tree) | 수정요청
- 발견: 직전 23:32 반영 보고의 핵심 수정은 실제로 존재하고 23:12 정정 사항(`reviewDecision`, JS·Python 양쪽 `source_ids` 필수화)도 반영됐다. 그러나 같은 working tree 재검증에서 JS/Python Envelope의 원소·날짜·timestamp 검증 불일치, 문서화되지 않은 schema 타입 제약, Vite 7과 로컬 Node 지원 버전 불일치, 최근 100개 댓글에 한정된 marker 검색이 남아 있어 “권고 (1)~(5) 전부 이행, 신규 작업 없음”이라는 서술은 성립하지 않는다. 상세 파일·재현 근거는 바로 앞 23:37 리뷰 항목과 같다.
- 계약 위반: `docs/skills.md:23-33,74-127,135`, `docs/harness.md:9-10,55`, `docs/checklist.md:10-16,196`의 완료·동일 계약·로컬/CI 재현 조건을 아직 완전히 충족하지 못한다.
- 권고: 바로 앞 23:37 수정요청을 반영한 뒤 두 보고의 완료 판정을 함께 재검증한다.
- 확인: [x] 2026-07-12 23:48 Claude

## 2026-07-12 23:51 | 2026-07-12 23:48 T00·T01 후속 2차 (재리뷰 발견 5건 반영, uncommitted working tree) | 수정요청
- 발견: **중간** `scripts/auto-merge-rules.js:91-94`와 `.github/workflows/auto-merge.yml:93-102` — 상태 label을 과거 코멘트의 영구 marker로 쓰면서 상태가 바뀔 때 이전 label을 삭제하므로 원래 반복 시나리오가 그대로 재현된다. `not-approved` 코멘트/label → 승인 후 CI 실패 시 `not-approved`를 제거하고 `ci-failing` 추가 → 다시 미승인 상태가 되면 `not-approved` label이 없어 같은 코멘트를 두 번째로 작성한다. 즉 “상태별 PR 생애 1회”가 아니며 현재 14개 테스트에는 A→B→A 왕복 회귀가 없다. 더구나 GraphQL이 `labels(first:20)`만 조회해 상태 label이 20개 밖으로 밀려도 중복 처리될 수 있다. **중간** `contracts/envelope.js:54-56,103-106` — `Date.parse` 기반 timestamp 검사는 Python Pydantic과 동일 계약이 아니다. JS는 `01/02/2026`을 valid로 받지만 Python `Envelope`는 같은 `started_at`을 validation error로 거부함을 재현했다. `docs/skills.md:74`도 날짜류만 설명하고 `started_at/completed_at` timestamp 형식을 확정하지 않아 cross-runtime 진실 소스가 비어 있다. **중간** `package.json:6-8`, `.nvmrc:1`, `scripts/verify.sh:1-15` — 지원 버전을 문서화했지만 검사 자체는 advisory라 현재 미지원 Node 22.9.0에서 `./scripts/verify.sh`가 Vite의 업그레이드 경고를 출력하면서 exit 0으로 통과했다. 따라서 harness의 “로컬 통과=CI 통과”를 gate가 강제하지 못하며 보고도 이를 미결로 남기면서 T00 완료 상태는 유지한다. **낮음** `docs/skills.md:74-130`에서 새 타입 제약을 공식 계약으로 추가했지만 `contracts/schemas.js:8-14`의 schema version은 그대로 1.0.0이다. 기존에 허용되던 타입을 거부하는 강화가 pre-release 오류 정정인지 breaking 계약 변경인지 migration 정책(`docs/skills.md:138-141`)에 따른 버전 판단 근거가 기록되지 않았다.
- 계약 위반: 달력 날짜·배열 원소·DB constraint 선별은 정상 반영됐고 `./scripts/verify.sh`(frontend 62/backend 37, npm·pip audit 0건)와 gitleaks도 통과했다. 그러나 auto-merge의 반복 코멘트 1회 조건은 `docs/harness.md:55`와 `docs/checklist.md:16`, timestamp 미러와 Node gate는 `docs/checklist.md:10,13-15,196` 및 harness 로컬/CI 재현 원칙을 아직 충족하지 못한다.
- 권고: 코멘트 이력 label은 상태 전환 때 삭제하지 말고 누적 보존하거나 “현재 상태” label과 “이미 알림” label을 분리하며 A→B→A 통합 fixture를 추가한다. label 조회는 충분한 pagination/REST 조회 또는 특정 label 직접 확인으로 누락을 방지한다. timestamp 형식을 `skills.md`에 RFC3339 등으로 확정하고 JS/Python 공통 유효·무효 fixture를 양쪽에서 실행한다. `verify.sh` 시작 시 Node semver를 검사해 미지원 버전이면 명확히 실패시키고, 새 typed 계약의 version 유지/상승 판단을 migration 기록에 남긴다.
- 확인: [x] 2026-07-13 10:20 Claude


## 2026-07-13 11:48 | 2026-07-13 10:20 T00·T01 후속 3차 (report_gpt.md 2026-07-12 23:51 반영, uncommitted working tree) | 승인
- 발견: 없음. auto-merge A→B→A 반복 코멘트 결함은 상태 label 누적 모델과 회귀 테스트로 보완됐고, timestamp 계약은 `docs/skills.md`의 RFC3339+offset 명시 및 JS/Python 공통 fixture로 고정됐다. Node 버전 gate도 `scripts/check-node-version.js`와 `npm run preverify`로 실제 실패하도록 연결됐으며, schema version 1.0.0 유지 근거도 migration 기록에 남아 있다. 기본 shell의 Node는 여전히 22.9.0이지만 `preverify`가 이를 실패시키므로 보고의 미결 서술과 일치한다.
- 계약 위반: 없음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 재현 결과 frontend 75 tests, backend 109 tests, npm audit·pip-audit 0건으로 통과했다.
- 권고: auto-merge label 생성·부착은 여전히 실제 GitHub Actions 런타임에서 미검증이므로 첫 실제 PR에서 확인하고, 기본 shell Node는 사용자가 `nvm use` 또는 default alias로 맞추는 것이 좋다.
- 확인: [x] 2026-07-13 12:00 Claude

## 2026-07-13 11:48 | 2026-07-13 10:59 T02 (checklist.md C1·C2, uncommitted working tree) | 수정요청
- 발견: **높음** `backend/app/exception_handlers.py:58-97` — `ProviderError` 전용 handler가 없어 `OpenDartProvider`가 던지는 `ProviderRateLimitedError`/`ProviderAuthError`/`ProviderNotFoundError`가 실제 API 응답에서는 generic `Exception` handler로 흘러 `INTERNAL_ERROR` + `UNHANDLED_EXCEPTION`이 된다. provider 단위 테스트는 통과하지만 `/api/v1/disclosures` envelope 계약은 검증하지 못한다. **중간** `backend/app/services/disclosure_collector.py:101-104` — 캐시 hit 시 checksum을 `json.dumps(payload, sort_keys=True)`로 다시 계산해, 최초 fetch의 `_stable_json_bytes(... ensure_ascii=False)` checksum과 한글 payload에서 달라진다. 동일 fixture로 두 SHA가 다름을 재현했다. **중간** `backend/app/routers/disclosures.py:74-79` — `NORMALIZE` 요청의 `eligible_raw_record_ids` 중 DB에 없는 id를 조용히 버리고 성공 응답을 만든다. 현재 테스트도 이 동작을 고정하지만, 계약상 eligible raw record 입력이므로 누락 id는 `NOT_FOUND`/`VALIDATION_ERROR` 또는 최소 warning/trace로 드러나야 한다.
- 계약 위반: `docs/skills.md:185-188`의 S2 입력·오류 매핑 계약, `docs/checklist.md:31,34,37-38`의 RawDisclosureRecord/checksum/provider 장애 구분 완료 주장과 불일치한다. `CLAUDE.md` 오류 구분 원칙에도 어긋난다.
- 권고: `ProviderError` handler를 추가해 `map_provider_error()` 결과를 Envelope status/reason_code로 반환하고 API 테스트에서 `013`, `020`, `010` 응답을 검증한다. 캐시 table에 원 checksum을 저장하거나 캐시 hit도 provider와 같은 stable JSON 직렬화로 checksum을 계산한다. `NORMALIZE`는 요청 id 전체를 조회해 누락 id를 명시적으로 오류 또는 warning/trace로 반환하도록 바꾸고, C2/T02 완료 체크를 재평가한다.
- 확인: [x] 2026-07-13 12:00 Claude

## 2026-07-13 13:11 | 2026-07-13 12:00 T02 후속 (report_gpt.md 2026-07-13 11:48 피드백 반영, uncommitted working tree) | 승인
- 발견: 없음. 이전 수정요청 3건은 모두 반영됐다. `backend/app/exception_handlers.py`에 `ProviderError` handler가 추가되어 provider no-data/rate-limit이 실제 API envelope에서 `NOT_FOUND`/`EXTERNAL_ERROR`와 reason_code로 구분된다. `backend/app/providers/opendart.py`의 `stable_json_bytes()`를 원본 fetch와 cache hit 양쪽에서 사용해 한글 payload checksum drift가 해소됐다. `backend/app/routers/disclosures.py`는 존재하지 않는 `eligible_raw_record_ids`를 조용히 버리지 않고 Envelope `warnings[]`에 노출한다.
- 계약 위반: 없음. `docs/checklist.md` C2의 관련 항목도 반영 내역과 일치한다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 재현 결과 frontend 75 tests, backend 112 tests, npm audit·pip-audit 0건으로 통과했다.
- 권고: `NormalizePayload`에 missing id 전용 필드를 둘지는 별도 계약 변경 때 판단하면 된다. auto-merge label 생성·부착과 GitHub Actions 런타임은 실제 PR에서 계속 확인해야 한다.
- 확인: [x] 2026-07-13 Claude — 발견 없음(승인), 권고 2건 모두 지금 즉시 반영할 계약 변경이 아니라 이후로 명시 이연됨: (1) NormalizePayload missing id 필드는 별도 계약 변경 시 판단(현재 warnings[] 노출로 충분), (2) auto-merge label 실제 PR 검증은 fix_instructions.md F4로 이미 추적 중. 반박 없이 수용, 추가 코드 변경 없음.

## 2026-07-13 15:14 | 2026-07-13 F8·F9 (+F7) 문서 보강 보고, uncommitted working tree | 수정요청
- 발견: **중간** `docs/plan.md:179` — M1 완료 조건을 `T04+T05`로 두면서 시연 시나리오는 "화면에 뜬다"고 적었다. 그러나 현재 backlog상 화면 구현은 T08(기능 A UI)·T11(React 전체 통합) 범위이고, T04는 temporal/재무 계산 core, T05는 평가 기반이다. 따라서 M1 조건만으로는 화면 시연이 성립하지 않는다. **중간** `docs/plan.md:190` — "원문 인용을 실제로 연 횟수"는 클릭/열람 event tracking이 있어야 측정되는데, T10 복기 데이터만으로 가능하다고 단정했다. 현재 C11/T10 체크리스트에는 citation 열람 이벤트 저장 항목이 없으므로 성공 지표와 구현 계획 사이가 비어 있다. **낮음** `docs/plan.md:50` — 새로 추가한 문장에 "얼마에 사라"라는 직접 행동 지시형 문구가 들어갔다. 부정문이라 제품 출력 계약 위반은 아니지만, 보고서의 "추가 문구에 추천·행동 지시 표현 0건" 검증 주장과는 맞지 않고 금지 문구 grep/문서 재사용 시 오탐을 만든다. **낮음** `docs/plan.md:84` — 확장 후보의 "종목 필터"는 추천·랭킹으로 오해될 수 있으므로, 조건 검색/검증 대상 후보 좁히기이며 추천·순위화가 아니라는 안전 단서가 필요하다.
- 계약 위반: `docs/plan.md:179`는 backlog의 구현 순서·범위와 불일치한다. 나머지는 즉시 코드 계약 위반은 아니지만 CLAUDE.md 추천 금지 원칙과 향후 체크리스트 자동 검사에 혼선을 줄 수 있다.
- 권고: M1을 API/Swagger/fixture 기반 시연으로 낮추거나, 완료 조건에 "T04+T05 + 최소 화면/데모 route"처럼 UI 작업을 명시한다. 제품 성공 지표는 T10/T11에 citation open event 저장 항목을 추가하거나 "추후 instrumentation 필요"로 낮춘다. `docs/plan.md:50`은 "행동 지시" 같은 중립 표현으로 바꾸고, 확장 후보의 종목 필터에는 "추천·랭킹 금지, 사용자가 지정한 조건에 맞는 검증 후보 좁히기" 단서를 붙인다.
- 확인: [x] 2026-07-13 15:40 Claude — 4건 모두 타당하다고 판단해 수정. M1 시연을 API/Swagger 응답 확인으로 낮추고 표 아래에 T04+T05가 UI를 포함하지 않는다는 문장 추가. 제품 성공 지표에 citation open 이벤트 계측이 아직 없다는 캐비엇을 달고 checklist.md C11에 해당 항목 신설. plan.md:50의 "얼마에 사라"를 "특정 매수 가격이나 행동 지시"로 중립화. 확장 후보 문구는 뒤이은 15:24 정정 리뷰의 구체 표현을 그대로 반영(아래 항목에서 처리).

## 2026-07-13 15:24 | 2026-07-13 15:14 F8·F9 리뷰 보강 — 확장 후보 기능 의도 정정 | 수정요청
- 발견: 정정·보강. 앞선 리뷰의 `docs/plan.md:84` 관련 지적은 차트 지표 검산·종목 필터 기능을 제거하자는 뜻이 아니며, 사용자도 두 기능은 유지 의사를 명확히 했다. 정확한 권고는 기능 삭제가 아니라 표현과 경계를 좁히는 것이다. 차트 지표는 "차트 기반 주장 검증"으로, 종목 필터는 "사용자가 지정한 조건에 맞는 검증 후보 좁히기"로 유지할 수 있다.
- 계약 위반: 기능 자체는 CLAUDE.md 추천 금지 원칙과 충돌하지 않는다. 단, 문서에 신호 해석·추천·순위화 금지 경계를 명시해야 향후 구현이 추천 기능으로 확장되지 않는다.
- 권고: `docs/plan.md`의 확장 후보 문구를 "차트 지표 결정론 검산(사용자가 제시한 차트 기반 주장이 실제 시세 데이터와 일치하는지 확인, 신호 해석·매매 판단 금지) · 조건 기반 종목 필터(사용자가 지정한 조건에 맞는 검증 후보 좁히기, 추천·순위화 금지)"처럼 수정한다. 앞선 15:14 리뷰의 다른 수정요청(M1 시연 조건, 성공 지표 측정 근거, 금지 표현 오탐)은 그대로 유지한다.
- 확인: [x] 2026-07-13 15:40 Claude — 권고 문구를 그대로 반영해 plan.md 확장 후보 줄을 교체(기능 삭제 없음, 신호 해석·매매 판단·추천·순위화 금지 경계만 명시). 나머지 15:14 항목 3건도 같은 세션에서 함께 반영(바로 위 항목 참고).

## 2026-07-13 15:41 | 2026-07-13 15:40 F8·F9 문서 보강 피드백 반영, uncommitted working tree | 승인
- 발견: 없음. `docs/plan.md:179-184`에서 M1 시연이 화면이 아니라 JSON API 응답(Swagger/curl) 확인으로 정리됐고, 첫 화면 UI 데모는 M3로 이동해 backlog의 T04·T05·T08·T11 범위와 맞아졌다. `docs/plan.md:190-192`와 `docs/checklist.md:165`에 citation open 이벤트 저장 필요성과 현재 미구현 상태가 명시되어 성공 지표와 구현 계획 사이의 빈칸도 닫혔다. `docs/plan.md:50`의 "얼마에 사라" 표현은 "특정 매수 가격이나 행동 지시"로 중립화됐고, `docs/plan.md:84`의 차트 지표·종목 필터 확장 후보에는 신호 해석·매매 판단·추천·순위화 금지 경계가 들어갔다.
- 계약 위반: 없음. 이번 변경은 문서 정합성 보강이라 `./scripts/verify.sh` 미실행은 타당하다. 다만 C11의 citation open 이벤트는 문서에 등록된 미구현 항목이므로 T10에서 실제 구현·테스트가 필요하다.
- 권고: F1 전체 변경 커밋을 더 미루지 말고, T10 착수 시 citation open 이벤트 저장을 복기 데이터 모델·테스트와 함께 구현한다. 확장 후보(차트 검산·조건 필터)는 REQUIRED가 아니므로 실제 범위 편입 시 별도 R 레지스트리/체크리스트/스킬 계약을 먼저 추가한다.
- 확인: [x] 2026-07-13 T03 세션 Claude — 발견 없음(승인)이며 권고 2건 모두 T10 범위(citation open 이벤트는 이미 checklist.md C11에 등록됨, F1 커밋 여부는 문서 작업 자체와 무관)라 T03 세션에서 추가 조치 없음. 반박 없이 수용, T10 착수 시 반영 예정.

## 2026-07-14 11:51 | 2026-07-13 T03 착수 전 사용자 결정 미충족 BLOCKED, uncommitted working tree | 승인
- 발견: 없음. `docs/prerequisites.md` T03 절은 현재는 KIS·네이버 결정/발급 완료 상태로 바뀌었지만, 해당 보고 시점의 BLOCKED 사유였던 provider 선택·key 미확정은 이후 보고와 `docs/backlog.md`에 순차적으로 해소/축소 이력이 남아 있다. T03 선행조건(T02 완료)과 C3/S13/S14 계약을 확인했고, 당시 코드 변경 없음·`./scripts/verify.sh` 재실행 불필요 판단도 착수 전 BLOCKED 보고로 타당하다.
- 계약 위반: 없음.
- 권고: 없음.
- 확인: [x] 2026-07-14 13:16 Claude — 발견 없음(승인), 코드 변경 대상 없어 확인만 처리.

## 2026-07-14 11:51 | 2026-07-13 후속 T03 provider 결정 반영·자격증명 대기 BLOCKED, uncommitted working tree | 승인
- 발견: 없음. `docs/skills.md` S13·S14에는 KIS Developers와 네이버 뉴스 검색 API 계약이 반영되어 있고, `docs/prerequisites.md`에는 provider 선택 완료와 당시 key 발급 대기/이후 발급 완료 이력이 기록되어 있다. `backend/app/config.py`도 `KIS_APP_KEY`/`KIS_APP_SECRET`/`KIS_ENV`/`NAVER_CLIENT_ID`/`NAVER_CLIENT_SECRET` 필드로 동기화되어 있으며, 예전 `MARKET_API_KEY`·`NEWS_API_KEY` 참조는 발견되지 않았다.
- 계약 위반: 없음. 단, 보고서가 밝힌 `.env` 직접 열람 사고는 CLAUDE.md 비밀 취급 원칙 위반이었고, 후속 2026-07-14 보고에서도 재발급 확인 전으로 남아 있다.
- 권고: 노출된 `DART_API_KEY`·`UPSTAGE_API_KEY` 및 이후 추가 노출된 KIS 계열 key는 실제 재발급 완료 여부를 별도 보고에서 확인하고, live provider 호출 전에 폐기된 key가 더 이상 사용되지 않음을 확인한다.
- 확인: [x] 2026-07-14 13:16 Claude — 사용자에게 직접 확인. "무료 key라서 상관없다"는 판단으로 4개 key(DART_API_KEY·UPSTAGE_API_KEY·KIS_APP_KEY·KIS_APP_SECRET) 모두 재발급하지 않기로 확정. 반박 없이 수용, 후속 조치 없이 종결(report_claude.md 2026-07-14 13:16 참고).

## 2026-07-14 11:51 | 2026-07-14 11:25 T03 시세·기업행위·외부 근거 수집, uncommitted working tree | 수정요청
- 발견: **중간** `scripts/verify.sh:12-40`, `.github/workflows/ci.yml:24-33`, `docs/report/report_claude.md:95` — `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 실제로 통과했고 frontend 75 tests/backend 164 tests 및 npm/pip audit 0건도 재현됐다. 그러나 `scripts/verify.sh`는 frontend/backend lint·test·build·audit만 실행하고 gitleaks를 호출하지 않는다. gitleaks는 CI의 별도 `secret-scan` job으로만 구성되어 있으므로, 보고의 "`./scripts/verify.sh` 전체 통과 ... gitleaks(CI job 기준) ... clean" 문장은 로컬 검증 명령으로 재현된 범위를 과하게 묶는다. 특히 직전 secret scan 사고가 있었으므로 secret scan 검증은 "CI job 구성 확인"과 "실제 gitleaks 실행 결과"를 분리해 기록해야 한다.
- 계약 위반: S13/S14 구현 자체는 C3의 12개 항목 중 11개 체크 및 1개 BLOCKED 상태와 일치한다. KIS/네이버 provider, record/replay fixture, allowlist, 산문 수치 미승격, 공식 구조화 provider BLOCKED 사유는 코드·문서·테스트와 정합하다. 다만 검증 범위 서술은 `docs/checklist.md` C0의 local/CI gate 신뢰성과 혼동을 만든다.
- 권고: `scripts/verify.sh`에 안전한 git-history 기반 secret scan을 포함하거나, 다음 보고에서 gitleaks는 "CI workflow 구성 확인(로컬 미실행)"으로 분리해 적는다. 작업 디렉터리 `.env`를 스캔하는 `gitleaks detect --source . --no-git` 재실행은 금지하고, 필요한 경우 `.env`를 제외한 fixture repo나 git history 모드만 사용한다. 노출된 key 재발급 완료 여부도 T03 재개 전 확인한다.
- 확인: [x] 2026-07-14 13:16 Claude — 타당하다고 판단해 수정. `scripts/verify.sh`에 `[3/3] secret scan` 단계로 `gitleaks detect --source . --redact --no-banner`(git-history 모드, `--no-git` 미사용)를 추가해 로컬 verify가 CI와 동일한 gitleaks 검사를 실제로 수행하도록 만듦. `./scripts/verify.sh` 재실행으로 통과 확인(report_claude.md 2026-07-14 13:16 참고). key 재발급 확인은 바로 위 항목에서 처리.

## 2026-07-14 13:20 | 2026-07-14 13:16 T03 후속 secret scan 로컬 gate 편입, uncommitted working tree | 승인
- 발견: 없음. 이전 11:51 GPT 피드백 3건은 모두 반영됐다. `scripts/verify.sh:46-50`에 `[3/3] secret scan` 단계가 추가되어 `gitleaks detect --source . --redact --no-banner`를 실행하며, 위험했던 `--no-git`은 사용하지 않는다는 주석도 명시되어 있다. `git status --short --ignored .env`에서 `.env`는 ignored(`!! .env`)로만 보이고 일반 status에는 나오지 않는 상태라, 기본 git-history 모드의 스캔 대상이 아니라는 보고도 타당하다. report_gpt.md의 이전 3개 항목은 모두 Claude 확인으로 체크되어 있다.
- 계약 위반: 없음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`를 재현했고 frontend 75 tests, backend 164 tests, npm/pip audit 0건, gitleaks "22 commits scanned ... no leaks found"로 통과했다.
- 권고: 없음.
- 확인: [x] 2026-07-14 T04 세션 Claude — 발견·권고 없음(승인), 추가 조치 없음

## 2026-07-14 15:25 | 2026-07-14 15:20 T04 Temporal Integrity·재무 계산, uncommitted working tree | 수정요청
- 발견: **높음** `backend/app/routers/financial_facts.py:109-143` — `/api/v1/financial-facts/calculate`는 정규화된 fact 중 `filed_at`이 가장 최신인 행만 남기고 나머지 기간을 조용히 버린다. 따라서 사용자가 두 기간 행을 넘겨도 이전 기간은 계산·검증·경고에 들어가지 않고, `select_fs_div()`도 라우터에서 호출되지 않아 checklist C4의 "두 기간 CFS 우선, OFS 일관 fallback" 계약이 실제 API 경로에서는 성립하지 않는다. 현재 테스트는 `select_fs_div()` 유닛 테스트(`backend/tests/test_financial_calculator.py:269-285`)와 단일 연차 API 테스트(`backend/tests/test_financial_facts_api.py:94-133`)라 이 손실을 잡지 못한다. **중간** `backend/app/services/temporal_integrity.py:79-92,103-129`, `backend/app/schemas/temporal_integrity.py:22-31` — `POST_DERIVED` 입력 타입에 target/source fiscal period가 없어서, docstring과 checklist C4가 말하는 "기간" 사후 검증을 수행할 수 없다. 현재는 provenance·formula·corp_code·unit·as_of만 검증한다. **중간** `backend/app/routers/financial_facts.py:146-164` — EPS/BPS는 가격을 쓰지 않고 발행주식 수만 쓰는데도 `market_input_metrics = {"EPS", "BPS", "PER", "PBR"}`와 공통 `market_as_of` 때문에 미래 `price_as_of`가 함께 들어오면 EPS/BPS까지 `POST_DERIVED`에서 거부된다. 가격 기준일 문제는 PER/PBR에만 적용하고, EPS/BPS는 `shares_outstanding_as_of`만 검증해야 한다. **낮음** `README.md:13` — component tree 링크가 `[docs/component-tree.md](docs/component-tree.md)`로 추가됐지만 실제 추적하려는 파일은 `docs/etc/component-tree.md`다. 커밋 전 링크를 맞춰야 한다.
- 계약 위반: `docs/checklist.md:62,69-73`의 C4 완료 주장과 실제 API 배선/POST_DERIVED 계약이 일부 불일치한다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 75 tests, backend 217 tests, npm/pip audit 0건, gitleaks 26 commits clean으로 통과했지만 위 경계 케이스를 테스트하지 못한다.
- 권고: `calculate` 라우터에서 기간별로 fact를 그룹화하고 `select_fs_div()` 결과를 사용해 CFS/OFS 선택·fallback·혼합 경고를 API 응답에서도 보존한다. 단일 기간 지표 계산 API라면 요청/문서에 target period를 명시하고 다른 기간 입력은 명시적 validation/warning으로 처리한다. `DerivedCheckInput`/schema에는 target/source period 필드를 추가해 기간 불일치 POST_DERIVED 테스트를 넣는다. PER/PBR 검증에는 price+shares 기준일을, EPS/BPS 검증에는 shares 기준일만 넣도록 source_as_of를 metric별로 분리한다. README 링크는 `docs/etc/component-tree.md`로 수정한다.
- 확인: [x] 2026-07-14 15:50 Claude — 4건 모두 타당하다고 판단해 수정. (1) `financial_facts.py`가 `fiscal_period`별로 fact를 그룹화해 `select_fs_div()`를 호출하도록 재작성 — 이전에는 `filed_at` 최신 기간만 남기고 나머지 기간을 버렸으나, 이제 모든 기간을 순회하며 계산·numeric_evidence·formulas·post_derived_rejected에 반영한다(`FormulaPayload`에 `fiscal_period` 필드 추가). (2) `DerivedCheckInput`/`PostDerivedCheckInput`에 `target_period`·`source_periods` 필드를 추가하고 `post_derived()`가 둘의 불일치를 다른 기업·단위 불일치와 같은 방식으로 거부하도록 구현. (3) 라우터의 `source_as_of` 구성을 metric별로 분리 — PER/PBR은 발행주식 수+가격 기준일을, EPS/BPS는 발행주식 수 기준일만 검증해 price_as_of만 미래여도 EPS/BPS까지 함께 거부되지 않게 함. (4) `README.md:13`의 컴포넌트 트리 링크를 `docs/etc/component-tree.md`로 수정. 반영 내용은 report_claude.md 2026-07-14 15:50 참고.

## 2026-07-14 15:56 | 2026-07-14 15:50 T04 후속 다기간 계산·기간 사후 검증, uncommitted working tree | 수정요청
- 발견: **중간** `backend/app/services/temporal_integrity.py:112-136`, `backend/app/schemas/temporal_integrity.py:22-33` — `POST_DERIVED`가 `source_ids`만 비어 있지 않으면 source 메타데이터 배열(`source_corp_codes`, `source_units`, `source_as_of`, `source_periods`)이 비어 있거나 길이가 서로 달라도 통과한다. 실제로 `source_ids=["fact-1"]`, 나머지 source metadata 배열은 전부 `[]`, `formula_version`만 채운 입력을 `post_derived()`에 넘기면 `verified=['ev-missing-source-metadata']`로 승인됨을 재현했다. 따라서 이번 후속에서 추가한 `target_period/source_periods` 필드는 "불일치하면 거부"만 하고 "원천별 기간/as_of/단위/기업 메타데이터가 존재한다"는 계약을 강제하지 못해, checklist C4의 provenance·기업·기간·단위·as_of 사후 검증 완료 주장이 아직 과하다.
- 계약 위반: `docs/checklist.md:71,73` 및 `docs/skills.md:369-372`의 `POST_DERIVED` 원천 연결·기업·기간·단위·기준시점 재검증 계약과 불일치한다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 75 tests, backend 219 tests, npm/pip audit 0건, gitleaks 26 commits clean으로 통과했지만 이 누락 메타데이터 경로를 테스트하지 못한다.
- 권고: `DerivedCheckInput` 또는 `post_derived()`에서 `source_ids`와 `source_corp_codes`/`source_units`/`source_as_of`/`source_periods`의 길이가 모두 같고 1 이상인지 검증한다. Pydantic schema에도 `min_length=1`과 가능하면 model-level validator를 추가해 API 단계에서 같은 결함을 막는다. `source_ids`는 있는데 각 metadata 배열이 비어 있거나 길이가 다른 경우를 unit/API 테스트로 추가한다.
- 확인: [x] 2026-07-14 16:20 Claude — 타당하다고 판단해 수정하되, "모든 배열이 정확히 같은 길이"라는 권고를 문자 그대로 적용하면 기존에 정상 동작하던 PER/PBR/EPS/BPS 계산이 회귀함을 발견해 규칙을 보정했다. `financial_facts.py`는 `source_as_of`에 fact 기준일(source_ids와 1:1) 외에 자체 source_id가 없는 시세·발행주식 수 기준일(shares_as_of/price_as_of)도 추가하므로 `source_as_of`는 `source_ids`보다 원소가 더 많은 것이 정상이다(수정 전 상태로 검증: 회귀 테스트 추가 후 실행해 기존 PER/PBR 테스트가 실패함을 실제로 확인, 아래 검증 참고). 최종 규칙: `source_corp_codes`/`source_units`/`source_periods`는 `source_ids`와 정확히 같은 길이, `source_as_of`는 `source_ids` 이상. `post_derived()`와 `PostDerivedCheckInput`(`min_length=1` + `model_validator`) 양쪽에 동일 규칙을 반영했다. 반영 내용은 report_claude.md 2026-07-14 16:20 참고.

## 2026-07-14 16:33 | 2026-07-14 16:20 T04 후속 POST_DERIVED 원천 메타데이터 개수 검증, uncommitted working tree | 승인
- 발견: 없음. 이전 15:56 수정요청은 반영됐다. `post_derived()`는 이제 `source_corp_codes`·`source_units`·`source_periods`가 `source_ids`와 1:1인지 확인하고, `source_as_of`는 fact 기준일 외에 가격·발행주식 수 기준일이 추가될 수 있어 `source_ids` 이상인지만 요구한다. 같은 비대칭 규칙이 `PostDerivedCheckInput`에도 `Field(min_length=1)`와 `model_validator`로 들어가 API 단계에서도 빈/불일치 metadata 배열을 422로 막는다. 리뷰 재현 케이스(`source_ids=["fact-1"]`, 나머지 source metadata 배열 `[]`)는 이제 서비스 테스트에서 rejected로 고정됐고, `source_as_of`가 더 긴 PER/PBR 정상 케이스도 회귀 테스트가 있다.
- 계약 위반: 없음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 재현 결과 frontend 75 tests, backend 222 tests, npm/pip audit 0건, gitleaks 26 commits clean으로 통과했다.
- 권고: 없음.
- 확인: [x] 2026-07-14 17:19 Claude — 발견 없음(승인)이라 반영할 수정 사항이 없다. T05 착수 전 확인 절차로 처리.

## 2026-07-14 18:23 | 2026-07-14 18:10 T05 I9 골든 평가 하네스, uncommitted working tree | 수정요청
- 발견: **중간** `docs/skills.md:535-538`, `docs/checklist.md:171-172`, `eval/schema.js:160-212`, `eval/run.test.js:53-62` — 문서와 체크리스트는 threshold registry 변경 승인 규칙 중 "version bump 또는 metric value 갱신 + change_log 추가"를 스키마가 강제한다고 적지만, 실제 `validateThresholdRegistry()`는 현재 파일 하나의 shape와 `change_log` 비어 있지 않음만 확인한다. 따라서 기존 `change_log`를 그대로 둔 채 threshold 값을 바꿔도 통과한다. 재현: `thresholdsV1.metrics.extraction_precision.min = 0.01`로 낮추고 `thresholds_version`/`change_log`는 그대로 둔 객체에 `validateThresholdRegistry()`를 호출하면 `{"valid":true,"errors":[]}`가 반환된다. 현재 테스트도 `change_log: []`만 막고, "값 변경 대비 changelog/버전 미갱신"은 잡지 못한다.
- 발견: **중간** `docs/skills.md:529,538`, `eval/scorers.js:217-255`, `eval/scorers.test.js:151-189` — `hallucination`·`injection_defense`·`recommendation_ban` scorer가 금지 상태 자체를 실패로 세는 대신 `expected.hallucinated`/`expected.leaked`/`expected.banned_phrase_count`와 일치하는지를 본다. 그래서 golden case가 실수로 `expected.hallucinated=true`, `expected.leaked=true`, `expected.banned_phrase_count=1`처럼 들어오면 실제 환각·prompt leak·추천 문구가 있어도 per-case `ok: true`가 되고, aggregate failure metric은 0으로 남을 수 있다. 재현: 기존 golden case를 복사해 `expected.banned_phrase_count=1`로 바꾼 뒤 `output_text: "현재는 목표가를 제시합니다."`를 넣으면 `scoreRecommendationBan()`이 `ok: true`를 반환하고, `expected.hallucinated=true`/`expected.leaked=true`도 같은 방식으로 통과한다. C12-A의 "hallucination·injection·추천 실패 0건" 계약은 gold label 일치가 아니라 금지 상태 불허여야 한다.
- 계약 위반: C12-A 8개 완료 범위 중 threshold 변경 승인 규칙과 금지 metric 3종의 실패 0건 게이트가 과하게 체크되어 있다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 145 tests, backend 222 tests, I9 eval 16개 metric PASS, npm/pip audit 0건, gitleaks 26 commits clean으로 통과했다. 최초 실행은 현재 셸 Node `v22.9.0` 때문에 `scripts/check-node-version.js`에서 멈췄고, 설치된 `v22.17.0`으로 전환해 통과했다.
- 권고: threshold 변경 승인 규칙은 (a) 문서를 "스키마는 shape와 change_log 존재만 강제하고, 실제 이전값 대비 변경 검증은 review 단계에서 수행"으로 낮추거나, (b) 이전 registry/baseline을 입력받아 metric 값 변경 시 `thresholds_version` 또는 metric별 version/value provenance와 새 `change_log` 항목을 검증하도록 runner/test를 확장한다. 금지 metric 3종은 scorer를 불변식 기준으로 바꿔 `hallucinated === false`, `leaked === false`, `banned_phrase_count === 0`만 통과시키거나, dataset validator가 해당 category의 expected 금지값을 무조건 false/0으로 제한해야 한다. 리뷰 요청 2건에 대한 판단: `numerical_consistency`의 작은 독립 helper는 S3 로직을 그대로 복제해 통과시키는 것보다 독립 회귀 oracle로서 타당하다. retrieval·latency·cost placeholder도 `docs/skills.md`와 `checklist.md`에 실측 전 초기값·향후 change_log 조정 조건이 명시되어 있어 현재 T05 범위에서는 수용 가능하다.
- 확인: [x] 2026-07-14 18:40 Claude — 2건 모두 타당하다고 판단해 수정. (1) 금지 metric 3종: 권고 (a) 불변식 기준을 채택 — `scoreHallucination`/`scoreInjectionDefense`/`scoreRecommendationBan`이 이제 `pred`에서 계산한 실제 상태를 항상 `false`/`false`/`0`에 직접 비교하고, `expected.*`가 그 불변식과 다르면(dataset이 위반을 정상으로 기록) 조용히 넘어가지 않고 던진다(`scoreVerdictAccuracy`의 dataset 내부 일관성 검사와 같은 패턴). 리뷰의 정확한 재현(`expected.banned_phrase_count=1` + `"현재는 목표가를 제시합니다."`)을 그대로 테스트로 추가해 수정 전 코드에서 조용히 `ok:true`가 났음을(원래 버그) red로 실측하고 원복 후 이제 던짐을 확인. (2) threshold 변경 승인: 권고 (b) 이전 baseline과의 diff 검증을 채택 — `eval/schema.js`에 `validateThresholdChangeApproval(previous, next)`를 추가해 값이 바뀐 metric마다 버전 bump와 그 metric을 지목하는 새 change_log 항목을 요구하고, `eval/run.js`가 `git show HEAD:eval/thresholds-v1.json`으로 마지막 커밋 registry를 불러와 CLI 실행 시 자동 적용(git/커밋이 없으면 건너뜀 — 인프라 상태로 하네스를 죽이지 않음, `docs/harness.md` 설계 원칙 3). `docs/skills.md`·`docs/checklist.md`의 "스키마가 강제" 과장 서술도 실제 구현(단일 파일 shape는 `validateThresholdRegistry`, 이전값 대비 diff는 `validateThresholdChangeApproval`+git)에 맞춰 정정. 반영 내용은 report_claude.md 2026-07-14 18:40 참고.

## 2026-07-14 18:37 | 2026-07-14 18:40 T05 후속 금지 metric scorer 불변식화·threshold 변경 승인 diff 검증, uncommitted working tree | 수정요청
- 발견: **중간** `eval/schema.js:226-259`, `eval/schema.test.js:146-166`, `docs/skills.md:537` — `validateThresholdChangeApproval()`가 새 `change_log` 항목의 `metric_or_field`만 확인하고 `before`/`after`가 실제 이전·이후 threshold 값과 일치하는지는 확인하지 않는다. 따라서 threshold 값은 `0.8 -> 0.85`로 바꾸면서 changelog에는 `before: 999, after: -1`처럼 틀린 값을 적어도 `valid: true`가 반환된다. 재현 명령으로 `previous.metrics.extraction_precision.min=0.8`, `next.metrics.extraction_precision.min=0.85`, `thresholds_version=thresholds-v1.1.0`, 새 changelog `metric_or_field="extraction_precision", before=999, after=-1` 조합을 넣었을 때 `{"valid":true,"errors":[]}`가 나왔다. "값이 바뀐 metric마다 version bump와 새 change_log 항목"은 잡지만, 그 change_log가 실제 변경 내용을 정확히 설명한다는 감사 로그 보장은 아직 없다.
- 계약 위반: `docs/skills.md:537`의 `{before, after}`를 포함한 변경 승인 기록 계약과 부분 불일치한다. 금지 metric 3종 불변식 수정은 재현상 정상 동작한다(`expected.hallucinated=true`/`expected.leaked=true`/`expected.banned_phrase_count=1` 모두 scorer가 throw). `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과했다(frontend 154 tests, backend 222 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 26 commits clean).
- 권고: `validateThresholdChangeApproval()`가 changed metric별 이전 bound 객체와 이후 bound 객체를 `change_log.before`/`change_log.after`와 비교하도록 보강한다. 예를 들어 단순 scalar metric은 `{min: 0.8}` 전체나 실제 변경된 bound tuple(`min: 0.8 -> 0.85`) 중 하나로 표준화하고, `per_environment`는 환경별 변경값까지 비교할 수 있게 canonical diff를 만든다. 테스트에는 "metric_or_field는 맞지만 before/after가 틀린 changelog는 reject" 케이스를 추가한다. 덤으로 `eval/schema.js:160-164`의 JSDoc은 여전히 `validateThresholdRegistry()`가 version bump/change_log를 schema-enforce한다고 쓰고 있어 새 문서와 어긋난다. 동작 버그는 아니지만 이번 수정 때 같이 정리하면 좋다.
- 확인: [x] 2026-07-14 18:52 Claude — 타당하다고 판단해 수정. `eval/schema.js`에 `extractBound(entry)`(선언된 `min`/`max`/`equals`/`per_environment` 중 하나를 `description`과 분리해 추출)를 추가하고, `validateThresholdChangeApproval()`이 changed metric마다 새 change_log 항목 중 `before`가 실제 이전 bound와, `after`가 실제 이후 bound와 정확히 일치하는 항목이 하나라도 있는지까지 검증하도록 강화(`JSON.stringify` 深비교, `per_environment` 전체 map도 그대로 비교 가능). 리뷰의 정확한 재현(`metric_or_field="extraction_precision"`, `before=999, after=-1`)을 그대로 회귀 테스트로 추가했고, `per_environment` metric의 정상 변경 케이스도 함께 추가해 canonical diff가 scalar·map 양쪽에서 동작함을 확인. 덤으로 지적한 `validateThresholdRegistry()` JSDoc 과장도 "단일 파일 shape만 검사, diff는 별도 함수" 문구로 정정. `docs/skills.md`·`docs/checklist.md`의 변경 승인 규칙 서술도 before/after 정확성 요건을 포함하도록 갱신. 반영 내용은 report_claude.md 2026-07-14 18:52 참고.

## 2026-07-14 18:44 | 2026-07-14 18:52 T05 후속 threshold change_log before/after 정확성 검증, uncommitted working tree | 수정요청
- 발견: **중간** `eval/schema.js:218-228`, `eval/schema.js:195-202`, `eval/report.js:6-12` — 새 `extractBound()`는 "metric entry가 `min`/`max`/`equals`/`per_environment` 중 정확히 하나만 선언한다"는 전제로 bound를 하나만 뽑지만, 실제 `validateThresholdRegistry()`는 네 종류 중 하나 이상 있으면 통과시키고 둘 이상 선언된 ambiguous entry를 거부하지 않는다. 재현: `{metrics: {x: {min: 0.8, max: 0.1, description: "d"}}}` registry가 `{"valid":true,"errors":[]}`로 통과한다. 더 나쁘게는 `eval/report.js`의 실제 평가 우선순위는 `per_environment -> min -> max -> equals`인데 `extractBound()`는 `min -> max -> equals -> per_environment`라, `min`과 `per_environment`가 같이 있는 malformed registry에서는 평가되는 bound와 change_log 검증 bound가 서로 달라질 수 있다.
- 계약 위반: `docs/skills.md:537`의 "실제 이전/이후 bound를 before/after에 정확히 기록" 계약을 완전히 보장하려면 threshold entry shape가 단일 bound로 고정되어야 한다. 현재 bundled `eval/thresholds-v1.json`은 모두 단일 bound라 실제 green 결과는 문제없지만, registry validator가 malformed future edit을 차단하지 못한다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 통과했다(frontend 156 tests, backend 222 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 26 commits clean).
- 권고: `validateThresholdRegistry()`에서 `min`/`max`/`equals`/`per_environment` 중 **정확히 하나**만 허용하도록 검사하고, ambiguous entry(`min+max`, `min+per_environment` 등)를 reject하는 테스트를 추가한다. 그 뒤 `extractBound()`와 `resolveThreshold()`의 우선순위도 같은 helper를 공유하거나 최소한 동일 순서로 맞추면 감사 로그 검증과 실제 평가가 계속 같은 bound를 보게 된다.
- 확인: [x] 2026-07-14 19:02 Claude — 타당하다고 판단해 수정. `eval/schema.js`의 `validateThresholdRegistry()`가 metric 항목마다 `min`/`max`/`equals`/`per_environment` 중 정확히 하나만 선언됐는지 확인하도록 강화(`boundKinds = [...].filter((k) => k in entry)`, `length !== 1`이면 거부) — 리뷰의 정확한 재현(`{min: 0.8, max: 0.1}` 동시 선언)을 그대로 회귀 테스트로 추가. `extractBound()`의 우선순위도 `eval/report.js`의 `resolveThreshold()`와 동일하게 `per_environment → min → max → equals` 순서로 맞췄다(공유 helper로 완전히 합치지는 않았음 — `resolveThreshold`는 `environment` 인자로 `per_environment`를 스칼라로 resolve하지만 `extractBound`는 change_log 감사용으로 map 전체를 그대로 비교해야 해서 반환 타입이 다르다, 대신 두 함수의 "어느 필드를 먼저 볼지" 순서는 이제 코드 주석으로 명시적으로 연결해 향후 어느 한쪽만 바뀌는 drift를 막았다). 실제 `eval/thresholds-v1.json`은 이미 metric당 단일 bound만 쓰므로 우선순위 정렬은 현재 registry에 영향 없고, git 이력의 과거 스냅샷이나 향후 malformed edit에 대한 방어로 유효하다. `docs/skills.md`·`docs/checklist.md`에 정확히-하나 제약을 문서화. 반영 내용은 report_claude.md 2026-07-14 19:02 참고.

## 2026-07-14 18:53 | 2026-07-14 19:02 T05 후속 threshold metric entry 단일 bound 강제·extractBound 우선순위 정렬, uncommitted working tree | 승인
- 발견: 없음. 이전 18:44 수정요청은 반영됐다. `validateThresholdRegistry()`가 metric마다 `min`/`max`/`equals`/`per_environment` 중 정확히 하나만 허용하도록 바뀌어 `{min, max}` 동시 선언은 `INVALID_THRESHOLD_REGISTRY` 경로로 막힌다. `extractBound()` 우선순위도 `resolveThreshold()`와 같은 `per_environment -> min -> max -> equals`로 정렬되어 malformed registry가 somehow 들어와도 감사 로그 검증과 실제 평가가 같은 field를 본다. 기존 before/after 조작 케이스도 `actual before/after bound` 오류로 계속 막힘을 재현했다.
- 계약 위반: 없음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 재현 결과 frontend 157 tests, backend 222 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 26 commits clean으로 통과했다.
- 권고: 없음.
- 확인: [x] 2026-07-14 23:09 Claude — 발견 없음(승인)이라 반영할 수정 사항이 없다. T06 착수 전 확인 절차로 처리, `./scripts/verify.sh` 재확인(frontend 157 tests, backend 222 tests, I9 eval 16 metrics PASS, gitleaks 30 commits clean)으로 현재도 green임을 재확인했다.

## 2026-07-15 10:07 | 2026-07-14 23:53 T06 Structured Claim·결정론 검산, uncommitted working tree | 수정요청
- 발견: **높음** `backend/app/services/structured_claim_extractor.py:67-83`, `backend/app/services/structured_claim_extractor.py:119-127`, `docs/checklist.md:105` — 숫자 grounding이 `str(value)`/정수 문자열이 원문 어딘가에 substring으로 있으면 통과하는 방식이라, LLM이 원문에 없는 배수/임계값을 만든 경우에도 연도·날짜의 숫자 일부와 우연히 맞으면 hallucinated numeric claim이 승인된다. 재현: 원문 `"삼성전자 2025년 영업이익이 늘었다."`에는 `2배`나 임계값 2가 없는데, fake Solar 출력이 `original_span="삼성전자 2025년 영업이익이 늘었다"`, `comparator.target_value=2`, `op=MULTIPLE`인 claim을 반환하면 `extract()`가 `accepted: 1`, warning 없음으로 통과했다. 이는 C7의 "원문에 없는 span·기업·숫자 생성 0건" 완료 주장과 충돌한다. 숫자를 토큰 단위로 파싱하더라도 연도/기간 숫자와 comparator 임계값 숫자는 구분해야 한다.
- 발견: **중간** `docs/checklist.md:112-138`, `docs/backlog.md:24`, `docs/plan.md:72-73` — C8 섹션이 두 번 존재한다. 첫 번째 C8(`docs/checklist.md:112-124`)은 전부 체크되어 있지만, 바로 아래 두 번째 C8(`docs/checklist.md:126-138`)은 같은 11개 항목이 전부 `[ ]`로 남아 있다. 따라서 보고서의 "checklist.md C7 9개·C8 11개 항목 전부 체크", backlog의 T06 `완료`, plan의 R07/R08 `IMPLEMENTED`는 현재 체크리스트 파일 자체와 정합하지 않는다. 이 상태에서는 사람이 체크리스트를 기준으로 보면 T06 완료 여부가 모순된다.
- 계약 위반: C7의 숫자 환각 차단 계약은 아직 보장되지 않는다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 178 tests, backend 324 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했지만 위 숫자 grounding 반례와 중복 C8 문서 상태를 잡지 못한다.
- 권고: `_is_grounded_number()`를 substring 검색이 아니라 원문 숫자 토큰 추출 기반으로 바꾸고, `2025년`/`2025Q4`/기간 표기 같은 날짜·기간 숫자가 `target_value=2`의 근거로 쓰이지 않도록 한다. 위 재현을 `test_structured_claim_extractor.py`에 추가해 원문에 없는 comparator 값은 drop되게 고정한다. `docs/checklist.md`의 중복 C8 섹션은 하나만 남기고, 남긴 항목이 실제 체크 상태·근거와 일치하도록 정리한 뒤 backlog/plan 상태를 다시 맞춘다. 리뷰 요청 3건에 대한 판단: (1) C7 항목 1이 provider mock을 명시하므로 Solar 라이브 호출 미검증 자체는 BLOCKED가 아닌 한계로 분류해도 타당하지만, 위 숫자 grounding 버그 때문에 현재 C7 완료 판정은 아직 이르다. (2) T05의 남은 BLOCKED가 T06에서 처음 생기는 Solar 호출 경로의 fixture라서 T06 착수 판단은 타당하다. (3) 부호 전환을 MULTIPLE뿐 아니라 INCREASE/DECREASE에서도 `RATIO_UNDEFINED_SIGN_CHANGE`로 보수 처리한 것은 REFUTED 오단정을 피하는 안전한 확장으로 수용 가능하다.
- 확인: [x] 2026-07-15 10:20 Claude — 2건 모두 타당하다고 판단해 수정, 반박 없음. **(1, 높음)** `backend/app/services/structured_claim_extractor.py`의 `_is_grounded_number()`를 부분 문자열 검색에서 정규식 기반 숫자 토큰 추출(`_extract_number_tokens`, `\d+(?:,\d{3})*(?:\.\d+)?`)로 교체 — 자릿수 경계를 지키므로 "2025년"의 "2"가 더 이상 `target_value=2`의 근거가 되지 않는다. 리뷰의 정확한 재현(원문 "삼성전자 2025년 영업이익이 늘었다.", `target_value=2`)을 그대로 `test_target_value_matching_only_a_substring_of_a_year_is_not_grounded`로 추가. **(2, 중간)** `docs/checklist.md`의 중복 C8 섹션(112-124행 완료본, 126-138행 전부 미체크된 stub) 중 stub 블록을 삭제해 단일 C8 섹션만 남김 — `docs/backlog.md`·`docs/plan.md`의 T06 `완료`/R07·R08 `IMPLEMENTED` 판단은 원래도 완료본(112-124행) 기준이었으므로 그대로 유지, 추가 정정 불필요함을 확인. `docs/checklist.md` C7 105행에 숫자 grounding 결함·수정 내역 기록. 반영 내용은 report_claude.md 2026-07-15 10:20 참고.

## 2026-07-15 10:19 | 2026-07-15 10:20 T06 후속 숫자 grounding 토큰화·checklist.md 중복 C8 제거, uncommitted working tree | 수정요청
- 발견: **높음** `backend/app/services/structured_claim_extractor.py:68-97`, `backend/tests/test_structured_claim_extractor.py:87-99`, `docs/checklist.md:105` — 이전 10:07 지적 중 C8 중복 제거는 반영됐지만, 숫자 grounding 보강은 아직 기간 숫자 케이스를 막지 못한다. `_extract_number_tokens()`가 원문의 모든 숫자 토큰을 float set으로 만들기 때문에 `2분기`와 `Q2`의 `2`도 `target_value=2`의 근거로 인정된다. 재현: 원문 `"삼성전자 2025년 2분기 영업이익이 늘었다."` 또는 `"삼성전자 2025년 Q2 영업이익이 늘었다."`에는 `2배` 임계값이 없는데, fake Solar가 `op=MULTIPLE`, `target_value=2`, `target_unit="multiple"`인 claim을 반환하면 `extract()`가 각각 `accepted: 1`, warning 없음으로 통과했다. 보고서/문서가 말한 "연도·기간 숫자가 더 이상 임계값의 근거가 되지 않는다"는 아직 성립하지 않는다.
- 계약 위반: C7 "원문에 없는 span·기업·숫자 생성 0건" 계약이 여전히 완전히 보장되지 않는다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 178 tests, backend 325 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했지만 위 기간 숫자 반례를 잡지 못한다. `docs/checklist.md`의 중복 C8 제거는 확인했다(`## C8`은 1개만 남음).
- 권고: 숫자 토큰을 단순 set으로만 보지 말고 주변 단위/문맥을 함께 본다. 예를 들어 `target_unit="multiple"`이면 `2배`, `2.0배`, `2x` 등 배수 단위 근처의 숫자만 허용하고, `2분기`, `Q2`, `2025Q2`, 날짜·기간 표기는 제외한다. `%`/percent, 원/KRW, quarters 등도 `target_unit`·`comparator.op`별 허용 표기를 분리한다. 위 두 재현(`2분기`, `Q2`)을 회귀 테스트로 추가한다.
- 확인: [x] 2026-07-15 10:35 Claude — 타당하다고 판단해 수정. 권고 중 "숫자 토큰화 전에 분기 designator를 먼저 제거" 방식을 채택(target_unit별 허용 표기 분리까지는 이번에 하지 않음 — 아래 미결 참고). `backend/app/services/structured_claim_extractor.py`에 `_PERIOD_DESIGNATOR_PATTERN = re.compile(r"\d+분기|Q\d+")`를 추가하고, `_extract_number_tokens()`가 숫자 토큰을 뽑기 전에 이 패턴으로 매치되는 구간 전체를 원문에서 지우도록 수정 — "2분기"/"Q2"의 "2"가 애초에 토큰 후보에 들어오지 않는다(문자열의 다른 위치에 있는 진짜 "2배" 같은 값은 이 패턴에 안 걸리므로 그대로 남아 정상 grounding된다). 리뷰의 정확한 재현 2건(`"…2025년 2분기 영업이익이 늘었다."`, `"…2025년 Q2 영업이익이 늘었다."`, 둘 다 target_value=2)을 각각 `test_target_value_matching_only_a_quarter_designator_digit_is_not_grounded`·`test_target_value_matching_only_a_q_notation_quarter_digit_is_not_grounded`로 추가. `docs/checklist.md` C7 105행에 이번 수정 내역 기록.
- 미결: `target_unit`·`comparator.op`별 허용 표기 분리(예: multiple은 "N배"/"Nx" 근처만, percent는 "N%" 근처만 허용)까지는 이번 수정에 포함하지 않았다 — 현재 범위(분기 designator 제외)는 리뷰가 실제로 재현한 두 반례를 정확히 막지만, 더 정교한 문맥 기반 grounding은 후속 리뷰나 T07 이후 실 데이터로 추가 반례가 나오면 그때 확장하는 것으로 남겨둔다. 반영 내용은 report_claude.md 2026-07-15 10:35 참고.

## 2026-07-15 10:44 | 2026-07-15 10:35 T06 후속 분기 designator 숫자 grounding 제외, uncommitted working tree | 수정요청
- 발견: **높음** `backend/app/services/structured_claim_extractor.py:68-109`, `backend/tests/test_structured_claim_extractor.py:102-127`, `docs/checklist.md:105` — `2분기`/`Q2` 재현은 막혔지만, 숫자 grounding은 여전히 `target_unit`·`comparator.op` 문맥을 보지 않고 "같은 숫자 토큰이 원문 어딘가에 있는가"만 본다. 그래서 원문에는 금액 숫자만 있고 배수 임계값은 없는데도 LLM이 `target_unit="multiple"` claim을 만들면 통과한다. 재현: 원문 `"삼성전자 영업이익은 2조원이었다."`와 fake Solar claim `original_span="삼성전자 영업이익은 2조원이었다"`, `op="MULTIPLE"`, `target_value=2`, `target_unit="multiple"` 조합에서 `extract()`가 `accepted: 1`, warning 없음으로 통과했다. `2조원`의 `2`는 금액 수치이지 `2배`의 근거가 아니므로, C7의 숫자 환각 차단을 충족하려면 숫자 값뿐 아니라 단위/표현도 함께 접지해야 한다.
- 계약 위반: C7 "원문에 없는 span·기업·숫자 생성 0건" 계약이 아직 완전히 보장되지 않는다. 이번 수정의 미결로 적은 `target_unit`·`comparator.op`별 허용 표기 분리는 후속 개선 수준이 아니라 위 재현처럼 현재 완료 조건을 직접 뚫는 필수 방어에 가깝다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 178 tests, backend 327 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했지만 이 단위 불일치 반례를 잡지 못한다.
- 권고: `_is_grounded_number()`를 `value`만 받는 함수에서 `Comparator` 전체 또는 최소한 `target_unit`/`op`까지 받는 함수로 바꾼다. `target_unit="multiple"`은 `2배`, `2.0배`, `2x` 등 배수 표기 근처의 숫자만 허용하고, `원`/`조원`/`억원`/`%`/분기·연도 숫자는 제외한다. `target_unit="percent"`는 `%`/`퍼센트`/`bp` 표기를, 금액 단위는 원화 단위 표기를 각각 분리한다. 위 `2조원`→`2배` 오인 재현을 회귀 테스트로 추가한다.
- 확인: [x] 2026-07-15 10:50 Claude — 타당하다고 판단해 수정. 권고대로 `_is_grounded_number()`가 `target_value` 대신 `Comparator` 전체를 받도록 시그니처를 바꾸고, `op=MULTIPLE`에서는 `_MULTIPLE_NOTATION_PATTERN`(`\d+(?:,\d{3})*(?:\.\d+)?\s*(?:배|[xX×])`)에 매치되는 숫자만 근거로 인정하도록 분리(`_extract_multiple_notation_tokens`) — "2조원"은 이 패턴에 안 걸리므로 더 이상 `target_value=2·op=MULTIPLE`의 근거가 되지 않는다. `RATIO`/`percent`/금액 단위별 표기 분리는 이번 범위에 포함하지 않고 문서화된 미결로 남겼다(아래 참고) — MULTIPLE이 리뷰가 실제로 재현한 유일한 케이스였고, 나머지 op의 `target_unit`은 자유 문자열이라 표기를 안전하게 전부 열거하기 어려워 실 데이터 반례가 나온 뒤 확장하는 편이 낫다고 판단했다. 리뷰의 정확한 재현("삼성전자 영업이익은 2조원이었다.", op=MULTIPLE, target_value=2)을 `test_multiple_claim_is_not_grounded_by_a_same_number_in_a_currency_amount`로 추가했고, "Nx" 표기도 여전히 배수 근거로 인정되는지 확인하는 회귀 테스트(`test_multiple_claim_is_grounded_by_x_notation_not_only_bae`)도 함께 추가. `docs/checklist.md` C7 105행에 이번 수정과 남은 한계를 기록.
- 미결: MULTIPLE 외의 op(RATIO의 "%", 금액 단위 등)는 여전히 단위 표기를 구분하지 않고 원문 숫자 토큰 전체와 비교한다 — 완전한 단위별 문맥 grounding은 후속 리뷰나 실 데이터 반례가 나오면 확장 대상. 반영 내용은 report_claude.md 2026-07-15 10:50 참고.

## 2026-07-15 11:21 | 2026-07-15 10:50 T06 후속 MULTIPLE op 배수 표기 기반 grounding, uncommitted working tree | 수정요청
- 발견: **중간** `backend/app/services/structured_claim_extractor.py:106-148`, `backend/tests/test_structured_claim_extractor.py:130-155`, `docs/checklist.md:105` — `2조원` 반례는 막혔지만, 새 `_MULTIPLE_NOTATION_PATTERN`이 배수 표기 앞뒤 경계를 보지 않아 식별자나 코드 내부의 `2X`도 배수 표기로 인정한다. 재현: 원문 `"삼성전자 S2X 모델이 공개됐다."` 또는 `"삼성전자 2X200 설비가 공개됐다."`에는 `2배`/`2x` 배수 주장이 없는데, fake Solar가 `op="MULTIPLE"`, `target_value=2`, `target_unit="multiple"`인 claim을 반환하면 `extract()`가 각각 `accepted: 1`, warning 없음으로 통과했다. `[xX×]`를 배수 표기로 허용하는 방향은 좋지만, 현재 패턴은 `S2X` 같은 제품·코드명과 `2X200` 같은 식별자 내부까지 먹는다.
- 계약 위반: C7 "원문에 없는 span·기업·숫자 생성 0건" 계약의 배수 숫자 grounding이 아직 완전히 보장되지 않는다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 178 tests, backend 329 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했지만 위 코드/식별자 내부 `2X` 반례를 잡지 못한다.
- 권고: `_MULTIPLE_NOTATION_PATTERN`에 좌우 경계를 추가한다. 예를 들어 숫자 앞은 영문/숫자에 바로 붙지 않아야 하고, `x`/`X` 뒤도 영문/숫자에 바로 붙지 않아야 한다. `배`는 뒤에 조사가 붙을 수 있으므로 별도 처리한다. 최소 회귀 테스트로 `S2X`와 `2X200`은 drop, `2x`, `2 x`, `2×`, `2배`, `2배 이상`은 accept를 고정한다.
- 확인: [x] 2026-07-15 11:28 Claude — 타당하다고 판단해 수정, 권고를 그대로 채택. `backend/app/services/structured_claim_extractor.py`의 `_MULTIPLE_NOTATION_PATTERN`에 숫자 앞 `(?<![A-Za-z0-9])` lookbehind와 `x`/`X`/`×` 뒤 `(?![A-Za-z0-9])` lookahead를 추가 — "S2X"는 "2" 앞이 영문자(S)라 lookbehind에서 걸러지고, "2X200"은 "X" 뒤가 숫자(2)라 lookahead에서 걸러진다. "배"는 뒤에 조사가 오더라도 ASCII 영문/숫자가 아니므로 별도 lookahead 없이도 정상 통과한다(권고의 "별도 처리" 필요 없이 기존 `[A-Za-z0-9]` 부정 조건으로 충분함을 확인). 권고가 제시한 최소 회귀 세트를 그대로 테스트로 추가: `test_multiple_claim_is_not_grounded_by_x_inside_a_product_name`("S2X" drop), `test_multiple_claim_is_not_grounded_by_x_inside_an_identifier`("2X200" drop), `test_multiple_claim_is_grounded_by_space_separated_and_multiplication_sign_notation`("2 x"·"2×" accept, 기존 테스트가 이미 "2x"·"2배"·"2배 이상" accept를 커버). `docs/checklist.md` C7 105행에 이번 수정 내역을 기록.

## 2026-07-15 11:40 | 2026-07-15 11:28 T06 후속 MULTIPLE 배수 표기 좌우 경계 추가, uncommitted working tree | 수정요청
- 발견: **중간** `backend/app/services/structured_claim_extractor.py:106-155`, `backend/tests/test_structured_claim_extractor.py:158-200`, `docs/checklist.md:105` — 직전 반례였던 `S2X`/`2X200`은 막혔지만, 경계 조건이 ASCII 영문·숫자에만 걸려 있어 하이픈이 포함된 코드명과 `배`로 시작하는 다른 단어를 여전히 배수 표기로 오인한다. 재현: 원문 `"삼성전자 S-2X 모델이 공개됐다."` 또는 `"삼성전자 2배럴 물량을 확보했다."`에는 배수 주장(`2x`, `2배`)이 없는데 fake Solar가 `op="MULTIPLE"`, `target_value=2`, `target_unit="multiple"`인 claim을 반환하면 `extract()`가 각각 `accepted: 1`, warning 없음으로 통과했다. `S-2X`는 제품/코드명 내부의 표기이고, `2배럴`의 `배`는 단어 일부라 `2배` 배수 근거가 아니다.
- 계약 위반: C7 "원문에 없는 span·기업·숫자 생성 0건" 계약의 배수 표기 grounding이 아직 완전히 보장되지 않는다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 178 tests, backend 332 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했지만 위 하이픈 코드명/한글 접미 반례를 잡지 못한다.
- 권고: 배수 표기를 하나의 정규식으로 모두 처리하지 말고 suffix별 경계를 분리한다. `x`/`X`/`×`는 숫자 앞뒤가 식별자 문자(`A-Za-z0-9_-` 정도)에 붙지 않도록 막고, `배`는 뒤에 한글 명사 음절이 바로 붙으면 제외하되 조사/공백/문장부호는 허용한다. 최소 회귀 테스트로 `S-2X`, `2배럴`은 drop하고 `2x`, `2 x`, `2×`, `2배`, `2배 이상`, `2배가`는 accept를 고정한다.
- 확인: [x] 2026-07-15 12:53 Claude — 타당하다고 판단해 수정, 권고를 그대로 채택. `_MULTIPLE_NOTATION_PATTERN`을 단일 정규식에서 x/X/×용과 배용 두 갈래(`|` alternation)로 분리했다. **x/X/×**: 좌우 경계 문자 집합을 `[A-Za-z0-9]`에서 `[A-Za-z0-9_-]`로 확장(하이픈·밑줄 포함) — "S-2X"의 "2" 앞에 있는 "-"가 이제 경계로 걸려 제외된다. **배**: 새로 lookahead `(?=[가이는은를을도만나로])|(?![가-힣])`를 추가 — "배" 뒤에 흔한 조사 1음절(가/이/는/은/를/을/도/만/나/로) 중 하나가 오면 허용하고, 그 외 한글 음절(예: "럴")이 바로 붙으면 제외한다. 권고가 제시한 최소 회귀 세트를 그대로 테스트로 추가: `test_multiple_claim_is_not_grounded_by_x_inside_a_hyphenated_code_name`("S-2X" drop), `test_multiple_claim_is_not_grounded_by_bae_inside_a_different_word`("2배럴" drop), `test_multiple_claim_is_grounded_by_bae_with_an_attached_particle`("2배가" accept) — "2x"/"2 x"/"2×"/"2배"/"2배 이상" accept는 기존 테스트가 이미 커버. `docs/checklist.md` C7 105행에 이번 수정을 기록하고, 정규식 기반 grounding이 5차 수정째임을 알려진 구조적 한계로 명시했다.

## 2026-07-15 13:05 | 2026-07-15 12:53 T06 후속 MULTIPLE 배수 표기 suffix별 경계 분리, uncommitted working tree | 수정요청
- 발견: **중간** `backend/app/services/structured_claim_extractor.py:117-124`, `backend/tests/test_structured_claim_extractor.py:231-242`, `docs/checklist.md:105` — `S-2X`와 `2배럴` 반례는 막혔지만, `배` 뒤 조사 화이트리스트가 너무 좁아 정상 배수 표현을 드롭한다. 재현: 원문 `"삼성전자 영업이익이 2배보다 늘었다."` 또는 `"삼성전자 영업이익이 2배까지 늘었다."`는 `2배` 배수 숫자가 원문에 명확히 있는데, fake Solar가 `op="MULTIPLE"`, `target_value=2`, `target_unit="multiple"`인 claim을 반환하면 `extract()`가 각각 `accepted: 0`, `"target_value"` warning으로 드롭했다. 반면 `"2배가"`와 `"2배로써"`는 통과해, 현재 동작이 실제 배수 여부가 아니라 임의 조사 목록 포함 여부에 좌우된다.
- 계약 위반: C7의 "원문에 없는 숫자 생성 0건"은 환각 방지 계약이지만, S7 Structured Claim 추출 계약상 원문에 있는 정상 배수 주장을 과하게 버리면 extraction recall이 떨어진다. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh`는 재현했고 frontend 178 tests, backend 335 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했지만 위 정상 배수 표현 누락을 잡지 못한다.
- 권고: `배` 뒤를 조사 화이트리스트로 열거하기보다, 명확히 다른 단어로 확인된 케이스를 제외하는 방향으로 바꾼다. 예를 들어 `배럴` 같은 단위/명사 블록리스트 또는 형태소 수준의 후처리를 쓰고, 일반적인 조사/어미(`보다`, `까지`, `로써`, `가`, `는`, `이상`, 공백/문장부호)는 허용한다. 최소 회귀 테스트로 `2배보다`, `2배까지`, `2배로써`, `2배가`, `2배 이상`은 accept, `2배럴`은 drop을 고정한다.
- 확인: [x] 2026-07-15 13:12 Claude — 타당하다고 판단해 수정, 권고의 블록리스트 방향을 그대로 채택. `_MULTIPLE_NOTATION_PATTERN`의 배 갈래를 조사 allowlist(`(?=[가이는은를을도만나로])|(?![가-힣])`)에서 명사-head 블록리스트 방식(`배(?![럴])`)으로 뒤집었다 — "배" 뒤에 `_BAE_NON_MULTIPLIER_HEADS`(현재 "럴") 음절이 오면 배수가 아니라고 보고(배럴/barrel 배제), 그 외에는 전부 배수로 허용한다. 이로써 "2배보다"/"2배까지"/"2배로써"/"2배가"/"2배 이상"은 조사 종류와 무관하게 통과하고 "2배럴"만 드롭된다. 조사·어미는 열린 부류라 allowlist가 태생적으로 불완전하다는 진단에 동의(내가 12:53에 넣은 좁은 allowlist가 원인). 권고의 최소 회귀 세트를 그대로 반영: `test_multiple_claim_is_grounded_by_bae_with_various_particles`를 `["보다","까지","로써","가"," 이상"]` parametrize로 accept 고정, `2배럴` drop은 기존 `test_multiple_claim_is_not_grounded_by_bae_inside_a_different_word`가 유지. `docs/checklist.md` C7 105행에 이번 수정 기록.
- 미결: 배 명사 블록리스트(`_BAE_NON_MULTIPLIER_HEADS`)는 "배"로 시작하는 명사가 열린 집합이라 태생적으로 불완전하다 — 현재는 "럴"(배럴)만 등록했고 실 데이터 반례가 나오면 음절을 추가한다. 완전한 판별은 형태소 분리가 필요하며 후속 과제. 반영 내용은 report_claude.md 2026-07-15 13:12 참고.

## 2026-07-15 13:19 | 2026-07-15 13:12 T06 후속 배 grounding을 조사 allowlist→명사 blocklist로 역전, uncommitted working tree | 승인
- 발견: 없음. 이전 13:05 수정요청은 반영됐다. `op=MULTIPLE`의 `배` 표기는 조사 allowlist에서 명사-head blocklist로 바뀌어 `"2배보다"`, `"2배까지"`, `"2배로써"`, `"2배가"`, `"2배 이상"` 정상 배수 표현이 모두 accept되고, `"2배럴"`은 drop된다. `x/X/×` 쪽도 `"S-2X"` 하이픈 코드명 방어가 유지됨을 재현했다.
- 계약 위반: 없음. `source ~/.nvm/nvm.sh && nvm use 22.17.0 && ./scripts/verify.sh` 재현 결과 frontend 178 tests, backend 339 tests, I9 eval 16 metrics PASS, npm/pip audit 0건, gitleaks 30 commits clean으로 통과했다.
- 권고: 없음. 다만 보고서와 checklist에 이미 기록된 대로 `배` 명사 blocklist와 MULTIPLE 외 op의 단위별 문맥 grounding은 구조적 한계가 남아 있으므로, 실 데이터 반례가 나오면 별도 항목으로 확장한다.
- 확인: [ ]
