# CLAUDE.md

이 문서는 개발 에이전트(Claude Code 등)가 이 프로젝트에서 작업할 때 따라야 할 규칙입니다.
코드를 작성하기 전에 항상 이 문서를 먼저 읽고, 여기 정해진 규칙을 지켜주세요.

---

## 1. 프로젝트 개요

**서비스명**: 첫날 — 신입 알바 온보딩 AI 매니저

**한 줄 소개**: 센스에 기대지 않고, 정해진 대응책으로 돌아가는 가게를 만드는 앱.

**해결하는 문제**: 사장이 '센스'와 '당연함'으로 넘긴 암묵지를 신입 알바가 알 방법이 없어 실수하고, 질책받고, 조기 이탈한다. 이 앱은 가게의 암묵지를 지침으로 만들어 알바가 눈치 안 보고 물어볼 수 있게 한다.

**핵심 기능 3가지** (우선순위 순):
1. **알바생 Q&A** (심장) — 모르는 걸 물으면 AI가 가게 지침대로 답변. 답변에 시각 가이드(화면 따라하기) 포함.
2. **인수인계 지침서** — 사장이 대화로 입력한 가게 정보가 문서로 쌓임.
3. **불만 집계** — 알바 불만을 익명으로 집계해 사장에게 전달.

**대상 사용자**: 카페·소규모 매장의 사장(정보 입력)과 신입 알바생(질문).

---

## 2. 기술 스택

| 구분 | 기술 | 비고 |
|---|---|---|
| 프론트엔드 | React | 컴포넌트 기반 UI |
| 백엔드 | Express (Node.js) | REST API |
| 데이터 | **가짜데이터(mock)** | 이번 단계에선 실제 DB 없음 (아래 4번 참고) |

> 왜 이 스택인가: React는 화면을 조각(컴포넌트)으로 나눠 관리하기 쉽고, Express는 가장 가벼운 Node 백엔드라 빠르게 API 뼈대를 만들 수 있다. 둘 다 자료가 많아 학습에 유리하다.

---

## 3. 폴더 구조

프론트엔드와 백엔드를 폴더로 분리한다.

```
/
├── client/          # React 프론트엔드
│   ├── src/
│   │   ├── components/   # 재사용 UI 조각 (버튼, 메시지 말풍선 등)
│   │   ├── pages/        # 화면 단위 (채팅, 지침서, 로드맵)
│   │   ├── data/         # 가짜데이터 (mock) — API 대신 임시로 사용
│   │   └── App.jsx
│   └── package.json
│
├── server/          # Express 백엔드
│   ├── routes/          # API 경로 (예: /api/questions)
│   ├── data/            # 서버측 가짜데이터
│   └── index.js
│
├── CLAUDE.md        # 이 문서
└── README.md
```

**규칙**:
- 프론트엔드 코드는 무조건 `client/` 안에, 백엔드는 `server/` 안에 둔다.
- 화면 하나 = `pages/` 안의 파일 하나. (예: `ChatPage.jsx`, `GuidePage.jsx`)
- 여러 화면에서 재사용되는 조각은 `components/`로 뺀다. (예: 메시지 말풍선 `MessageBubble.jsx`)

---

## 4. 개발 범위 (중요)

**이번 단계는 UI + 가짜데이터로 껍데기를 만드는 것까지다.**

- ✅ 화면 UI 구현 (채팅, 지침서, 로드맵, 시각 가이드)
- ✅ 가짜데이터로 화면 채우기 (실제처럼 보이게)
- ✅ Express API 뼈대 (가짜데이터를 내려주는 정도)
- ❌ **LLM/AI API 연동은 하지 않는다.** (다음 단계)
- ❌ 실제 데이터베이스 연결 안 함.
- ❌ 로그인/인증 등 부가 기능 안 함.

> 에이전트에게: 요청받지 않은 이상 AI API 연동이나 DB 연결을 임의로 추가하지 마세요. 지금은 "눈으로 보이는 껍데기"가 목표입니다.

---

## 5. 가짜데이터(Mock) 규칙

나중에 실제 API로 쉽게 바꿀 수 있도록, 가짜데이터는 한 곳에 모은다.

- 프론트 가짜데이터는 `client/src/data/`에 파일로 둔다. (예: `mockMessages.js`, `mockGuides.js`)
- 컴포넌트 안에 데이터를 직접 하드코딩하지 말고, `data/`에서 import해서 쓴다.
- 이렇게 하면 나중에 `import mockMessages` → `fetch('/api/messages')` 로 바꾸기만 하면 된다.

예시:
```js
// client/src/data/mockGuides.js
export const mockGuides = [
  { id: 1, category: "마감", title: "마감 절차", steps: ["주문 마감", "머신 청소", "정산", "문단속"] },
  { id: 2, category: "결제", title: "포스기 환불", steps: ["설정→결제취소", "주문 선택", "카드 재삽입", "승인"] },
];
```

---

## 6. 커밋 규칙

- **커밋 메시지는 한국어로 작성한다.**
- 무엇을 했는지 명확하게. (예: `채팅 화면 기본 레이아웃 추가`, `가짜데이터로 지침서 목록 연결`)
- 한 커밋은 하나의 작업 단위로. (여러 기능을 한 커밋에 섞지 않기)

**좋은 예**:
- `채팅 말풍선 컴포넌트 추가`
- `사이드 메뉴 화면 전환 기능 구현`
- `포스기 환불 시각 가이드 화면 추가`

**나쁜 예**:
- `수정` (뭘 수정했는지 모름)
- `update` (한국어 규칙 위반)

---

## 7. 코딩 컨벤션

- **컴포넌트 파일명**: PascalCase (예: `MessageBubble.jsx`, `ChatPage.jsx`)
- **일반 함수·변수**: camelCase (예: `sendMessage`, `guideList`)
- **한 파일이 너무 길어지면**(대략 150줄 넘으면) 컴포넌트를 쪼갠다.
- 주석은 한국어로, "왜 이렇게 했는지"가 중요한 곳에만 단다.
- 색상·간격 같은 디자인 값은 가능하면 CSS 변수로 관리한다. (프로토타입의 `--brand`, `--accent` 방식 유지)

---

## 8. 디자인 기준

기존 프로토타입(`prototype.html`)의 디자인 톤을 유지한다.

- **대표색**: 딥그린 `#3E6B4F` (알바생/기본), 보라 `#7A5C9E` (사장 모드), 오렌지 `#C9683D` (긴급/경고)
- **배경**: 크림색 `#FBF7F0`
- **톤**: 다급한 상황에서도 침착하게 안내하는 느낌. 둥근 모서리, 넉넉한 여백.
- 시각 가이드는 "1장 1클릭 · 굵은 빨간 박스 + 화살표 · 배경 딤처리 · 세로 스텝 · 짧은 CTA" 원칙을 따른다.

---

## 9. 작업 시작 전 체크리스트

에이전트가 새 작업을 시작할 때 확인:
1. 이 작업이 **개발 범위(4번)** 안에 있는가? (AI 연동·DB는 하지 않음)
2. 파일을 올바른 폴더(`client/` 또는 `server/`)에 두는가?
3. 데이터를 하드코딩하지 않고 `data/`의 가짜데이터를 쓰는가?
4. 커밋 메시지를 한국어로 명확히 쓰는가?
