---
name: cheotnal-design
description: "첫날" 앱(신입 알바 온보딩 AI 매니저)의 화면을 만들거나 수정할 때 반드시 사용하는 디자인 규칙. 색상, 타이포그래피, 컴포넌트 패턴, 어투를 정의한다. 새 화면(채팅, 카드, 리포트, 안내 등)을 그리거나 기존 화면을 고칠 때, 이 문서의 색상 토큰과 컴포넌트 패턴을 그대로 재사용해 톤을 통일할 것. "첫날", "온보딩 앱", "알바 매니저 앱" 화면 작업이라면 항상 이 스킬을 먼저 확인한다.
---

# 첫날 디자인 스킬

**"다급하고 불안한 첫날, 침착하게 옆에서 알려주는 사람"** — 이 앱의 모든 화면은 이 한 문장을 기준으로 만든다.

이 스킬은 세 개의 레퍼런스 앱에서 원칙을 가져왔다:

| 레퍼런스 | 가져온 것 |
|---|---|
| **토스** | 안심시키는 톤, 넉넉한 여백, 화면(또는 카드)당 하나의 행동만 |
| **듀오링고** | 친근하고 다정한 어투, 실수해도 다그치지 않는 피드백 |
| **ChatGPT** | 말풍선 안에 버튼·리스트·단계가 자연스럽게 섞이는 구조 |

---

## 1. 색상 토큰 (반드시 이 값을 그대로 재사용)

```css
--bg:        #FBF7F0;   /* 배경 — 우유거품 크림색. 흰색 대신 이걸 쓴다 */
--surface:   #FFFFFF;   /* 카드·말풍선 배경 */
--ink:       #2B2622;   /* 본문 텍스트 (진한 원두색, 순검정 금지) */
--ink-soft:  #6F6459;   /* 보조 설명 텍스트 */
--line:      #EAE1D4;   /* 테두리·구분선 */

--brand:      #3E6B4F;  /* 대표색 — 딥그린. 알바생/기본 모드 */
--brand-soft: #EAF1EB;  /* brand의 옅은 배경 버전 */

--owner:      #7A5C9E;  /* 사장님 모드 전용 — 보라 */
--owner-soft: #F0EBF5;

--accent:      #C9683D; /* 긴급·경고·확인 필요 — 오렌지 */
--accent-soft: #FBEBE2;

--mark: #E4322B;        /* 시각 가이드의 강조 마커 전용 — 빨강. 이 외 용도로 쓰지 않는다 */
```

**규칙**:
- 순수 흰색(`#FFF`) 배경은 카드·말풍선 안에서만. 화면 전체 배경은 항상 `--bg`.
- 순검정 텍스트 금지. 본문은 항상 `--ink`.
- **역할별 색 분리를 지킨다**: 알바생 관련 = 그린 계열, 사장님 관련 = 보라 계열, 긴급/미확인 = 오렌지 계열. 색을 섞어 쓰지 않는다. (예: 사장님 화면에 그린 버튼 쓰지 않기)
- `--mark`(빨강)은 오직 "여기를 누르세요" 시각 가이드에서만 쓴다. 일반 경고에는 `--accent`를 쓴다.

---

## 2. 타이포그래피 & 형태

- 폰트: 시스템 폰트 (`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, "Noto Sans KR", sans-serif`)
- 제목: 15~22px, `letter-spacing:-.02em` (자간을 살짝 좁혀 또렷하게)
- 본문: 12.5~14px, `line-height:1.5`
- 보조 텍스트: 10.5~11.5px, `--ink-soft`
- **모서리는 항상 둥글게.** 카드 16~20px, 버튼 10~14px, 말풍선 16px (단, 화자 쪽 모서리만 5px로 콕 찍어 "말하는 사람"을 표시)
- 그림자는 아주 옅게만: `box-shadow:0 4px 16px rgba(43,38,34,.05)` 수준. 진한 그림자 금지.

---

## 3. 컴포넌트 패턴

### 3-1. 채팅 말풍선
```
상대(AI) 메시지: 흰 배경 + 옅은 테두리, 좌측 하단 모서리만 5px (말하는 방향 표시)
내(사용자) 메시지: brand색(또는 owner색) 배경 + 흰 글씨, 우측 하단 모서리만 5px
```
- 알바생 대화 스레드 = brand(그린) 톤, 사장님 대화 스레드 = owner(보라) 톤으로 전체 컬러를 바꾼다.

### 3-2. 메시지 안에 박히는 행동 버튼 (ChatGPT 패턴)
정보만 주고 끝내지 않는다. 다음 행동이 있으면 **말풍선 안에 버튼을 심는다.**
```
[말풍선 텍스트 한두 줄]
[━━━━━━━━━━━━━━━━━━━]
[  📱 화면 따라하기 가이드      › ]  ← 브랜드색 꽉 찬 버튼, 좌우 양끝 정렬
```

### 3-3. 카드 (지침서 문서, 로드맵 스텝)
- 흰 배경 카드 + 옅은 테두리 + 둥근 모서리
- 카테고리는 작은 알약(pill) 태그로: `brand-soft` 배경 + `brand` 글씨, 10.5px, 굵게
- 확인/대기 중인 항목은 카드 전체를 `accent-soft` 톤으로 바꿔서 "이건 다르다"를 색으로 알린다 (텍스트로 "주의!"라고 쓰지 않아도 되게)

### 3-4. 시각 가이드 강조 마커 (네가 정한 3원칙, 스킬에 고정)
- **1장 1클릭**: 이미지 하나에 강조 포인트 하나만
- **강조 = 굵은 빨간(`--mark`) 테두리(4px) + 화살표**, 나머지 배경은 55% 어둡게 딤 처리
- **크롭 금지**: 버튼만 잘라내지 말고 화면의 1/4~1/2 맥락을 유지
- 세로 스크롤 스텝 구조, 각 스텝 아래 **CTA는 5단어 이하** ("우측 하단 [바탕화면] 클릭")

### 3-5. 콜아웃 / 안내 박스
```
[아이콘] 짧은 안내 문장. 강조어는 <b>bold</b>로만, 색은 배경색과 세트로 (brand-soft엔 brand 글씨).
```

---

## 4. 어투 (Voice & Tone) — 듀오링고에서 가져온 규칙

- **문장은 짧게.** 한 말풍선에 한 가지 정보만.
- **다그치지 않는다.** "틀렸어요" 대신 "조금만 더 자세히요 🙂" 처럼 부드럽게.
- **이모지는 감정 온도 조절용으로만, 남발 금지.** 말풍선 하나에 최대 1개.
- **명령 대신 안내.** "입력하세요" 보다 "알려주세요"
- 사장님 모드는 조금 더 담백하게, 알바생 모드는 조금 더 다정하게 — 같은 정보라도 대상에 따라 온도를 미세 조정.

**나쁜 예** → **좋은 예**
- "오류: 정보 없음" → "이건 가게 확인이 필요해요, 사장님께 여쭤볼게요"
- "3단계를 완료하십시오" → "이제 마지막이에요, 카드만 꽂으면 끝!"

---

## 5. 체크리스트 (새 화면 만들 때 이것만 확인)

1. 배경은 `--bg`, 카드는 `--surface`, 순검정·순흰 텍스트 없는가?
2. 역할별 색(그린=알바생 / 보라=사장님 / 오렌지=긴급)이 섞이지 않았는가?
3. 화면(또는 카드) 하나에 행동이 하나로 좁혀져 있는가? (토스 원칙)
4. 말풍선/문구가 다그치지 않고 친근한가? (듀오링고 원칙)
5. 행동이 필요한 정보엔 버튼이 말풍선 안에 박혀 있는가? (ChatGPT 패턴)
6. 모서리는 둥글고, 그림자는 옅은가?
