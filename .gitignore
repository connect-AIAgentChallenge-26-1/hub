# Dependencies
node_modules/

# Env files (keep .env.example tracked)
.env
.env.*
!.env.example

# Holds live GitHub access tokens after OAuth login — never commit these.
# v7 replaced the single data/session.json with per-session files under
# data/sessions/ (one per logged-in browser session).
data/sessions/

# Claude Code local config/skills — not shared with the team
.claude/

# Build output
dist/
dist-ssr/

# .NET build artifacts
tools/analyzer/bin/
tools/analyzer/obj/

# Logs
*.log
npm-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
